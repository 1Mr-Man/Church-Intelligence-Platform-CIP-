{
    "applicationName": "Church Intelligence Platform",
    "cipVersion": "0.1.0",
    "gitCommit": "d9bd2d3cf0164f57c828b887229dbdbe01d0e293",
    "gitCommitShort": "d9bd2d3",
    "gitBranch": "claude/cip-foundation-init-i85g87",
    "gitCommitNote": "This is the source tree commit (Phase 24.3.2's own commit) this rebuild was performed against. Phase 24.3.2 fixes the quality-tier backlog-visibility gap: SpeechQualityDiagnostics gains consecutive_jobs_dropped (a streak, reset the moment the quality worker actually processes any job, incremented on every dropped-for-backlog job), and commands::classify_quality_backlog derives a QualityBacklogState (normal/busy/falling_behind/overloaded) from it, surfaced in PilotDiagnosticsPanel so an operator can tell one transient drop apart from sustained hardware-too-slow backlog. See docs/phase-24-3-2-audit.md. This is the source tree commit (Phase 24.3.1's own commit) this rebuild was performed against. Phase 24.3.1 fixes the quality-tier language-conditioning gap: SpeechEngine::transcribe_once now accepts a language_hint, populated from the fast tier's own real per-window detected language, so both Whisper tiers condition their decode of the same audio on the same evidence instead of potentially disagreeing defaults. See docs/phase-24-3-1-audit.md. This is the source tree commit (Phase 24.3's own commit) this rebuild was performed against. Phase 24.3 gives CIP an optional second, independently-loaded Whisper model (AppState.speech_quality_engine) that re-decodes each fast-tier window's raw audio on its own thread and routes the result as a new, linked final transcript segment through the existing detection/persistence pipeline - never an in-place edit of the original. See docs/phase-24-3-audit.md. This is the source tree commit (Phase 24.2's own commit) this rebuild was performed against. Phase 24.2 gives WhisperSpeechEngine one real, bounded interim whisper.cpp decode per window (at most one extra pass, once 1.5s has buffered without the window closing) instead of only ever emitting a final segment - closing the one actual gap in an architecture (SpeechEngine trait, the real-time worker's is_final gating, the frontend's discard logic) that already fully supported interim segments end to end since Phase 1.2. Never fabricated text: the same real decode the final segment uses, run early on a shorter prefix of the same buffered audio. See docs/phase-24-2-audit.md. This is the source tree commit (Phase 24.1's own commit) this rebuild was performed against. Phase 24.1 adds get_prepared_item_slide (a thin, read-only re-render of an already-persisted presentation item via the existing render_content pipeline) so the Live/Preview stage's queue strip - shipped in Phase 24 - shows each prepared item's actual word-wrapped RenderedSlide instead of a raw-text approximation, closing the one honestly-deferred gap Phase 24's own audit doc flagged. Phases 23 and 24 (the layout-shell milestone and the dual Live/Preview panel this phase extends) were both frontend-only and never triggered a Windows rebuild - this is the first rebuild since Phase 22. See docs/phase-24-1-audit.md. This is the source tree commit (Phase 22's own commit) the Phase 22 rebuild was performed against. Phase 22 is a direct response to operator feedback (with screenshots) naming three detection-accuracy symptoms - inaccurate, late-appearing, and rare (~10%) Bible/sermon detections - plus a hypothesis that CIP mishandles a Nigerian pastor's English. Root cause, grounded in code: this project's own default/documented Whisper model (ggml-tiny.en.bin) is whisper.cpp's smallest, least accurate model - detection can only match text Whisper transcribed correctly, so a model this small mistranscribing what was said, especially on an under-represented accent, is a real, sufficient explanation for all three symptoms. A second issue found while investigating: install_whisper_model always copies whatever file the operator selects to the same fixed destination filename (by design, so the engine can always find the active model at one known path), which meant System Diagnostics' displayed path could never honestly say which model tier was actually installed. This phase adds a heuristic, size-based classify_model_size_tier function threaded into WhisperModelDiagnostic::Present as a new sizeTierHint field (Rust + TS), a startup log warning when a tiny-class model loads, and rewrites docs/live-speech.md/docs/first-use.md to recommend base.en/small.en as the real-time floor and large-v3-turbo for highest accuracy - while explicitly, honestly scoping out a true concurrent fast+quality dual-tier engine (what the operator actually asked for) as real, deferred future work, not implemented here. The stale Phase 12 'Language support' doc section (still claiming zero non-English support after Phase 12 shipped real Yoruba/Hausa selection) is corrected in the same pass; Igbo's absence is re-confirmed as a real whisper.cpp vocabulary gap, not an oversight. See docs/phase-22-audit.md for full reasoning. Previous note, retained for provenance: This is the source tree commit (Phase 21's own commit) the Phase 21 rebuild was performed against. Phase 21 adds VAD-triggered flush to WhisperSpeechEngine - a window now flushes to inference as soon as at least 1.5s has buffered and the trailing 0.4s is genuinely quiet, instead of always waiting for the fixed ~3s cap, so most windows now end where the speaker actually paused rather than at an arbitrary sample count. The fixed 3s cap is unchanged and still fires for continuous speech with no detectable pause. True audio-overlapping windows (the other half of the operator's own 'overlapping windows + VAD' request) were deliberately deferred - see docs/phase-21-audit.md for why the duplicate-transcription risk of that technique needs real, timing-sensitive audio this container cannot produce or validate against. Previous note, retained for provenance: this is the source tree commit (the README-update commit immediately following Phase 20's own core-implementation commit bca06ab) the Phase 20 rebuild was performed against. Phase 20 adds ReferenceKind::FuzzyBook - a new near-miss book-name detection tier in core/bible (Levenshtein-distance matching against single-word canonical book names, e.g. 'Roman 8:28' -> Romans 8:28), the leading item explicitly deferred from Phase 19's response to the operator's own detection-accuracy audit - and fixes a real UX bug the operator reported with a screenshot: the Live Service UI's manual 'Correct Active Context' field required typing the exact internal 3-4 letter database book code, since correct_scripture_context passed the raw input straight to an exact-match SQL lookup while the frontend additionally uppercased it (turning 'John' into the never-matching 'JOHN'); it now resolves through the same canonicalize_book alias table the live detector already uses. See docs/phase-20-audit.md for full reasoning. Previous note, retained for provenance: this is the source tree commit (Phase 19's own commit) the Phase 19 rebuild was performed against. Phase 19 is a direct response to the operator's own unsolicited detection-accuracy audit of the repository. Every claim the audit cited was independently re-verified against the actual current code before acting on it (see docs/phase-19-audit.md), catching one real factual error (Sermon/Service/Music intelligence DO run live automatically via route_segment_to_live_intelligence_engines, contrary to the audit's claim). Of the audit's own findings, this phase implemented only the one item that was both real and fully safe/testable without real hardware: general ordinal-word normalization (core/bible/src/normalize.rs) - first/second/third and their digit-suffix forms 1st/2nd/3rd now convert to digits ahead of detection, closing a real gap (digit-suffix forms like '1st Corinthians' were never in book_alias.rs's literal alias table and were invisible to the detector before this phase). The audit's own top-ranked items (overlapping Whisper windows + VAD, interim transcripts, fuzzy book-name matching) were explicitly deferred - the first two touch the live capture path this container cannot safely validate without real hardware, and fuzzy matching is a dedicated-phase-sized feature. This phase changes only core/bible/src/normalize.rs - no other Rust crate, no frontend file, no migration, no Tauri command or event. Previous note, retained for provenance: this is the source tree commit (Phase 18's own commit) the Phase 18 rebuild was performed against. Phase 18 (loopback-aware NO SIGNAL/LOW SIGNAL guidance) is a direct response to a real Windows pilot report: an operator selected Stereo Mix (Realtek(R) Audio) - a loopback device that mirrors system playback, not a microphone - to capture a YouTube video's audio, started listening, and got NO SIGNAL throughout while System Diagnostics confirmed 22669 audio chunks genuinely received but 0/0 inferences (all windows classified as silence). Traced the full capture path (device selection, stream negotiation, downmix, RMS computation, resampling) and found no CIP defect - two independently-implemented RMS computations, in two different crates, both agreed genuinely near-silent PCM was reaching the app, pointing at a real Windows Stereo Mix quirk (its own recording level muted/zero, or system audio not routed through the mirrored default output) outside what a cross-platform audio library can detect or fix (see docs/phase-18-audit.md). What was addressable: the existing message assumed a physical microphone ('move it closer, raise its gain'), actively wrong advice for a loopback device. describeAudioSignal now recognizes known Windows loopback device names (Stereo Mix and common aliases) and gives guidance that actually applies. Unlike every phase since Phase 7.1, this phase changes ZERO Rust code - it is a pure frontend change (apps/desktop/src/lib/format.ts + apps/desktop/src/components/LiveChurchBrain.tsx). core/bible, core/sermon, core/music, core/service, core/presentation (every domain contract crate) are entirely untouched, and this rebuild's own symbol-regression check confirms zero new or changed Rust symbols. See loopbackAudioSignalGuidanceProof below and docs/phase-18-audit.md.",
    "targetArchitecture": "x86_64 (x64)",
    "targetPlatform": "Windows 11 x64",
    "installer": {
        "filename": "Church Intelligence Platform_0.1.0_x64-setup.exe",
        "installerType": "NSIS self-extracting installer (.exe)",
        "fileSizeBytes": 14423517,
        "sha256": "0aaf6f73e5970568d5a6882e7d5ad1cf3eeb514e7e9415b1779ea2e2875c18d6",
        "buildTimestampUtc": "2026-09-04T15:17:00Z",
        "packagedIntoReleaseTimestampUtc": "2026-09-04T15:17:00Z"
    },
    "speechWorkerDecouplingProof": {
        "summary": "The operator's real Windows report (after Phase 3.8.7.1's model installed successfully) showed app slowness, poor/repetitive transcription, empty Intelligence Feed, and intermittent NO SIGNAL. Root-caused (see docs/phase-3-8-7-2-audit.md) to Whisper inference running synchronously on cpal's real-time audio capture callback thread, blocking both real-time audio servicing and the speech_engine mutex the UI's own 3-second status poll also needed.",
        "fix": "handle_audio_chunk moved off the audio callback thread onto a dedicated speech worker thread (mirroring the already-proven acoustic-worker pattern), fed via an unbounded channel so the real-time audio callback never blocks or drops a chunk.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe finds the mangled symbol for cip_desktop_lib::commands::spawn_speech_worker - direct proof this fix is genuinely compiled into this exact binary, not just present in source.",
        "sermonContentCrossDomainFinding": "Confirmed, separately, that Sermon/Content/Cross-Domain Intelligence are not wired to the live audio path at all - a pre-existing, already-documented architectural boundary (Phase 3.8's own ServiceReplay screen exists for this reason), not a regression and not fixed this phase.",
        "seeAlso": "pilot-evidence/3.8.7.2/build/speech-worker-decoupling-evidence.json, docs/phase-3-8-7-2-audit.md, docs/phase-3-8-7-2-real-time-speech-performance.md"
    },
    "backpressureProof": {
        "summary": "The operator's real Windows report after Phase 3.8.7.2's worker-thread fix confirmed genuine improvement (transcription active, audio arriving) but CIP still became slow over extended listening, transcript remained poor/repetitive, and intermittent NO SIGNAL persisted. Re-auditing the actual current code (docs/phase-3-8-7-3-audit.md) confirmed the operator's own hypothesis: the speech channel (`mpsc::channel::<AudioChunk>()`, start_listening) was genuinely unbounded - if Whisper inference ever takes longer than the ~3s of audio each inference window represents, the backlog grows without limit, and the worker falls permanently further behind live audio.",
        "fix": "A shared `pending_ms: Arc<AtomicU64>` tracks the wall-clock duration of audio queued but not yet fed to the engine (milliseconds, not raw chunk count, since chunk size/rate varies by capture device). start_listening's sink closure increments it on send; spawn_speech_worker decrements it on dequeue. When the remaining backlog crosses OVERLOAD_THRESHOLD_MS (10s), the worker drains and discards the entire queued backlog plus WhisperSpeechEngine's own internal buffer (new SpeechEngine::discard_buffered_audio() trait method) rather than grinding through an ever-more-stale FIFO - this was chosen over a plain bounded/drop-newest channel specifically because that design would still leave the worker permanently behind, never catching up to live audio.",
        "secondaryFixes": "(1) The inferences-attempted counter previously counted every feed_audio call to a ready engine, not just the ~1-in-300 that actually trigger a real whisper.cpp inference pass - fixed via a new SpeechEngine::last_feed_triggered_inference() trait method, checked after each feed_audio call. (2) All four status-poll call sites (get_pilot_diagnostics, start_listening, stop_listening, get_live_status) locked speech_engine's mutex just to read is_ready() - the same mutex the worker holds for the full duration of a blocking Whisper inference - fixed by caching the fact once at construction (AppState::speech_ready), which never changes after that for either existing engine. (3) A low-severity race where a speech worker whose channel closed mid-inference could write output into a newer listening session after a rapid stop/start - fixed via a listening_generation counter checked before any non-empty result is emitted/persisted.",
        "newDiagnostics": "SpeechRuntimeDiagnostics gained queuePendingMs, queueHighWaterMs, overloadEvents, audioMsDroppedOverload, lastInferenceDurationMs/maxInferenceDurationMs/avgInferenceDurationMs, lastTranscriptPipelineDurationMs, and a derived overloadState (normal/busy/falling_behind/overloaded) - all surfaced in the System Diagnostics panel's new 'Speech pipeline health' section, so the operator can directly answer whether audio is backing up, whether inference is keeping up, and whether the app is actively shedding stale audio.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe finds the mangled symbols for cip_desktop_lib::commands::spawn_speech_worker, cip_desktop_lib::commands::handle_audio_chunk, cip_desktop_lib::commands::saturating_sub_u64, and both new SpeechEngine trait method overrides (WhisperSpeechEngine's discard_buffered_audio/last_feed_triggered_inference) - direct proof this phase's fix is genuinely compiled into this exact binary. classify_overload/chunk_duration_ms (small pure functions) were inlined by the release-mode optimizer, as expected, and are instead verified via 10 dedicated unit tests run as part of this phase's regression suite.",
        "notImplementedThisPhase": "Extended-overload (Test C) and stop-during-inference (Test E) real-thread integration scenarios could not be exercised via automated test in this container - consistent with this codebase's own established testing boundary (commands.rs's test module: command tests stop short of standing up a full tauri::test::mock_builder() harness). These require the operator's own real-hardware 20-30 minute long-run test.",
        "seeAlso": "docs/phase-3-8-7-3-audit.md, docs/phase-3-8-7-3-live-speech-stability.md, pilot-evidence/3.8.7.3/"
    },
    "runtimeDependencyProof": {
        "peImportInspection": "x86_64-w64-mingw32-objdump -p against the built cip-desktop.exe lists libstdc++-6.dll as a genuine dynamic import, alongside only standard Windows system DLLs (KERNEL32, user32, ole32, etc.) and WebView2Loader.dll (already handled by Tauri's own NSIS generation). objdump run again against libstdc++-6.dll itself shows it further depends on libgcc_s_seh-1.dll and libwinpthread-1.dll, and objdump against those two shows no further non-system dependencies - the complete, closed set is exactly these three DLLs.",
        "installerExtractionProof": "The packaged NSIS installer was extracted directly with 7z and inspected: libstdc++-6.dll, libgcc_s_seh-1.dll, and libwinpthread-1.dll are all present at the installer root (the same directory cip-desktop.exe installs into, $INSTDIR - confirmed via the generated installer.nsi's `File` directives, placed after `SetOutPath $INSTDIR` alongside cip-desktop.exe and WebView2Loader.dll, with matching `Delete` entries in the uninstaller section). This is now an automated, build-time-enforced check inside scripts/build-windows-whisper.sh, not a one-off manual verification.",
        "dllSourceAndArchitecture": "All three DLLs were resolved from the exact posix-threads mingw-w64-x86-64 toolchain used to build cip-desktop.exe itself (via `x86_64-w64-mingw32-g++/gcc -print-file-name=<dll>`, never a hardcoded path), confirmed PE32+ x86-64 via file(1), and had debug symbols stripped (x86_64-w64-mingw32-strip --strip-debug) to keep the installer size reasonable (libstdc++-6.dll: 26.3MB -> 4.6MB) without altering their code or export tables. See pilot-evidence/3.8.6.1/build/runtime-dll-evidence.json for full SHA-256 checksums and per-DLL detail."
    },
    "architectureVerification": {
        "outerInstallerStub": "PE32 executable (GUI) Intel 80386 (stripped to external PDB), Nullsoft Installer self-extracting archive - a standard 32-bit NSIS bootstrapper; this is normal NSIS behavior on Windows and does NOT mean the installed application is 32-bit",
        "installedApplicationBinary": "cip-desktop.exe: PE32+ executable (GUI) x86-64 (stripped to external PDB), confirmed via `file` against target/x86_64-pc-windows-gnu/release/cip-desktop.exe - the actual application that runs after installation is genuinely x64",
        "method": "file(1) run directly against both the installer and the embedded application binary in this build environment"
    },
    "whisperFeatureProof": {
        "cargoFeatureFlag": "--features whisper, passed explicitly to both `cargo build` and `npx tauri build --target x86_64-pc-windows-gnu` - the FIRST time this flag has ever been passed for a Windows build in this project's history (confirmed by grepping every prior phase's own 'Windows artifact'/'buildEnvironment' provenance text, none of which include it)",
        "cargoTreeProof": "`cargo tree -p cip-desktop --target x86_64-pc-windows-gnu --features whisper -i whisper-rs` shows whisper-rs -> cip-ai-speech -> cip-desktop; the same command without --features whisper shows nothing (whisper-rs is an optional dependency, entirely absent from the graph otherwise) - re-verified against this Phase 3.8.7.1 rebuild, unchanged",
        "embeddedBinarySizeProof": "cip-desktop.exe (the installed application binary, before NSIS compression): 34,407,914 bytes as of this Phase 3.8.7.1 rebuild (up from 33,776,623 bytes in Phase 3.8.6/3.8.6.1 - the increase is consistent with this phase's new tauri-plugin-dialog/rfd dependency and the install_whisper_model command, not a whisper-feature regression). The symbol-strings proof below remains the stronger, unambiguous evidence.",
        "symbolStringsProof": "`x86_64-w64-mingw32-strings cip-desktop.exe | grep -i ggml` finds real whisper.cpp/ggml symbols compiled into the binary: whisper_full_with_state, ggml_backend_init, and related mangled symbols - re-verified against this Phase 3.8.7.1 rebuild, unchanged. See pilot-evidence/3.8.7.1/windows/installer-contents-verification.json for the exact command output."
    },
    "modelProvisioningProof": {
        "command": "install_whisper_model(source_path: String) -> Result<WhisperModelDiagnostic, String> - registered in lib.rs's invoke_handler, feature-gated: the whisper build validates the candidate file by actually loading it as a real Whisper model before copying; the non-whisper build honestly refuses (no engine to validate against)",
        "filePickerProof": "x86_64-w64-mingw32-strings finds tauri_plugin_dialog::init and tauri_plugin_dialog::desktop::pick_file mangled symbols compiled into this exact binary - the native file-picker capability is genuinely present, not just declared as a Cargo dependency",
        "capabilityScope": "capabilities/default.json (main window only) grants dialog:allow-open - no save dialog, no message boxes, no fs/shell/http plugin added. capabilities/display.json (presentation window) is unchanged and still has zero plugin access.",
        "networkDependency": "NONE - this feature requires the operator to have already downloaded a model file themselves (e.g. on their own Windows machine's internet connection); it never fetches anything itself. See modelPackagingStatement.",
        "seeAlso": "pilot-evidence/3.8.7.1/build/model-provisioning-evidence.json, docs/phase-3-8-7-1-audit.md, docs/phase-3-8-7-1-windows-model-provisioning.md"
    },
    "liveIntelligenceRouterProof": {
        "summary": "Phase 3.8.7.4's audit found Sermon/Service Phase/Music-text intelligence to be complete, tested engines that were never called from the live speech pipeline - each deliberately built manual-command-only since Phase 2.1-2.6. This phase connects them for the first time via a Live Intelligence Router, and separately bounds Whisper's raw ~3s-per-window transcript output into 12-20s logical segments before anything downstream (Bible detection included) ever sees it.",
        "segmentation": "New apps/desktop/src-tauri/src/segmentation.rs::TranscriptSegmenter accumulates consecutive raw Whisper-window segments' text until the span reaches a 15s target (the middle of the operator's 12-20s band, landing between 15-18s in practice given Whisper's fixed ~3.0s cadence). Owned exclusively by one speech-worker thread per listening session. Resets on Phase 3.8.7.3's own overload-drain (preventing pre/post-overload text splicing) and force-flushes any partial window when stop_listening closes the channel (previously, any sub-threshold buffered text was simply lost on stop).",
        "router": "commands.rs::route_segment_to_live_intelligence_engines runs immediately after handle_final_transcript succeeds, on the exact same bounded segment. Calls crate::sermon::analyze_and_queue, crate::service::analyze_and_queue, and music::analyze_and_queue unchanged - the identical functions each manual Tauri command already called, with identical event emissions (SermonFindingDetected/SermonStateChanged/SermonThemeChanged/SermonStructureUpdated, ServicePhaseChanged/ServiceAnomalyDetected, MusicFindingDetected) and timeline records. No new engine logic, no new database schema, no new event contracts, no new Tauri commands - only a new caller.",
        "prayerAndWorshipCoverage": "Neither is a separate engine: PrayerPoint is a SermonElementKind Sermon Intelligence already detects internally, and ServicePhase::Worship is a phase Service Intelligence already detects internally (both confirmed in docs/phase-3-8-7-4-audit.md). Routing Sermon+Service therefore surfaces Prayer/Worship findings with zero additional code.",
        "deliberatelyExcluded": "Cross-Domain Correlation and Content Intelligence remain manual-only, per their own explicit 'never automatic' doc comments (a considered Phase 2.4/2.7/2.8 design decision this router does not reverse). Automatic Altar Call detection remains unimplemented - SermonSectionKind::AltarCall exists only as an operator-assignable label with no phrase detector.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe finds the mangled symbols for segmentation::TranscriptSegmenter::push/flush/flush_remaining/reset and commands::finalize_and_route_segment - direct proof this phase's code is genuinely compiled into this exact binary. The router's own dispatch functions were inlined by the release-mode optimizer (single call site each, the classic inlining case) and are verified via the regression suite instead.",
        "seeAlso": "docs/phase-3-8-7-4-audit.md, docs/phase-3-8-7-5-audit.md, pilot-evidence/3.8.7.5/"
    },
    "overloadSegmenterFixProof": {
        "trigger": "The operator's real Windows diagnostics for the Phase 3.8.7.5/3.8.7.6 artifact showed 'Nothing transcribed yet' and 'Transcript pipeline: last n/a' despite 21/21 successful Whisper inferences, with exactly 21 overload events (a 1:1 match, not a coincidence) and an average inference duration of 14,991ms against a 10-second overload threshold.",
        "rootCause": "spawn_speech_worker (commands.rs) is single-threaded: while blocked inside one Whisper inference, CPAL keeps depositing new audio chunks into the channel, so the backlog counter climbs by roughly that inference's own wall-clock duration. On hardware where a single inference alone exceeds OVERLOAD_THRESHOLD_MS (10s), the very next dequeue after every successful inference hits the overload branch, which was unconditionally calling segmenter.reset() (Phase 3.8.7.5) - discarding the text that inference just produced before it could ever reach the 15s accumulation target and flush. See docs/phase-3-8-7-7-audit.md for the full trace and arithmetic.",
        "fix": "A new consecutive_overloads counter (commands.rs, owned exclusively by the speech worker thread) tracks overload crossings since the worker was last caught up - reset to 0 on every normal dequeue. A new pure function should_reset_segmenter_on_overload(consecutive_overloads) only returns true at >= 2 (backlog still elevated on the very next dequeue, immediately after already draining once - genuine sustained overload). A single isolated crossing (the case on this operator's hardware) now preserves the segmenter's already-accumulated text; the stale audio backlog itself is still discarded unconditionally either way, unchanged from Phase 3.8.7.3's original design. OVERLOAD_THRESHOLD_MS itself was NOT changed, per the operator's own explicit instruction not to blindly raise it.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe finds the exact log string '%speech worker overloaded: discarded ~' (the overload branch's own log::warn!, now gated by the new consecutive-overload logic before it reaches segmenter.reset()) and the mangled symbols for commands::handle_audio_chunk, commands::finalize_and_route_segment, and segmentation::TranscriptSegmenter::push/flush_remaining/reset, all still present. should_reset_segmenter_on_overload itself (a small pure function, single call site) was inlined by the release-mode optimizer - the same pattern already established for classify_overload in Phase 3.8.7.3 and the router's own dispatch functions in Phase 3.8.7.5 - and is instead verified via two new dedicated unit tests (should_not_reset_segmenter_on_the_first_isolated_overload_crossing, should_reset_segmenter_once_overload_persists_across_consecutive_dequeues), both passing as part of this phase's regression suite (248/248 cip-desktop tests, up from 246).",
        "seeAlso": "docs/phase-3-8-7-7-audit.md, pilot-evidence/3.8.7.7/"
    },
    "bibleDisplayShortcutProof": {
        "trigger": "Real Windows operator screenshots comparing Live Service and Service Replay showed Service Replay reaching a detected Bible reference (MAT 6:9) and presenting it correctly, but the operator noted the existing Needs Attention -> Approve -> scroll to Presentation card -> Prepare -> Display flow cost several seconds and clicks during a live service. The operator's own explicit request: replace 'Approve' with 'Display' for Bible findings, or add Display alongside it.",
        "designDecision": "Chose 'replace Approve with Display' (the operator's own stated preference) over 'add Display alongside Approve' - fewer buttons, and Reject remains available unchanged. No new Tauri command was added: the new 'display' action (apps/desktop/src/components/workspace/actions.ts) chains the exact same approve_suggestion, prepare_presentation, and display_presentation commands the Presentation card's own three separate buttons already call (commands.ts, LiveChurchBrain.tsx:684-704), inside one withBusy-wrapped async handler in LiveChurchBrain.tsx's handleUnifiedAction. display_presentation already opens the display window itself if it isn't open (commands.rs:2786, open_display_window) and already handles swapping out a currently-active item (presentation::prepare_to_activate/commit_activation) - both pre-existing, already-tested behaviors this phase does not touch.",
        "scope": "Bible domain only, in the Needs Attention queue (apps/desktop/src/components/workspace/AttentionQueue.tsx). Music/Sermon/Service/Content/Correlation actions are unchanged. The separate diagnostics-mode 'Pending Suggestions' panel and its A/R/E/P keyboard shortcuts are unchanged - they call approveSuggestion directly and were not part of the operator's request.",
        "noNewBackendCommand": "Zero new Tauri commands, zero new events, zero new database columns/tables. Purely a frontend orchestration change, matching AttentionQueue's own existing design principle ('this component never talks to the backend itself... only calls existing commands').",
        "regressionProof": "Frontend: typecheck 0 errors, lint 0 errors (4 pre-existing warnings unchanged), test 210/210 passed (unchanged count - one existing assertion in actions.test.ts updated from ['approve','reject'] to ['display','reject'] for the bible domain, no new test files needed since no sibling action in this codebase has dedicated LiveChurchBrain-level test coverage either), build clean. Zero Rust files changed this phase - no Rust regression re-run needed.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe confirms the three chained command names (approve_suggestion, prepare_presentation, display_presentation) are still registered in the invoke handler's literal command-name table, and their handler function symbols are still present - direct proof these pre-existing commands the new frontend code calls are genuinely compiled into this exact binary. The frontend orchestration logic itself lives in the minified JS bundle Tauri embeds into the binary (confirmed present - the literal string 'Display' appears in the extracted binary), verified functionally via the frontend test suite rather than binary disassembly of minified JS, consistent with how this project verifies frontend-only changes."
    },
    "sermonHarvestProof": {
        "trigger": "The user shared the project's original master architecture document (the same specification this codebase has been built against since Phase 0) plus example videos, and asked to continue building out the remaining vision, explicitly authorizing all five identified gaps: multi-screen presentation output, semantic/paraphrase Bible detection, Sermon Harvest, real audio fingerprinting, and multi-language support - with Sermon Intelligence kept strict/evidence-only rather than adopting the document's LLM-based generative framing. Sermon Harvest was sequenced first because it required zero new dependencies, zero new detection logic, and zero new AI - it only assembles data this application already captures through five separately-tested pipelines.",
        "design": "harvest_sermon(conn, findings, sermon) (new module apps/desktop/src-tauri/src/harvest.rs) is a pure aggregation function with no detection logic of its own: it does not classify text, does not invent a title/summary when one is absent, and runs no new pass over the transcript. It reads back sections (persistence::list_sermon_sections), elements (every non-rejected IntelligenceFinding with domain==Sermon and sermon_id==Some(sermon.id) from the in-memory FindingQueue), scripture (every Suggestion for the sermon's service), transcript (bounded 5,000 segments), and timeline (bounded 5,000 entries) into one SermonHarvest bundle.",
        "scopeBoundaryStatedHonestly": "IntelligenceFinding::sermon_id linkage lives only in the in-memory FindingQueue, never persisted to the database - a deliberate Phase 2.0 design already documented in docs/phase-3-8-7-6-live-intelligence-integration-audit.md. This means Harvest only ever works for the currently active sermon, in the same app session it was delivered in. The Tauri command calls active_sermon_or_error (the same guard every other active-sermon-scoped command already uses) and refuses cleanly instead of returning a misleadingly incomplete elements: [] bundle for a past/restarted session. A past-session harvest remains a real future need, not attempted this phase.",
        "filesChanged": "New apps/desktop/src-tauri/src/harvest.rs (SermonHarvest struct + harvest_sermon() + 3 unit tests). New harvest_sermon Tauri command (commands.rs), registered in lib.rs - records a SermonHarvested timeline entry and emits a new SermonHarvested event (events.rs), mirroring every other explicit operator action in this codebase. Frontend: SermonHarvest type (domain/sermon.ts), harvestSermon() wrapper (lib/commands.ts), and a new 'Sermon Harvest' panel in LiveChurchBrain.tsx - a single button (disabled when no sermon is active) displaying the bundle in five collapsible sections (Sections/Elements/Scripture/Transcript/Timeline).",
        "noNewInfrastructure": "No new database migration, no new persistence, no new detection engine, no new dependency.",
        "regressionProof": "Backend: cargo fmt --check clean, cargo clippy --all-targets -- -D warnings clean (default and --features whisper), cargo test -p cip-desktop 251/251 passed (up from 248 - 3 new harvest tests). Frontend: tsc -b 0 errors, oxlint 0 errors (same 4 pre-existing warnings), vitest 210/210 passed (eventNames.test.ts count guard updated from 53 to 54 for the new SERMON_HARVESTED event), vite build clean.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe confirms: the literal command name 'harvest_sermon' in the invoke handler's registered command-name table; the mangled function symbol for cip_desktop_lib::commands::harvest_sermon; drop-glue and Serialize-impl symbols for the new SermonHarvest struct; and the literal event string 'SERMON_HARVESTED' - direct proof this phase's new code is genuinely compiled into this exact binary. See pilot-evidence/3.9/.",
        "seeAlso": "docs/phase-3-9-sermon-harvest.md, pilot-evidence/3.9/"
    },
    "multiScreenProof": {
        "trigger": "After Phase 3.9 (Sermon Harvest) closed, the user explicitly asked to continue with the remaining four pillars identified from the project's master architecture document: multi-screen presentation output, semantic/paraphrase Bible detection, real audio fingerprinting, and multi-language support. This phase delivers multi-screen presentation output, the second of the five pillars overall.",
        "audit": "presentation_display.rs managed exactly one hardcoded Tauri WebviewWindow (label \"display\") before this phase. presentation.rs's Prepared->Active->Stopped lifecycle and its 'at most one Active item per service' invariant (spec section 10) has zero coupling to window count - confirmed by direct code inspection. Key finding: multi-screen means the one active item reaching more physical screens, not more concurrent active items. Tauri's own Emitter::emit already broadcasts PresentationStarted/PresentationStopped to every listening webview with no target filter - confirmed via the vendored Tauri crate's Emitter trait - so zero changes to event emission were needed.",
        "design": "Three fixed display roles (Stage/Confidence Monitor/Lobby-Overflow) via a new presentation_display::DisplayScreen enum, not an arbitrary N-window system - bounded to what was actually asked for. Stage keeps the exact pre-3.10 window label ('display') so its behavior (including display_presentation's auto-open) is byte-for-byte unchanged. Confidence Monitor and Lobby/Overflow use new window labels ('display-confidence'/'display-lobby'), opened only on explicit operator request via the existing open_presentation_display command, now screen-parametrized. No change to presentation.rs's domain model or lifecycle.",
        "reconciliationFix": "The window-close reconciliation (stopping the active item when its display disappears) now only fires when the closing screen was the last one still open - via a new any_display_window_open(app) check - so closing one of several simultaneously open screens never blanks the others still genuinely showing content. For the single-screen case (the pre-3.10 common case), this is identical to before.",
        "noFabrication": "The Confidence Monitor's extra operator-only metadata (auto-detected vs. manual, template name, item status) is rendered purely from fields already present in the existing PresentationDisplayPayload (item.sourceSuggestionId, item.template, item.status) - no new backend query, no fabricated data. Stage and Lobby render identically, mirroring the exact pre-3.10 passive slide view.",
        "filesChanged": "New DisplayScreen enum + 4 unit tests (presentation_display.rs). New PresentationError::UnknownDisplayScreen variant (presentation.rs). open_presentation_display/close_presentation_display now take a screen argument; get_presentation_display_state returns per-screen state; close reconciliation only fires on the last open screen + 1 new unit test (commands.rs). capabilities/display.json extended to all three window labels, same core:default-only grant. Frontend: PresentationScreen type + PresentationScreenState (domain/presentation.ts), screen-parametrized command wrappers (commands.ts), window-label-to-role resolution (main.tsx), role-aware rendering (PresentationDisplay.tsx), three independent screen controls replacing the single Open/Close Display button (PresentationCard.tsx), both operator surfaces updated identically (LiveChurchBrain.tsx, ServiceReplay.tsx).",
        "regressionProof": "Backend: cargo fmt --check clean, cargo clippy --all-targets -- -D warnings clean (default and --features whisper), cargo test -p cip-desktop 256/256 passed (up from 251 - 5 new tests). Frontend: tsc -b 0 errors, oxlint 0 errors (same 4 pre-existing warnings), vitest 210/210 passed, vite build clean.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe confirms: the new window label strings 'display-confidence'/'display-lobby'; the new operator/window-title labels 'Confidence Monitor'/'Lobby / Overflow'/'CIP Confidence Monitor'/'CIP Overflow Display'; the existing command names 'open_presentation_display'/'close_presentation_display' still present (now screen-parametrized). Prior phases confirmed unaffected: harvest_sermon (2 occurrences), SERMON_HARVESTED event string, whisper_full_with_state (96 occurrences, unchanged). See pilot-evidence/3.10/.",
        "seeAlso": "docs/phase-3-10-multi-screen-audit.md, docs/phase-3-10-multi-screen.md, pilot-evidence/3.10/"
    },
    "displayRegistryProof": {
        "trigger": "Following Phase 3.10.1's pure architecture audit (performed at the user's explicit request, given how much Windows-specific debugging the presentation window pipeline had already been through), the user gave a fully detailed Phase 3.10.2 specification: give each physical monitor a stable session identity, let the operator assign it a role, persist the assignment, and place the presentation window on the assigned monitor - with an explicit instruction not to touch the proven Windows-safe window-creation pipeline itself.",
        "twoDistinctRoleConcepts": "DisplayScreen (Phase 3.10: which content stream/window - Stage/Confidence/Lobby) and the new DisplayRole (this phase: which role a physical monitor plays - Unassigned/Operator/Projector/Stage/Confidence/Lobby) are kept as two separate enums, connected only by an explicit bridge function display_registry::screen_role (DisplayScreen::Stage maps to DisplayRole::Projector) - chosen to avoid silently conflating window identity with monitor role.",
        "honestMonitorIdentity": "Tauri exposes no real OS-level stable monitor id. compute_monitor_id prefers the OS-reported name, falling back to a position/resolution fingerprint (unnamed@x,y-WxH) - documented as CIP's own best-effort session identity, not a hardware serial number claim.",
        "provenPipelineUntouched": "open_display_window's async-command/build ordering and its #[cfg(target_os = \"windows\")] resize-nudge workaround are structurally unchanged from Phase 3.8.4 - the function only gained an Option<MonitorPlacement> parameter. No assigned role (or a disconnected assigned monitor) reproduces the exact pre-3.10.2 unpositioned 1280x720 window, satisfying the user's explicit 'single-display machines keep working exactly as before' requirement.",
        "dpiCorrectPlacementFix": "Monitor's position/size are physical pixels; WebviewWindowBuilder::position/inner_size expect logical pixels. Placement is converted via Tauri's own PhysicalPosition/PhysicalSize::to_logical rather than a hand-rolled division, avoiding an off-by-scale-factor bug on a non-100%-scaled monitor. The pre-existing Windows resize-nudge workaround, previously hardcoded to 1280x720, now consumes the same logical_size actually used to build the window - fixing a latent defect that would have silently fought new placement sizing on Windows specifically.",
        "oneDeliberateUpsertTable": "display_role_assignments uses INSERT ... ON CONFLICT DO UPDATE, the one deviation from this project's append-only-log convention for every other table, because role assignment has current-value (not one-row-per-event) semantics - documented explicitly in the migration file. Storage is global (not service-scoped), matching the Phase 3.6 saved_scriptures precedent.",
        "noLiteralFullscreen": "This phase positions/sizes the window to the monitor's full bounds but does not call a platform fullscreen API - judged a separate, riskier decision than monitor-fill positioning, documented as a known limitation rather than silently decided.",
        "filesChanged": "New database/migrations/0012_display_role_assignments.sql + migrations.rs registration. New apps/desktop/src-tauri/src/display_registry.rs (DisplayRole, screen_role, compute_monitor_id, enumerate_monitors, Display, merge_displays, MonitorPlacement, resolve_role_position - 11 unit tests). persistence.rs gained assign_display_role/list_display_role_assignments (3 unit tests). presentation_display.rs's open_display_window gained Option<MonitorPlacement> + logical-pixel conversion. commands.rs gained resolve_screen_placement plus the list_displays/assign_display_role commands, wired into open_presentation_display and display_presentation. lib.rs registers the new module and commands. Frontend: domain/presentation.ts gained DisplayRole/Display; lib/commands.ts gained listDisplays/assignDisplayRole; new components/workspace/DisplayRegistryPanel.tsx operator configuration panel, wired into LiveChurchBrain.tsx only (not ServiceReplay.tsx - a live-operator physical-setup concern, not a replay concern).",
        "regressionProof": "Backend: cargo fmt --check clean, cargo clippy --all-targets -- -D warnings clean (default and --features whisper), cargo test -p cip-desktop 273/273 passed (up from 256 - 14 new tests: 11 in display_registry.rs, 3 in persistence.rs). Frontend: tsc -b 0 errors, oxlint 0 errors (same pre-existing warning pattern, now also on DisplayRegistryPanel.tsx), vitest 211/211 passed (1 new domain-contract test), vite build clean.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe confirms: the literal command names 'list_displays' and 'assign_display_role' in the invoke handler's registered command-name table, plus their mangled function symbols (cip_desktop_lib::commands::list_displays, cip_desktop_lib::commands::assign_display_role, cip_desktop_lib::persistence::assign_display_role); the concatenated DisplayRole serde discriminants 'unassignedoperatorprojectorstageconfidencelobby'; the literal SQL text 'INSERT INTO display_role_assignments (monitor_id, role, updated_at)' and 'ON CONFLICT(monitor_id) DO UPDATE SET role = excluded.role, updated_at = excluded.updated_at'; the Display struct's camelCase field list 'monitorIdnamexywidthheightscaleFactorisPrimaryassignedRoleconnected'; and the migration file's own comment text ('0012_display_role_assignments', 'monitor_id is not a real OS-issued identifier'). Direct proof this phase's new code is genuinely compiled into this exact binary. Prior phases confirmed unaffected: display-confidence/display-lobby window labels (2 occurrences), harvest_sermon (3 occurrences), SERMON_HARVESTED event string present, open_presentation_display present, whisper_full_with_state (96 occurrences, unchanged).",
        "seeAlso": "docs/phase-3-10-1-audit.md, docs/phase-3-10-2-display-registry.md, pilot-evidence/3.10.2/"
    },
    "presentationRouterProof": {
        "trigger": "The user asked to continue with Phase 3.10.3 (Presentation Router), their own prior one-line description: 'independently route content to different roles'. Because that phrase is genuinely ambiguous between two very different designs, and because it touches the same presentation pipeline the user has repeatedly asked to be careful with, this phase opened with a scoping question rather than an assumption.",
        "scopingQuestion": "Asked the user to choose between (1) a per-screen Live/Held toggle - no domain model change, a screen can be independently taken off the live broadcast and freezes on its last content - and (2) true independent content per screen, which would require breaking presentation.rs's extensively-tested at-most-one-Active-item invariant. The user chose option 1.",
        "designSummary": "PresentationStarted/PresentationStopped previously broadcast to every open screen unconditionally (tauri::Emitter::emit, no target filter - confirmed in the 3.10.1 audit). This phase replaces that at exactly the two emission call sites (display_presentation, clear_active_presentation) with a new broadcast_to_live_screens helper, which computes the open+Live screens (a new pure, unit-tested screens_to_broadcast function) and delivers via a new per-window emit_to. A screen missing from the route-mode map defaults to Live, so with nothing ever set this behaves identically to the pre-3.10.3 broadcast.",
        "catchUpMechanism": "Switching a screen from Held back to Live emits a new payload-less PresentationScreenSynced event targeted at just that window. The frontend re-runs the exact same hydration pull its own mount effect already uses (getPresentationDisplayState) - no second content-delivery mechanism invented.",
        "domainModelUnaffected": "presentation.rs's Prepared->Active->Stopped lifecycle and its at-most-one-Active-item invariant are completely unchanged - confirmed by direct inspection: zero lines of presentation.rs were touched this phase. presentation_display.rs's window-creation pipeline is also unchanged (DisplayScreen gained a Hash derive for use as a HashMap key, no behavioral change).",
        "filesChanged": "New apps/desktop/src-tauri/src/presentation_router.rs (RouteMode enum + screens_to_broadcast, 7 unit tests). state.rs gained screen_route_modes: Mutex<HashMap<DisplayScreen, RouteMode>>. events.rs gained AppEvent::PresentationScreenSynced + a new emit_to helper. commands.rs gained broadcast_to_live_screens (used at both presentation-event emission sites), PresentationScreenState.route_mode, and the set_screen_route_mode command. Frontend: RouteMode type, onPresentationScreenSynced listener, setScreenRouteMode wrapper, PresentationDisplay's hydration pull extracted into a reusable hydrate() function, a new Live/Held toggle button in PresentationCard, wired identically in both LiveChurchBrain.tsx and ServiceReplay.tsx.",
        "regressionProof": "Backend: cargo fmt --check clean, cargo clippy --all-targets -- -D warnings clean (default and --features whisper), cargo test -p cip-desktop 277/277 passed (up from 270 at the Phase 3.10.2 commit, verified via a direct #[test] count comparison against git HEAD - 7 new tests, all in presentation_router.rs). Frontend: tsc -b 0 errors, oxlint 0 errors (same pre-existing warnings), vitest 211/211 passed (event-count guard updated 54->55, PresentationDisplayState contract test extended for routeMode), vite build clean.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe confirms: the literal command name 'set_screen_route_mode' in the invoke handler's registered command-name table; the literal event string 'PRESENTATION_SCREEN_SYNCED'; the mangled function symbols for presentation_router::screens_to_broadcast, presentation_router::RouteMode::parse, commands::set_screen_route_mode, and commands::broadcast_to_live_screens; and a drop-glue symbol for Mutex<HashMap<DisplayScreen, RouteMode>> - direct proof the new AppState field's exact type is genuinely compiled into this binary. Prior phases confirmed unaffected: display-confidence/display-lobby labels (3 occurrences), list_displays (2), assign_display_role (3), display_role_assignments SQL text (5), harvest_sermon (2), SERMON_HARVESTED event string present, whisper_full_with_state (96 occurrences, unchanged).",
        "seeAlso": "docs/phase-3-10-3-presentation-router.md, pilot-evidence/3.10.3/"
    },
    "multiWindowLifecycleProof": {
        "trigger": "Every prior 3.10.x phase's own known-limitations section named this phase explicitly: 'No live monitor hot-plug detection... Live disconnect/reconnect handling is Phase 3.10.4's scope.' This closes the fourth and final sub-phase of the multi-screen pillar the user proposed at 3.10.1.",
        "scopeDecision": "Tauri exposes no monitor hot-plug push event found in the vendored crate, and this container has one display and no tauri::test harness - so speculative real-time OS-level hot-plug handling was deliberately not attempted (unverifiable in this environment, would violate this project's audit-first/evidence-based discipline). Instead this phase closes the one genuine gap this container's own architecture makes visible: open_display_window's existing-window branch only ever called show()/set_focus(), never reapplied a freshly-resolved placement to a window that was already open.",
        "fix": "open_display_window's existing-window branch now reapplies the freshly-resolved placement (set_position + set_size, same DPI-correct logical-pixel conversion as window creation) before showing/focusing - only when placement is Some. A None placement leaves an already-open window exactly where it is, preserving an operator's manual drag on a machine with no Display Registry assignment - the same boundary 3.10.2 established for window creation, now extended to repositioning.",
        "noNewCommandOrEvent": "Reuses the existing open_presentation_display command (now placement-aware for existing windows) rather than adding a parallel reposition command - commands.rs required zero changes since it already resolves placement fresh on every call. A new Reposition button on each open screen's row (PresentationCard.tsx) calls the same existing onOpenScreen handler.",
        "regressionProof": "Backend: cargo fmt --check clean, cargo clippy --all-targets -- -D warnings clean (default and --features whisper), cargo test -p cip-desktop 277/277 passed (unchanged count - window-lifecycle glue is not independently unit-testable per this project's established convention). Frontend: tsc -b 0 errors, oxlint 0 errors (same pre-existing warnings), vitest 211/211 passed (unchanged), vite build clean.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe confirms the build commit 'a88679ba' (the real Phase 3.10.3 baseline) and the mangled function symbols for presentation_display::open_display_window and presentation_display::close_display_window are present with hashes distinct from prior builds (the function body changed, so its symbol hash changed too) - direct proof the reposition-on-reopen logic is genuinely compiled into this exact binary. Prior phases confirmed unaffected: set_screen_route_mode command name present, PRESENTATION_SCREEN_SYNCED event string present, list_displays (2 occurrences), whisper_full_with_state (96 occurrences, unchanged), harvest_sermon (2 occurrences).",
        "seeAlso": "docs/phase-3-10-4-multi-window-lifecycle.md, pilot-evidence/3.10.4/"
    },
    "provenance": {
        "builtDuring": "Phase 3.8.6 (Windows Whisper Build & Packaging Audit) - the operator's Phase 3.8.5 real-Windows re-test confirmed audio capture now genuinely works (SIGNAL CAPTURED, correct listening lifecycle) but isolated the remaining blocker to transcription itself ('SPEECH ERROR'). The audit (docs/phase-3-8-6-audit.md) confirmed the root cause the operator suspected: no Windows build in this project's history has ever been compiled with the `whisper` Cargo feature, so no prior installer could ever transcribe regardless of hardware. Compiling with --features whisper for this exact cross-compilation target (x86_64-pc-windows-gnu, from Linux) hit two independent, real, verified toolchain/upstream defects that had to be fixed before a working build was even possible: (1) whisper.cpp's vendored `ggml` CMake build unconditionally strips the 'lib' prefix from its own static library output for any Windows target (assuming MSVC naming), but the actual linker here is MinGW's GNU ld, which requires the Unix 'libX.a' convention for a plain -lX flag - so ggml.a/ggml-base.a/ggml-cpu.a were built but never found; (2) this container's default x86_64-w64-mingw32-gcc/g++ alternative is the win32-threading-model variant, but whisper.cpp/ggml link against pthread and expect POSIX threading, producing 'undefined reference to __mingwthr_key_dtor' at final link - fixed by switching to the already-installed posix-threads alternative via update-alternatives. Neither fix touches the vendored whisper-rs-sys/ggml/whisper.cpp source; see scripts/build-windows-whisper.sh for the exact, repeatable two-pass build process and docs/phase-3-8-6-windows-whisper-build.md for full root-cause evidence. Separately, the audit also found and fixed a real, previously-undocumented defect that would have blocked correct transcription even with the feature/model both present: `integrations/audio` delivers AudioChunks at the capture device's own native sample rate and never resamples (by design - see its module docs), but WhisperSpeechEngine hardcoded a 16kHz assumption and the one consumer that needed to convert (handle_audio_chunk) never did - so a real Windows device negotiating 44.1kHz/48kHz (the norm for consumer WASAPI devices like Stereo Mix/Microphone Array) would have fed audio to Whisper at roughly 3x the wrong rate. Fixed by adding SpeechEngine::required_sample_rate_hz() (a new default trait method, Some(16_000) only for WhisperSpeechEngine) and a linear-interpolation resampler at the one call site that needs it. Also fixed: the real underlying model-load/inference error text (already computed by WhisperSpeechEngine and SpeechEngineError, but discarded after logging) is now retained in AppState and surfaced through the existing get_pilot_diagnostics command/System Diagnostics panel (feature compiled, model loaded, chunks received, last chunk sample rate, inferences attempted/succeeded, real last-error text) instead of the generic 'SPEECH ERROR' notice alone.",
        "buildEnvironment": "cross-compiled from Linux using rustup target x86_64-pc-windows-gnu, gcc-mingw-w64 (posix-threads alternative - see whisperFeatureProof above), cmake, and nsis (makensis) via `npx tauri build --target x86_64-pc-windows-gnu --features whisper` after the two build.rs-external fixes described above; see scripts/build-windows-whisper.sh for the exact repeatable process",
        "sourceCodeChangedToProduceThisArtifact": true,
        "note": "This artifact supersedes the Phase 3.8.5 installer (previously SHA-256 70ea056359a1dbc738b9b84bd7f2e8bbe6d6a0088bb858aeac47cddaad04b813) because this phase modified core/ai/src/speech_engine.rs (new default SpeechEngine::required_sample_rate_hz() method), ai/speech/src/whisper.rs (WhisperSpeechEngine overrides it to Some(16_000)), apps/desktop/src-tauri/src/commands.rs (resample_pcm16() pure function + wired into handle_audio_chunk before feed_audio; speech/chunk/inference diagnostics counters; PilotDiagnostics gains a `speech` field), apps/desktop/src-tauri/src/state.rs (new SpeechDiagnostics struct + AppState field), apps/desktop/src-tauri/src/lib.rs (create_speech_engine now returns and retains real load-error diagnostics instead of discarding them after logging), and the frontend TS mirrors/PilotDiagnosticsPanel.tsx (renders the new Whisper diagnostics). Most importantly, THIS rebuild was compiled with --features whisper for the first time - see whisperFeatureProof above. Zero new database migrations, zero new Tauri commands (get_pilot_diagnostics keeps its exact name/params; its return type gained one additive field), zero Bible/intelligence engine changes, zero event contract changes, zero capability changes, zero changes to CpalAudioEngine or the AudioEngine trait, no second audio engine, no second speech engine, no second audio meter - see docs/phase-3-8-6-windows-whisper-build.md for the full architectural-safety diff. This is a freshly compiled build, not a copy of a prior artifact.",
        "phase3861Update": "This exact artifact (SHA-256 b737f72b9f870393065777516a58d0a5c5e6e7e9411ccdd3cd8c40578249e177) supersedes the Phase 3.8.6 installer above (previously SHA-256 26739280b7cc4ef750d73a7d04248cf00533b7ff5623720f8ad1621f5e3f9441) for a build-tooling/packaging reason only, not a source-logic reason: the operator installed the Phase 3.8.6 installer on a real Windows machine and it failed to launch at all with 'libstdc++-6.dll was not found' (a real Environment C failure - see pilot-evidence/3.8.6.1/build/runtime-dll-evidence.json for the triggering screenshot description). No Rust or TypeScript application source changed for this rebuild; only apps/desktop/src-tauri/tauri.windows.conf.json (new - bundles libstdc++-6.dll, libgcc_s_seh-1.dll, libwinpthread-1.dll into $INSTDIR) and scripts/build-windows-whisper.sh (extended to stage those three DLLs from the active mingw toolchain before the first cargo build, and to verify their presence in the final packaged installer via 7z extraction) changed. See runtimeDependencyProof above and docs/phase-3-8-6-1-windows-runtime-packaging.md for the full PE-import audit and fix.",
        "phase3871Update": "The operator installed the Phase 3.8.6.1 artifact on real Windows hardware and confirmed it launches correctly (no DLL error, UI/audio all work) but Whisper transcription itself still failed. Phase 3.8.7's diagnostics collection isolated the exact root cause: 'Whisper model: Not found', 'Model loaded: NO', 'Speech engine ready: NO' - the whisper feature and audio pipeline are both genuinely working; there is simply no model file installed. Phase 3.8.7 also found and fixed two real, previously-undiscovered defects while investigating: (1) build.rs's git-commit embed had no rerun-if-changed tracking, so a build with no other apps/desktop/src-tauri file changed (like 3.8.6.1's own rebuild) silently kept a stale commit hash - fixed by watching .git/HEAD and its resolved ref, plus a new build_dirty flag; (2) handle_audio_chunk counted every audio chunk as an 'inference attempted' even when the speech engine was not ready, producing a misleading '60,684 attempted / 0 succeeded' reading and writing a redundant timeline row per chunk for a static, unchanging condition - fixed by checking speech.is_ready() first and short-circuiting with a new chunks_skipped_engine_not_ready counter instead.",
        "phase38711Update": "This exact artifact (SHA-256 ab35b87e2d48760dffed2cb243a84cccc420412755f5ff50a2d5e070103036f7) supersedes the Phase 3.8.6.1 installer above. This container's network egress to the standard Whisper model host (huggingface.co) was re-tested fresh this phase and confirmed still deliberately blocked by organization policy - see modelProvisioningProof and pilot-evidence/3.8.7.1/build/model-provisioning-evidence.json. Rather than guess at an automatic-download implementation this container cannot test or verify a checksum for, this phase adds a real, fully-testable provisioning path: a native 'Select Existing Model File' picker + install_whisper_model command that validates a candidate file by actually loading it as a real Whisper model before copying it atomically into place - the operator's own Windows machine, which almost certainly has ordinary internet access unlike this container, can download the canonical model file directly and install it with one click. See docs/phase-3-8-7-1-audit.md for the full Option A/B/C decision reasoning.",
        "phase3872Update": "This exact artifact (SHA-256 e9fd4d2d28719ca28a6b7269498d0b0f4a542bb12dc2dcde43c28f106032ed7c) supersedes the Phase 3.8.7.1 installer above. The operator installed that artifact, successfully installed a real Whisper model via the new picker, and reported genuine transcription activity - but also app slowness, poor/repetitive transcript text, an empty Intelligence Feed, and intermittent NO SIGNAL. Root-caused, with full file:line citations, to Whisper's real whisper.cpp inference call running synchronously on cpal's own real-time audio capture callback thread (see speechWorkerDecouplingProof above and docs/phase-3-8-7-2-audit.md) - a defect that could only surface once real inference was actually running, which required Phase 3.8.7.1's model-provisioning fix first. Fixed by moving that work onto a dedicated worker thread, mirroring the already-proven acoustic-worker pattern. No resampling, buffering-window, Bible-detection, or event-contract logic changed - purely relocates where existing per-chunk work executes.",
        "phase3873Update": "This exact artifact (SHA-256 78ecdf793b6b64993db703714ea8e46316c0a7d429af13b581f20cdfa06ad3c2) supersedes the Phase 3.8.7.2 installer above. The operator installed that artifact and confirmed real, partial improvement (transcription active, audio arriving, input signal 4-7%) but CIP still became slow over extended listening, transcript remained poor/repetitive, Intelligence Feed stayed empty, and NO SIGNAL still occurred intermittently. The operator's own hypothesis - that the speech queue was unbounded and Whisper could fall behind real-time audio - was re-audited against the actual current code (not assumed) and confirmed exactly correct: start_listening's speech channel was a plain std::sync::mpsc::channel(), genuinely unbounded. Fixed via a measured, wall-clock-duration-tracked backlog with an explicit overload-triggers-drain design (never a naive bounded-channel-drop-newest swap, per the operator's own explicit instruction) - see backpressureProof above and docs/phase-3-8-7-3-audit.md/docs/phase-3-8-7-3-live-speech-stability.md for the full measurement-first reasoning, the three converging secondary defects found and fixed alongside it, and the honest testing-boundary statement for what could and could not be verified without real hardware.",
        "phase3875Update": "This exact artifact (SHA-256 b48b34895f4db49a56a4909ddb8c9a5bef8e1ea58e8976065f1d64e32345c681) supersedes the Phase 3.8.7.3 installer above (Phase 3.8.7.4 was an audit-only phase, no rebuild). The operator's real Windows long-run test of the Phase 3.8.7.3 artifact confirmed the stability fix works (62 real inferences succeeded, no hangs) but showed only Bible activity in the Intelligence Feed. Phase 3.8.7.4's audit traced the actual code and confirmed Sermon/Service Phase/Music-text were complete, tested, but deliberately manual-command-only engines, never called from the live pipeline. This phase connects them via a Live Intelligence Router and, separately, bounds Whisper's raw ~3s transcript windows into 12-20s logical segments before anything downstream sees them - see liveIntelligenceRouterProof above and docs/phase-3-8-7-5-audit.md for the full design reasoning, including why pause-based early-flushing and Cross-Domain/Content/Altar-Call automation were deliberately not attempted this phase.",
        "phase3877Update": "This exact artifact (SHA-256 3335f5101d0dbeb1d83dbeac567b83f9d56c5e836459c104459012a1ba2e9cbd) supersedes the Phase 3.8.7.5 installer above (Phase 3.8.7.6 was an audit-only phase, no rebuild). The operator's real Windows diagnostics for the Phase 3.8.7.5 artifact showed 21/21 successful Whisper inferences yet 'Nothing transcribed yet' and transcript pipeline 'last n/a' - a genuine, real defect, not a hardware limitation: this hardware's avg inference duration (14,991ms) alone exceeds the speech worker's OVERLOAD_THRESHOLD_MS (10s), so every single successful inference was immediately followed by an overload-drain that unconditionally wiped the segmenter's just-produced, valid text before it could ever reach its own 15s flush target. Fixed by only resetting the segmenter on genuine sustained overload (backlog still elevated across consecutive dequeues), not on the first isolated crossing that a single slow-but-successful inference alone explains - see overloadSegmenterFixProof above and docs/phase-3-8-7-7-audit.md for the full trace, arithmetic, and root-cause analysis.",
        "phase3878Update": "This exact artifact (SHA-256 5587f4ec47a80a2062995e35fc48b222fae27f5dd96722d98dea562fa3910634) supersedes the Phase 3.8.7.7 installer above. Operator screenshots comparing Live Service and Service Replay confirmed the downstream Bible/presentation pipeline works correctly on real hardware (Service Replay reached MAT 6:9 and displayed it), but the operator asked for the Needs Attention queue's Bible 'Approve' button to become 'Display', so a live Bible reference reaches the projector in one click instead of a multi-step Approve-then-scroll-to-Prepare-then-Display sequence. Implemented by chaining the three already-existing, already-tested commands (approve_suggestion, prepare_presentation, display_presentation) in one frontend handler - no new backend command, no new event, no new persistence. See bibleDisplayShortcutProof above and docs/phase-3-8-7-8-bible-display-shortcut.md.",
        "phase39Update": "This exact artifact (SHA-256 d72008247cf05bfb92681912b32bd5fe730ae1e8297436bac654121e089e2199) supersedes the Phase 3.8.7.8 installer above. The user shared the project's original master architecture document plus example videos and asked to continue building out the remaining vision to the end, explicitly authorizing all five identified gaps (multi-screen presentation output, semantic/paraphrase Bible detection, Sermon Harvest, real audio fingerprinting, multi-language support) while confirming Sermon Intelligence stays strict/evidence-only rather than adopting the document's LLM-based generative framing. Sermon Harvest is delivered first of the five, sequenced for being the smallest genuinely justified slice: a pure aggregation of sections/elements/scripture/transcript/timeline this application already captures through five separately-tested pipelines, with zero new detection logic and zero new AI. The one real scope boundary (IntelligenceFinding.sermon_id is in-memory only, so Harvest only ever covers the currently active sermon in the same app session) is surfaced honestly via a clean active-sermon guard rather than hidden behind a silently-incomplete response. See sermonHarvestProof above and docs/phase-3-9-sermon-harvest.md.",
        "phase310Update": "This exact artifact (SHA-256 50c47988ac3d06342c8d5c6c0e63e1dd7bead34669239cacbf283714375bda9b) supersedes the Phase 3.9 installer above. The user explicitly asked to continue with the remaining four pillars after Phase 3.9 closed. Multi-screen presentation output is delivered second: the single local display window is generalized into three fixed roles (Stage/Confidence Monitor/Lobby-Overflow), all mirroring the one active presentation item a service can ever have - the domain lifecycle's 'at most one Active item' invariant is completely unchanged, and Tauri's own event broadcast already reached every open window with zero plumbing changes needed. The Confidence Monitor's extra operator-only metadata is rendered purely from fields the display payload already carried - no fabricated data. See multiScreenProof above and docs/phase-3-10-multi-screen.md.",
        "phase3102Update": "This exact artifact (SHA-256 5ebc3109f7c0519be9a3e18648fcff562515d5cebf99f3064df4ba90f1483a8b) supersedes the Phase 3.10 installer above (Phase 3.10.1 was a pure architecture audit, no rebuild). The user, before authorizing any further multi-screen code changes, first asked for a careful audit of the presentation window/monitor pipeline given how much Windows-specific debugging it had already been through (Phase 3.10.1), then specified Phase 3.10.2 in full: give each monitor a stable session identity, let the operator assign it a role (Unassigned/Operator/Projector/Stage/Confidence/Lobby), persist the assignment, and place the presentation window directly on the assigned monitor - explicitly instructing that the proven Windows-safe window-creation pipeline itself must not be touched. Delivered exactly that: a new display_registry module bridges the existing DisplayScreen (content stream) and new DisplayRole (monitor role) concepts, DPI-correct placement via Tauri's own to_logical conversion, and a role-assignment operator panel - with the existing async-command/build-ordering/resize-workaround structure in open_display_window left structurally unchanged, and a None placement (no role assigned, or the assigned monitor disconnected) reproducing the exact pre-3.10.2 behavior. See displayRegistryProof above and docs/phase-3-10-2-display-registry.md.",
        "phase3103Update": "This exact artifact (SHA-256 092306eeef6a9ba9b74123ed1998a3ec39efff6fb56de8a00ec8e33da98cf447) supersedes the Phase 3.10.2 installer above. The user asked to continue with Phase 3.10.3 (Presentation Router), described in their own prior one line as 'independently route content to different roles' - a phrase genuinely ambiguous between a per-screen Live/Held toggle and true independent concurrent content per screen (which would break presentation.rs's at-most-one-Active-item invariant). Rather than assume, this phase asked the user to choose first; they chose the Live/Held toggle. Delivered a delivery-layer-only routing mechanism: PresentationStarted/PresentationStopped now go only to screens currently Live (a new presentation_router module decides this, pure and unit-tested), via a new per-window emit_to instead of the prior unconditional broadcast; a Held screen freezes rather than blanking; switching back to Live re-syncs via the exact same hydration pull a freshly opened window already used. Zero change to presentation.rs's domain model or presentation_display.rs's window-creation pipeline. See presentationRouterProof above and docs/phase-3-10-3-presentation-router.md.",
        "phase3104Update": "This exact artifact (SHA-256 14a44626058aeddecd0830f280f6c585a02000baf917af9cbd96fecf6d89bec0) supersedes the Phase 3.10.3 installer above. Every prior 3.10.x phase's own known-limitations section named Phase 3.10.4 explicitly for one gap: live monitor disconnect/reconnect handling. Tauri exposes no monitor hot-plug push event this session could find, and this container has one display and no tauri::test harness, so speculative real-time OS-level hot-plug handling was deliberately not attempted (unverifiable here, would violate this project's audit-first discipline). Instead this phase closed the one genuine, verifiable gap: open_display_window's existing-window branch only ever called show()/set_focus(), never reapplied a freshly-resolved monitor placement to a window that was already open - so reconnecting a monitor or reassigning a role while a screen was already displayed had no way to move it short of closing and reopening. Fixed by making that branch placement-aware (set_position/set_size when a connected, assigned monitor resolves; left untouched when nothing resolves, preserving manual placement). A new Reposition button lets the operator trigger this explicitly. This closes the fourth and final sub-phase of the multi-screen pillar (3.10-3.10.4). See multiWindowLifecycleProof above and docs/phase-3-10-4-multi-window-lifecycle.md.",
        "phase41Update": "This exact artifact (SHA-256 4066768fd59a3d39e2912c04aa8bae4c7bbb4fa7b27807e2bb5a998201f2f2d8) supersedes the Phase 3.10.4 installer above. Phase 4.1 (Paraphrase Bible Detection) is the user's chosen first item from the Phase 4 gap audit: lexical/keyword-overlap detection of a verse paraphrased without a citation (the master plan's own Section 8.4 example), always a Pending suggestion requiring operator approval, never auto-projected. Honestly scoped as lexical/keyword-overlap matching, not semantic/neural embeddings - no embedding model is reachable in this network-restricted container; the harder 'conceptual references' tier remains a documented, not-yet-started gap. Wiring this into the live pipeline surfaced and fixed a real cross-category suggestion-dedup bug (see paraphraseDetectionProof.dedupFix above). See paraphraseDetectionProof above and docs/phase-4-1-semantic-paraphrase-bible-detection.md.",
        "phase42Update": "Phase 4.2 (Live Bible Detection Performance & Search Filter): fixed a build-configuration gap (Whisper inference compiled without AVX2/FMA/F16C for every prior Windows build) via a new CMake toolchain file, which required also fixing a second, previously-undiscovered defect the toolchain file's own presence introduced (suppressed cmake-rs's cross-compile auto-detection). Added a Bible-search book-name prefix filter. See liveBibleDetectionPerformanceProof and docs/phase-4-2-live-bible-detection-performance.md.",
        "phase43Update": "Phase 4.3 (Instant Bible Detection): found and fixed a second, larger latency source (12-20s batching before Bible detection ran at all) beyond Phase 4.2's SIMD inference fix, via a new fast detection lane decoupled from the batch. No cloud/subscription service considered, per the operator's explicit free-local-model constraint. See instantBibleDetectionProof and docs/phase-4-3-instant-bible-detection.md.",
        "phase44Update": "This exact artifact (SHA-256 46a78173c32990b161836f360e18c28ed92d00b2d5cdd859b68ba69b97d431bb) supersedes the Phase 4.3 installer above. Phase 4.4 (True Semantic Bible Search) closes the 'conceptual/semantic Scripture matching' gap the master plan audit graded NOT STARTED and Phase 4.1 explicitly left unaddressed: a local embedding backend (candle + all-MiniLM-L6-v2, behind the semantic-search Cargo feature) is now wired into the live pipeline as a further fallback after explicit citation and lexical paraphrase both fail. No cloud embedding API or subscription was evaluated or added, per the operator's restated free-local-model constraint. This is the first Windows build in this project's history compiled with --features whisper,semantic-search together. See semanticBibleSearchProof above and docs/phase-4-4-semantic-bible-search.md.",
        "phase51Update": "This exact artifact (SHA-256 356e69df388e80b1a3de2e86b6b8344f8ce18a68f7c5575ccfc63aca54038b8b) supersedes the Phase 4.4 installer above. Phase 5.1 (Post-Service Observability Report), the first slice of the operator's own 'Reliability & Trust' Phase 5 theme, adds a read-only aggregation of data each service already produces (suggestion outcomes, detection-kind counts, timeline/error counts) plus a labeled, honestly process-lifetime-scoped speech/embedding diagnostics snapshot - zero new detection logic, zero new AI, zero new persisted state, mirroring Sermon Harvest's (Phase 3.9) own pure-aggregation discipline exactly. No existing command's behavior changed. See serviceReportProof above and docs/phase-5-1-post-service-observability-report.md.",
        "phase52Update": "This exact artifact (SHA-256 b06ba51af70681e66a3e6c9aa978acaf2a0fbc290779298f1dd650c3178d0eec) supersedes the Phase 5.1 installer above. Phase 5.2 (Temporal Confirmation / Sliding Re-Score), the second slice of 'Reliability & Trust,' bumps a still-Pending Paraphrase/Semantic suggestion's confidence and confirmation count when its reference is independently redetected within the existing 60-second dedup window - previously silently discarded as pure noise. The score rise is capped below what an explicit citation earns, only Pending suggestions are ever touched, and no second suggestion is ever created (dedup's own suppression is unchanged). Also fixes a real, previously-latent dedup bug found during the audit: ReferenceKind::Semantic had no DetectionCategory of its own, so a repeated semantic guess could never dedup against itself. See temporalConfirmationProof above and docs/phase-5-2-temporal-confirmation.md.",
        "phase53Update": "This exact artifact (SHA-256 fb68799ed4dd64d2d432f28046226d379f671948d503eb81039b69543ca620df) supersedes the Phase 5.2 installer above. Phase 5.3 (Audio Pipeline Hardening: Voice Activity Detection), the third slice of 'Reliability & Trust,' adds RMS-energy-based silence gating to WhisperSpeechEngine's inference path: a fully-buffered ~3s window whose RMS falls below a conservative, documented-as-not-empirically-calibrated threshold is skipped without ever running whisper.cpp's real inference, avoiding wasted inference cost and Whisper's known hallucination-on-silence failure mode. Scoped to RMS thresholding alone, chosen as the smallest honest slice of the operator's own roadmap item 'audio pipeline hardening (noise suppression/AEC/VAD)' - noise suppression would need a new native dependency with no way to validate real audio quality in this container, and AEC is inherently a real-hardware-only concern; both remain explicitly unaddressed. The gate is placed after existing timestamp bookkeeping and before whisper.cpp is ever called, so no downstream segment's start_ms/end_ms is ever distorted. A new silent_windows_skipped diagnostics counter is surfaced in the Pilot Diagnostics panel. See audioVadProof above and docs/phase-5-3-audio-vad.md.",
        "phase54Update": "This exact artifact (SHA-256 19f62376c71692633629a47d60eb10ca484f213b1a11fab8c989d4b6e553ea06) supersedes the Phase 5.3 installer above. Phase 5.4 (Wrong-Verse Feedback Loop), the fourth slice of 'Reliability & Trust,' closes a real gap the audit found: reject_suggestion had zero downstream effect anywhere - a same-category (Paraphrase/Semantic) repeat of an already-rejected reference within the existing 60s dedup window was silently dropped with no trace at all, since Phase 5.2's confirm_suggestion path only ever looks up still-Pending suggestions. Per the operator's own explicit choice between silently re-echoing vs. re-surfacing a dampened suggestion, this phase implements the former: the repeat is still silently suppressed exactly as before (a decided suggestion is never resurrected), but a new rejection_echo_count column on the rejected suggestion's own row now records that it happened, surfaced through the Phase 5.1 Service Report's SuggestionStats and in History's Scripture & Findings list. See wrongVerseFeedbackLoopProof above and docs/phase-5-4-wrong-verse-feedback-loop.md.",
        "phase61Update": "This exact artifact (SHA-256 d964e9c94083c9600a3ef9cce00f9c5b368c3f3bce0d8b52e2bae7da06859197) supersedes the Phase 5.4 installer above. Phase 6.1 opens a new roadmap theme, 'Operator Ergonomics' (Phase 6), with an audit-first slice the operator chose from 8 genuine gaps a broader ergonomics audit found. The chosen gap was also a real correctness bug: Operator Mode's A/R keyboard shortcuts always acted on suggestions[0] (the raw, Bible-only pending-suggestion list) regardless of which mode the operator was in, while Operator Mode's own primary panel - the Needs Attention queue, spanning six domains with its own confidence-based ordering - was a completely different list. A keypress could silently act on a different item than the one visually at the top of the screen. Fixed by making Operator Mode's A/R act on the Needs Attention queue's own top item, dispatched through the exact same handleUnifiedAction every card's own buttons already call; Diagnostics Mode's A/R/E/P behavior against suggestions[0] is byte-identical to before. A new visible legend on the queue's heading now shows exactly what A/R will do. Zero new Tauri commands, zero new events, zero new database schema - a pure frontend dispatch-target and discoverability fix. See keyboardShortcutsProof above and docs/phase-6-1-operator-ergonomics-shortcuts.md.",
        "phase62Update": "This exact artifact (SHA-256 f9a64c54e65234e8ca5fc83bb018f29fa62ede756507e929538d7fad06d15f43) supersedes the Phase 6.1 installer above. Phase 6.2 closes gap #2 from Phase 6's own audit: the Needs Attention queue's one-click Bible 'Display' action had no confirmation before it projects to a real screen (deliberately, per Phase 3.8.7.8's own one-click speed goal) and no undo after. Put directly to the operator - confirm-before, undo-after, or both, with the tradeoff stated explicitly - they chose both. A new small, generic, pure two-click guard (decideConfirmClick, lib/confirmGuard.ts) arms on the first click/keypress and fires the exact same pre-existing three-command chain on a second matching click within 4 seconds; every other unified action (approve/reject/accept/acknowledge/review/dismiss) is completely unaffected and still fires on its first click. A new post-success 'Undo (blank screen)' banner appears for 8 seconds (or until the operator acts otherwise) and calls the exact same clear_presentation_display command the Presentation card's own Stop button has always called - no new 'restore the previous item' capability was invented, since nothing tracks what was showing before. Zero new Tauri commands, zero new events, zero new database schema. See displayConfirmationUndoProof above and docs/phase-6-2-display-confirmation-undo.md.",
        "phase63Update": "This exact artifact (SHA-256 e76d5e566bfe6e853b21b065cd3ca59756a64ff339c039fc30776f00d72c457e) supersedes the Phase 6.2 installer above. Phase 6.3 closes gap #3 from Phase 6's own audit: the Intelligence Feed (the read-only cross-domain activity log) had domain filter chips but no text search. A new pure function, searchIntelligenceFeed (case-insensitive substring match against summary/detailLine, following the filterBooksByPrefix precedent from Phase 4.2), composes with the existing domain filter over the same already-fetched, 50-item-capped array - no new fetch, no new backend surface, the panel stays read-only. Unlike Phase 6.2, this gap had one clear shape rather than a genuine architectural fork, so implementation proceeded directly without an operator design question. See feedSearchProof above and docs/phase-6-3-feed-search.md.",
        "phase64Update": "This exact artifact (SHA-256 2ba6125ce2ce2ab5cce29509556e8cc70b4781fe7f525bf71bf79c7fbd952826) supersedes the Phase 6.3 installer above. Phase 6.4 closes gap #4 from Phase 6's own audit - the first Phase 6.x slice to touch Rust, not just the frontend. get_live_status already computed whether a real audio/speech error occurred (audio.stream_error/state.audio_error/state.speech_error, ORed together to decide the AudioStatusKind/SpeechStatusKind enum) but discarded the actual message. Operator Mode's error notices were two hardcoded generic strings; Diagnostics Mode rendered real text for speech only, even though diagnostics.audio.streamError was already arriving on every poll unrendered. LiveStatus gains audio_error_text/speech_error_text, computed with the exact same condition already deciding the enum via a new pure error_text_if helper (inlined by the optimizer, verified via 3 dedicated unit tests); Operator Mode's notices now interpolate the real text with the generic phrasing kept as a fallback, and Diagnostics Mode gained a matching 'Last error' line for audio. No genuine architectural fork this phase - the text already existed, was already computed by the exact condition already driving the enum, and was already operator-readable plain text - so implementation proceeded directly. See audioSpeechErrorVisibilityProof above and docs/phase-6-4-audio-speech-error-visibility.md.",
        "phase65Update": "This exact artifact (SHA-256 81141b822aac63ede70801a5d4a1df0f9f04d854620a44603cbba83ad91cd666) supersedes the Phase 6.4 installer above. Phase 6.5 closes gap #5 from Phase 6's own audit: the generic error banner had no dismiss affordance at all and cleared only at the start of the next tracked action regardless of outcome, and every setError call site discarded the descriptive busy key already in scope, so two different failures in quick succession produced indistinguishable banners. A new pure helper, humanizeBusyKey (strips a trailing UUID, turns hyphens into spaces, capitalizes), covers all ~40+ withBusy call sites without a lookup table; the banner gained a small dismiss button calling the pre-existing setError(null) - no new clearing mechanism. Like Phase 6.3, this gap had one clear shape rather than a genuine architectural fork, so implementation proceeded directly without an operator design question. This phase touched zero Rust files - the second Phase 6.x slice, after 6.2/6.3, to be frontend-only. See errorBannerDismissContextProof above and docs/phase-6-5-error-banner-dismiss-context.md.",
        "phase66Update": "This exact artifact (SHA-256 65ad448fee8411ec354c35224ecd1568f04b7ad68c452d95e0f116ef74f80341) supersedes the Phase 6.5 installer above. Phase 6.6 closes gap #6 from Phase 6's own audit - the last of the original 8 to be scoped, since 'no onboarding' was never broken down further. Put the two genuinely different things onboarding could mean (a first-launch workflow walkthrough vs. a setup-readiness checklist) to the operator directly, per the same discipline as Phase 6.2's confirm-vs-undo question; they chose both. Adds a dismissible, localStorage-gated OnboardingWalkthrough overlay explaining the Start Service -> Needs Attention -> Approve/Reject -> Display workflow, and a new Operator-Mode-only setup notice (computeSetupGaps) naming outstanding items (Bible dataset, Whisper model) from LiveStatus fields SystemStatusStrip already fetches, with a button that switches to the pre-existing Diagnostics Mode controls that fix them. No new Tauri command, event, or database schema; the setup notice is purely a pointer to existing controls, not a new setup mechanism. This phase touched zero Rust files - the third Phase 6.x slice, after 6.2/6.3, to be frontend-only. See onboardingProof above and docs/phase-6-6-onboarding.md.",
        "phase67Update": "This exact artifact (SHA-256 8c57a9929c8efec8effc47c3749bd22513ba9c2609f7711c954da5604bfb5857) supersedes the Phase 6.6 installer above. Phase 6.7 closes gap #7 from Phase 6's own audit: Diagnostics Mode rendered 11 top-level panels back to back, always fully expanded, with no way to collapse any of them. Put the default-state fork to the operator (all open vs. collapse the least-used ones by default, keeping the two with real actions open), per the same discipline as Phase 6.2/6.6's design questions; they chose collapsing the less-used panels. 9 of 11 panels converted from <section> to <details className=\"live-brain__panel\"> (collapsed by default), matching the precedent two other panels already set; Pending Suggestions and Service Timeline stay untouched, always visible. No new Tauri command, event, or database schema; no panel's content changed, only the outer wrapper and default visibility. This phase touched zero Rust files - the fourth Phase 6.x slice, after 6.2/6.3/6.6, to be frontend-only. See diagnosticsModeDensityProof above and docs/phase-6-7-diagnostics-mode-density.md.",
        "phase68Update": "This exact artifact (SHA-256 949d8a9a71b6d1786f33ad6266abcee9154ad3b3758788f86b67fa47efce5a09) supersedes the Phase 6.7 installer above. Phase 6.8 closes gap #8 from Phase 6's own audit - the last of the original 8. Diagnostics Mode's Pending Suggestions panel already had a full inline Edit flow for Bible references; the Needs Attention queue had no equivalent. Adds 'edit' to UnifiedItemAction and actionsFor('bible') (appended last, not before 'reject', preserving resolveUnifiedShortcutAction's positional A/R keyboard mapping); IntelligenceCard's new inline edit UI and LiveChurchBrain's new edit branch reuse the exact same editingId/editValue state and edit_suggestion command Diagnostics Mode already uses. No genuine architectural fork existed - the target UI was the only reasonable shape, so implementation proceeded directly. No new Tauri command, event, or database schema. This phase touched zero Rust files - the fifth Phase 6.x slice, after 6.2/6.3/6.6/6.7, to be frontend-only. This closes the last of Phase 6's original 8 ergonomics gaps. See unifiedQueueEditSupportProof above and docs/phase-6-8-unified-queue-edit-support.md.",
        "phase71Update": "This exact artifact (SHA-256 39a9fe76d3bc26f3cda4b46d6dc7be68fcdd8518a7d4157246362b8912fa76a5) supersedes the Phase 6.8 installer above. Phase 6 closed all 8 of its own ergonomics gaps; this audits the project's own master-architecture gap list (docs/phase-4-master-plan-gap-audit.md) for what remains, asks the user which of the five remaining pillars Phase 7 should tackle, and - absent a response - proceeds with the audit's own recommended, most contained option: real audio fingerprinting. Replaces LocalAcousticMusicRecognizer's honest Phase 2.2 scaffold (always Unavailable/Error - no fingerprint algorithm existed) with a real, offline spectral landmark (constellation) hashing recognizer (Wang 2003, the published Shazam algorithm). New integrations/music-acoustic/src/fingerprint.rs implements STFT-based peak extraction (rustfft), landmark hashing, and offset-histogram voting; local.rs enrolls real WAV reference recordings (hound) named in an extended manifest and answers recognize() from a genuine FingerprintIndex. core/music's AcousticMusicRecognizer trait and every caller above it are unchanged. Unlike every Phase 6.x slice, this phase changes Rust code compiled into the desktop binary - a genuine rebuild with direct binary proof, not a frontend-only rebuild. No real recorded music exists anywhere in this repository or container - the algorithm is proven on synthetic multi-tone signals only (self-match, cross-song rejection, time-shift invariance, noise tolerance); real-world accuracy remains an open, documented Environment C question. See realAudioFingerprintingProof above and docs/phase-7-1-real-audio-fingerprinting.md.",
        "phase72Update": "This exact artifact (SHA-256 47712c815da2461c39be92876bfd8264549ab3a152edfce232cdd6a77396622b) supersedes the Phase 7.1 installer above. Phase 7.1 named the gap directly: enrolling a reference recording required hand-writing JSON matching a private manifest schema and manually placing a WAV file. This phase closes it by extending the Whisper/embedding-model provisioning pattern (Phase 3.8.7.1/Phase 4.4: native file picker + validate/copy/restart-required) to Phase 7.1's manifest. New list_acoustic_enrollments/enroll_acoustic_reference Tauri commands; Manifest/ManifestSong made pub with Serialize; enroll_one's WAV validation extracted into a shared decode_reference_wav both it and the new pub validate_reference_wav use, so a file accepted by the enrollment command is guaranteed to enroll identically at the next startup; new pub read_manifest_entries/write_manifest_entries for whole-manifest read/replace. LiveChurchBrain.tsx's Music Intelligence panel gains a 'Reference recordings' section (song search via the existing searchMusic command + a native WAV file picker). Like Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/music and every existing recognizer implementation are unchanged; enrollment never mutates the live AppState.acoustic_recognizer - it remains a restart-required change. See enrollmentWorkflowProof above and docs/phase-7-2-real-audio-fingerprinting-enrollment.md.",
        "phase73Update": "This exact artifact (SHA-256 d41215a254f1d39ff39f170ac2b1c94cc09d0e3f9d7ea21a4543a558bfb8e599) supersedes the Phase 7.2 installer above. Phase 7.2's own 'Known limitations' named this gap directly: removing an enrollment was not supported, only add/replace via re-enrolling the same songId. This phase completes the enrollment CRUD lifecycle with a new remove_acoustic_reference Tauri command - reads the manifest, errors honestly if song_id names no entry, writes back the filtered list, then best-effort deletes the entry's audio file (a delete failure is logged, never fatal, since the manifest entry is what actually governs recognition). No new crate-level function - composes Phase 7.2's read_manifest_entries/write_manifest_entries as-is. LiveChurchBrain.tsx's enrolled-recordings list gains a per-entry Remove button. Like Phase 7.1/7.2, this phase changes Rust code compiled into the desktop binary. core/music and every existing recognizer implementation are unchanged; removal never mutates the live AppState.acoustic_recognizer - it remains a restart-required change, exactly like enrollment itself. See removeEnrollmentProof above and docs/phase-7-3-remove-enrollment.md.",
        "phase8Update": "This exact artifact (SHA-256 b3724dc437a3b832bad9764171b1a6cff3d6148f1426e6ee4527a9386293f6a1) supersedes the Phase 7.3 installer above. docs/phase-4-master-plan-gap-audit.md and docs/phase-7-audit.md both graded 'OBS/vMix/livestream integration' NOT STARTED - integrations/obs and integrations/vmix were 6-line placeholders since Phase 1. The user explicitly named Production Integration as the next phase. This phase adds two real, protocol-correct clients (a real obs-websocket v5 client with SHA256 challenge/response auth and SetInputSettings; a real vMix HTTP SetText client) that push CIP's currently-displayed presentation text into an operator-configured text source/title, on their own worker thread, best-effort, never blocking or affecting CIP's own local Stage/Confidence/Lobby display. New dependencies (tungstenite, ureq, sha2, base64) are pure-Rust with no TLS backend. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/presentation and core/music (domain contract crates) are untouched; broadcast_to_live_screens and every existing presentation code path are byte-identical to before this phase. See productionIntegrationProof above and docs/phase-8-production-integration.md.",
        "phase9Update": "This exact artifact (SHA-256 a2076e7d5dd8b69394543ffac3ee04705d7dcf918e02cf7ba7218a49df03e9d7) supersedes the Phase 8 installer above. User-supplied licensing-strategy research (YouVersion Platform vs. API.Bible vs. direct publisher licensing) prompted a review of CIP's existing Bible Translation Registry: LicensingStatus governs whether a dataset may be bulk-imported at all, but nothing governed what CIP may then do with an already-admitted translation - concretely, generate_verse_embeddings sent every verse of a translation's text into a local AI embedding model with zero licensing check, safe only by accident (BSB, the only translation ever embedded, happens to be public domain). This phase adds cip_core_content::UsagePermissions (distribution/offline/projection/API/commercial/AI-processing/LLM-prompt/training, every field defaulting to unknown/None, never permissive) on ContentMetadata/content_registry (migration 0016), and wires the one real enforcement point this phase needs: generate_verse_embeddings now refuses to run unless the resolved translation's registry record explicitly grants ai_processing_allowed (ensure_ai_processing_permitted, fail-closed by design). Also fixes a pre-existing bug in generate_verse_embeddings/get_embedding_capabilities, which hardcoded the KJV dev-fixture translation id instead of resolving the real default (matching Phase 3.7's identical fix at twelve other call sites) - previously silently a no-op against any real production database. BSB's dataset now records real, evidence-backed usage permissions, so the new gate does not block the one real translation this codebase actually embeds. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/bible and core/service (Bible detection) are entirely untouched - licensing lives only in ContentMetadata/content_registry, never in the detection or provider-serving path. See translationRegistryV2Proof above and docs/phase-9-bible-translation-registry-v2.md.",
        "phase10Update": "This exact artifact (SHA-256 c95b256da5685ea9c0843f2c0dee1d2f4136624e3339ea2c82cb082dc267f020) supersedes the Phase 9 installer above. The user's explicit instruction, 'Keep going into church/user roles & permissions next', targets item 5 in docs/phase-4-master-plan-gap-audit.md's own Phase 4 candidate list, graded NOT STARTED: no login, no user table, no role enforcement existed anywhere in the app. This phase adds cip_core_access::Role (Admin/Operator, a closed two-value set), OperatorAccount, and local PIN-based authentication (base64(sha256(salt||pin)), the same algorithm shape Phase 8's OBS auth already uses - a deliberate choice over bcrypt/argon2 since the threat model is a single local desktop machine, not a networked credential), backed by a new operator_accounts table (migration 0017) and a new integrations/access crate. A bootstrap rule lets the very first account ever created skip login and always become Admin; every subsequent account requires a logged-in Admin to create. Session state is in-memory only (AppState.current_operator), never persisted - identical precedent to screen_route_modes. Wires a real, fail-closed access::ensure_admin gate onto seven existing configuration commands (import_bible_dataset, set_content_enabled, set_production_integration_config, generate_verse_embeddings, install_whisper_model, install_embedding_model_file, install_embedding_tokenizer_file) and adds five new commands (list_operator_accounts, create_operator_account, login, logout, get_current_operator). The frontend's new LoginScreen gates the entire app behind a login, mirroring WebRuntimeNotice's existing pattern exactly. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/bible, core/service, and core/presentation (every domain contract crate) are entirely untouched - access control lives only in the new access module and the seven gate call sites, never in the detection, presentation, or intelligence path. See rolesPermissionsProof above and docs/phase-10-church-user-roles-permissions.md.",
        "phase11Update": "This exact artifact (SHA-256 8cdc879881f859f237c781b2ce386b5994162bb7a448584526764f515a0a9e92) supersedes the Phase 10 installer above. The user's own item 11 from a pasted advice list ('a tiny local-network HTTP server broadcasting the current slide/verse to a phone browser... for follow along and save a note'), followed by the explicit instruction 'Keep going into the local congregant companion view next', prompted this phase: a LAN-only, read-only HTTP server (apps/desktop/src-tauri/src/companion.rs, off by default, Admin-gated to enable/disable, joining Phase 10's seven gated commands) that mirrors CIP's Stage display to a congregant's phone browser - no app to install, no cloud, never leaves the LAN. A personal notes textarea on the served page is saved only in the phone's own browser localStorage; the server has no write endpoint at all and CIP never receives, stores, or transmits a congregant's notes. The server core (spawn_server/stop_server) is a real, Tauri-agnostic std::net::TcpListener with a genuine stop mechanism (a shared AtomicBool plus a self-connect to unblock the blocking accept() call) - unlike almost every other Tauri-command-backed feature in this codebase, this let 5 of its 9 new tests bind a real TcpListener on an OS-assigned ephemeral port and make real TcpStream requests against it, not pure-function proxies for an untestable command. Wires companion::update_snapshot into display_presentation/clear_active_presentation at the same two call sites production::push_to_configured_targets already uses, so the companion view tracks Stage in lockstep with any configured OBS/vMix push. Adds three new Tauri commands, zero new events, zero new migrations, no new workspace dependency - the fixed port (49876) and LAN address detection (a dependency-free UdpSocket::connect routing-table trick) need nothing beyond std. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/bible, core/service, and core/presentation (every domain contract crate) are entirely untouched - the companion server only ever reads the same RenderedSlide the display-window path already produces. See congregantCompanionProof above and docs/phase-11-congregant-companion.md.",
        "phase12Update": "This exact artifact (SHA-256 4244ca9d667fe2330cbe79319d43037d436d7078a398d94b2e7c5000b2e50ba7) supersedes the Phase 11 installer above. The user's own item 9 from a pasted advice list ('Multi-language Whisper - currently pinned to en...'), followed by the explicit instruction 'Keep going into multi-language Whisper next', prompted this phase: real, verified transcription-language selection - English (default), Yoruba, Hausa, and Auto-detect - replacing the implicit English-only pin whisper.cpp's own default silently applied (ai/speech/src/whisper.rs never called FullParams::set_language at all before this phase). Verified directly against whisper.cpp's own vendored source (the g_lang 100-language vocabulary table) that Yoruba and Hausa are real, trained languages; Igbo has no entry at all under any code and is deliberately not offered - a hard limitation of the Whisper model architecture, not a missing CIP config option. Live-editable via a new SpeechEngine::set_language trait method (default no-op), applied starting with the next inference window. Every real inference pass now reads back the language whisper.cpp actually used via full_lang_id_from_state + get_lang_str, so TranscriptSegment.language is an honest report instead of the permanently-null field it was before. WhisperContext::is_multilingual() is checked once at model-load time and surfaced honestly via SpeechDiagnostics.model_is_multilingual, so an English-only model never silently claims a language switch worked. Not Admin-gated - a live-workflow choice like translation/audio-device selection. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/bible, core/service, and core/presentation (every domain contract crate) are entirely untouched - language selection lives only in the speech-recognition layer. See multiLanguageWhisperProof above and docs/phase-12-multi-language-whisper.md.",
        "phase13Update": "This exact artifact (SHA-256 0900b7cac5f3ca642d4e2ef2e2de610b2e0983968314ad44772a4f2e0ad57a70) supersedes the Phase 12 installer above. The last remaining item from the user's own pasted advice list ('nothing aggregates it across services yet'), followed by the explicit instruction 'church knowledge base / cross-sermon analytics', prompted this phase. Direct source inspection sharpened the user's own framing to the real, verified gap: IntelligenceFinding (Sermon domain included) was never persisted to any database table - only held in AppState.intelligence_findings, an in-memory FindingQueue constructed once at app startup and never cleared - so genuine cross-sermon, cross-restart analytics was impossible, not merely unaggregated. accept_sermon_finding now persists to a new saved_sermon_findings table (mirroring Phase 2.7.1's saved-content precedent: explicit operator acceptance only, never automatic), and a new read-only sermon_knowledge_base module aggregates theme frequency (most-preached themes with the sermons each was attached to), sermons-by-speaker history, and a recent-findings feed - all spanning every service and surviving a restart. IntelligenceFinding.sermon_id (added Phase 2.6) already attributed each finding to its sermon, so no new correlation logic was needed. Theme grouping uses the same summary.starts_with(...) text-prefix convention already established elsewhere in this codebase (service.rs, sermon_foundation.rs, sermon.rs, pipeline.rs) - exact-label match only, no semantic similarity. Surfaced via a new get_church_knowledge_base command (open to any operator) and a Church Knowledge Base section in HistoryView. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/bible, core/service, core/presentation, and core/music (every other domain contract crate) are entirely untouched. See churchKnowledgeBaseProof above and docs/phase-13-church-knowledge-base.md.",
        "phase14Update": "This exact artifact (SHA-256 f0c52145fa3adfb4ab5a6ed8d38e0ef0a735891b3e85d5b992ebd53ada728b33) supersedes the Phase 13 installer above. This is the first genuine Environment C bug report this project has received: two real Windows pilot screenshots, submitted directly by the operator, showed the Live Transcript panel full of whisper.cpp's own non-speech placeholder captions ('[BLANK_AUDIO]', '(speaking in foreign language)', '[inaudible]', '[LAUGHTER]'), every line at an identical hardcoded 75% confidence, a 6% input level, and zero Bible detections. Direct source inspection (docs/phase-14-audit.md) found two independent, verified defects rather than a single vague 'AI fabricating things' explanation: every confidence badge was a hardcoded 0.75 literal in ai/speech/src/whisper.rs, whose own comment claiming 'no per-token confidence exposed by this API' was itself false - confirmed against the vendored whisper-rs-0.14.4 source that WhisperState::full_n_tokens/full_get_token_prob are real, implemented accessors this code simply never called; and nothing in this codebase recognized whisper.cpp's own well-documented non-speech placeholder captions as not-real-speech before displaying, persisting, and feeding them into Bible/Sermon/Music/Content detection exactly as if the congregation had said them. Phase 5.3's SILENCE_RMS_THRESHOLD was confirmed still working correctly - the reported 6% signal genuinely reached inference, so the gap is entirely downstream of the VAD gate, not in it. run_inference now discards a segment whose entire decoded text is one of a documented, whole-string-only NON_SPEECH_PLACEHOLDERS set (never a partial match, so real words mixed with a bracketed tag are kept in full) and computes a real per-segment confidence by averaging whisper.cpp's own per-token decode probability across the pass. A new, honest 'LOW SIGNAL' UI band (1%-15% input level) was calibrated directly from this real operator's own 6%-produced-garbage evidence. Software gain normalization/AGC was seriously considered and explicitly deferred - WhisperSpeechEngine buffers many small AudioChunk deliveries into one ~3s inference window, so a per-chunk gain stage would apply inconsistently across that window, a real artifact risk this environment has no real audio to verify against. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/bible, core/sermon, core/music, core/service, core/presentation (every domain contract crate) are entirely untouched. See whisperHallucinationFilteringProof above and docs/phase-14-whisper-hallucination-filtering.md.",
        "phase15Update": "This exact artifact (SHA-256 86d453131a0286bafb2ad95a6f221e79ef4830863c76815e902b73f585cacc3e) supersedes the Phase 14 installer above. Triggered by nine real Windows pilot screenshots of the Phase 14 artifact, taken minutes apart while a YouTube sermon played through the laptop's own speakers and was picked up by its own microphone, showing the Live Transcript panel frozen at the exact same six lines despite the video's own captions and playback timer clearly advancing between screenshots - plus a request that CIP detect Scripture even when paraphrased without a book name. Direct code trace (docs/phase-15-audit.md) found this was a real side effect of Phase 14's own correct fix: a placeholder-discarded Whisper window now returns an empty result, so neither Bible detection nor the 12-20s transcript accumulator (which never starts its span clock until it sees non-empty text) ever sees it - during a sustained quiet/unclear stretch the operator saw nothing happen with no signal CIP was still listening. An already-computed 'seconds since last transcript' freshness signal already existed and was already polled, but was only ever shown in the Service Intelligence section, not next to the Live Transcript panel itself where the screenshots show the operator was actually looking - now repeated there when stale. Separately confirmed Bible detection only ever ran on each raw ~3s Whisper window, never the fuller 12-20s accumulated window; a new fuller-context retry (paraphrase/semantic fallbacks only, same thresholds, never citation detection) now runs once an accumulated window closes, but only when its own raw sub-windows found nothing. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/bible, core/sermon, core/music, core/presentation (every domain contract crate other than core/service, touched only to add one new function) are entirely untouched. See transcriptStallAndFullerContextDetectionProof above and docs/phase-15-transcript-stall-and-fuller-context-detection.md.",
        "phase16Update": "This exact artifact (SHA-256 10c2f3b96d24545eac107515ecfb517ccbda6e37a778bf1619e3de4bdc2e616f) supersedes the Phase 15 installer above. Triggered by the operator's own request, made while reviewing real Windows pilot screenshots of Phase 15's own stall indicator and Bible-detection-to-display pipeline both working live on real hardware: 'Can it pause the live transcript for now focus major on detecting scripture, music and the intelligences.' The screenshots themselves showed a long, noisy raw transcript (garbled fragments, frequent stall messages) crowding the panel the operator scrolls past to reach Bible/Sermon/Service/Music detection further down. Direct code trace (docs/phase-16-live-transcript-collapse.md) confirmed Bible, Sermon, Service, and Music detection are driven entirely by the backend speech worker thread (spawn_speech_worker, finalize_bible_only, route_segment_to_live_intelligence_engines) with zero dependency on whether the frontend renders the Live Transcript list - so there is nothing to 'pause' in the pipeline sense without breaking the detection the operator's own wording explicitly wants kept running. The correct, safe interpretation is a pure display toggle: a new Show/Hide button in the Live Transcript panel's own heading collapses the transcript list (replacing it, and Phase 15's own stall message which would otherwise repeat under a hidden list, with a one-line note confirming detection keeps running unchanged), persisted across restarts via localStorage exactly like the existing onboarding-seen preference (apps/desktop/src/lib/onboarding.ts), wrapped in the same try/catch-guarded real-storage-access pattern as OnboardingWalkthrough.tsx. Collapsing the panel never discards already-received transcript segments from component state - they are simply not rendered; expanding again shows the full list unchanged. Unlike every phase since Phase 7.1, this phase changes ZERO Rust code compiled into the desktop binary - it is a pure frontend change (a new apps/desktop/src/lib/transcriptPanel.ts pure function plus its 4 unit tests, and the Show/Hide button wiring in apps/desktop/src/components/LiveChurchBrain.tsx). core/bible, core/sermon, core/music, core/service, core/presentation (every domain contract crate) are entirely untouched, and this rebuild's own direct-binary symbol-regression check confirms every prior-phase Rust symbol/string count, including Phase 15's own fuller-context detection functions, is unchanged. See liveTranscriptShowHideProof above and docs/phase-16-live-transcript-collapse.md.",
        "phase17Update": "This exact artifact (SHA-256 1b33d762285d3162a3f2978b85dc67c28b1e801b31e8ed0ed93b8cc77b36d5ac) supersedes the Phase 16 installer above. Not triggered by a new operator bug report - an autonomous continuation ('Keep going, start Phase 17') that re-audited docs/phase-4-master-plan-gap-audit.md for a still-unstarted item directly relevant to the operator's own most recent real feedback. That audit names one explicitly: 'Analytics - NOT STARTED - measuring detection accuracy/false-positive rate/correction rate empirically... none of that is instrumented' - a gap that connects directly to the operator's own Phase-15-triggering report ('accuracy fair') and to Phase 15's own honest limitation that no one could actually see whether its fixes were working. Direct schema audit (docs/phase-17-detection-accuracy-analytics.md) confirmed ai_suggestions (status/confidence/rejection_echo_count, persisted since Phase 1.3/5.4) and scripture_detections (detection_type, persisted since Phase 1.5) already record everything a real accuracy dashboard needs, correlated via the same transcript_segment_id join key pipeline.rs::persist_detections_and_suggestions already guarantees shared between a detection and the suggestion it produced - no new column, no schema change. A new bible_detection_analytics.rs aggregates cross-service outcome counts (approved/edited/rejected/pending), a confidence-level breakdown, a detection-method breakdown (citation vs paraphrase vs semantic - the categories Phase 15 targeted), and a per-service accuracy trend, exposed via a new read-only get_bible_detection_analytics command and a new Detection Accuracy section in HistoryView. Also confirmed only the Bible domain has complete accept/edit/reject history persisted - Music/Sermon/Content route through IntelligenceFinding, which never durably records a rejected item outside their own accepted-only tables, so this phase is honestly scoped to Bible detection only rather than fabricating a cross-domain metric the data can't support. No detection threshold was changed - this only makes the evidence for a future calibration decision visible. Like every phase since Phase 7.1, this phase changes Rust code compiled into the desktop binary. core/bible, core/sermon, core/music, core/presentation (every domain contract crate other than core/ai/core/service, touched only for read-only aggregation) are entirely untouched. See bibleDetectionAnalyticsProof above and docs/phase-17-detection-accuracy-analytics.md.",
        "phase18Update": "This exact artifact (SHA-256 7d22d91ca80a5cef33a3cf7b0d5c07c964088ef596b2e6abb807a909fdd51e53) supersedes the Phase 17 installer above. Triggered by a real Windows pilot report: an operator selected Stereo Mix (Realtek(R) Audio) - a loopback device that mirrors system playback, not a microphone - to capture a YouTube church-service video's audio, started listening, and the Audio & Speech panel showed NO SIGNAL throughout while System Diagnostics confirmed 22669 audio chunks genuinely received (a live, continuously-running capture stream) but 0/0 inferences (75/75 windows classified as silence). Traced the full capture path end to end before concluding anything (docs/phase-18-audit.md): device selection (matches the exact selected device, never falls back to a global default), stream negotiation (uses that specific device's own default format), downmix (correctly averages every interleaved channel per frame using cpal's own negotiated channel count, not a hardcoded mono assumption), RMS computation (a standard root-mean-square, clamped correctly), and resampling (the diagnostics screenshot's own '480 samples @ 48000 Hz, resampled to 160 samples' is exactly the correct 48000-to-16000 Hz ratio). Whisper's own independent silence gate (ai/speech/src/whisper.rs::is_silence, a second, separately-implemented RMS computation over the resampled 16kHz buffer, in a different crate) also independently agreed every window was silent - two independently-implemented RMS computations agreeing rules out a computation bug in either path and points instead at genuinely near-silent PCM reaching the app, i.e. a real Windows Stereo Mix quirk (its own recording level muted/zero, or system audio not actually routed through the default playback device it mirrors) that no cross-platform audio library can detect or fix. What was addressable: the existing NO SIGNAL/LOW SIGNAL message (Phase 14) assumed a physical microphone ('move it closer, raise its gain'), which is actively wrong advice for a loopback device with no physical position or gain control. describeAudioSignal (apps/desktop/src/lib/format.ts) now takes an optional deviceName parameter (the backend's own resolved AudioEngineStatus.selectedDevice) and, when it matches a known Windows loopback/monitor device name pattern (Stereo Mix and common aliases, case-insensitive), gives guidance that actually applies - the device's own Windows recording level and output routing - instead of microphone-gain advice. Every existing caller/test defaults deviceName to null and keeps its exact original wording. Unlike every phase since Phase 7.1, this phase changes ZERO Rust code compiled into the desktop binary - it is a pure frontend change. core/bible, core/sermon, core/music, core/service, core/presentation (every domain contract crate) are entirely untouched, and this rebuild's own direct-binary symbol-regression check confirms every prior-phase Rust symbol/string count, including Phase 17's own Detection Accuracy Analytics functions, is unchanged. See loopbackAudioSignalGuidanceProof above and docs/phase-18-audit.md.",
        "phase19Update": "This exact artifact (SHA-256 a93b6d49ec133587027eb3422a3d0c54244164625e8930710526dc4529bc881c) supersedes the Phase 18 installer above. Not triggered by a new operator bug report - the operator ran their own independent, unsolicited detection-accuracy audit of the repository (five-stage funnel analysis with file:line citations) and asked whether to start implementing any of its findings. Every cited claim was re-verified directly against the current code before acting (docs/phase-19-audit.md), catching one real factual error in the audit (Sermon/Service/Music intelligence DO run live automatically via route_segment_to_live_intelligence_engines, established since Phase 3.8.7.5 - not 'effectively dead in a service' as claimed) and one footnote-worthy imprecision (bare-verse fragments are matched by an existing BARE_VERSE_PATTERN but resolve to Unresolved and are never persisted without active context, so the audit's practical symptom was accurate even though its literal mechanism description wasn't quite right). Of the audit's own findings, only the one item that was both real and fully safe/testable without real hardware was implemented this phase: general ordinal-word normalization (core/bible/src/normalize.rs) - a new ORDINALS table converts first/second/third and their digit-suffix forms 1st/2nd/3rd to digits ahead of detection, deliberately scoped to 1st-3rd since no canonical Bible book is ever numbered past three. This closes a real, previously-uncovered gap: digit-suffix forms like '1st Corinthians' were never in book_alias.rs's literal per-book alias table and were invisible to the detector before this phase; only the word form ('First Corinthians') worked, and only because someone had hand-written that exact alias. normalize_text runs only on a local copy used for detection (core/service/src/bible_intelligence.rs), never touching the transcript text persisted or shown to the operator, making this a zero-risk, detection-only change. The audit's own top-ranked items (overlapping Whisper windows + VAD-triggered flush, emitting interim transcripts, fuzzy/near-miss book-name matching) were explicitly deferred with reasoning in docs/phase-19-audit.md - the first two touch the live capture/segmentation path this container cannot safely validate without real hardware, and fuzzy matching is a dedicated-phase-sized feature, not a same-turn addition. Like every phase since Phase 7.1 (excepting the pure-frontend Phase 16/18), this phase changes Rust code compiled into the desktop binary - but scoped to exactly one file in one crate (core/bible/src/normalize.rs); core/sermon, core/music, core/service, core/presentation, apps/desktop/src-tauri, and every frontend file are entirely untouched. See ordinalNormalizationProof above and docs/phase-19-audit.md.",
        "phase20Update": "This exact artifact (SHA-256 537b20a4ec421f2876d17b73a6869a541c853cf477b9d6c6dcc390c943b4b872) supersedes the Phase 19 installer above. Triggered by two direct operator requests in one message: 'start Phase 20 with fuzzy book-name matching' (the leading item explicitly deferred from Phase 19's own audit response) plus a real UX complaint with a screenshot of the Correct Active Context panel: 'Bible edit here requires pressing the full name of the book and chapter and verse can you change it along side.' Part 1: a new ReferenceKind::FuzzyBook detection tier - core/bible/src/book_alias.rs::fuzzy_match_book does length-scaled Levenshtein-distance near-miss matching against the single-word canonical book names only (multi-word/numbered books like '1 Corinthians' are deliberately excluded - guessing between numbered variants is a higher-risk problem left to the existing exact alias table); core/bible/src/detection.rs's detect_candidates only trusts a fuzzy match immediately followed by a real chapter:verse shape, the same precision guard the exact pass gets; core/service/src/bible_intelligence.rs::resolve_fuzzy_book re-validates against the real Bible data before ever suggesting anything and, unlike a real citation, never mutates the active Scripture context - a near-miss book name must never become the trusted context a later bare verse would silently inherit. Part 2: apps/desktop/src-tauri/src/commands.rs::correct_scripture_context now resolves the operator's typed book text through canonicalize_book (the same alias table the detector uses) before validating the chapter, fixing a real bug where the manual-entry field silently required the exact undocumented internal database book code (the frontend's own .toUpperCase() turned a typed 'John' into the never-matching 'JOHN' against the stored code 'JHN') - now the full name, a common abbreviation, or the code all work, case-insensitively. Like every phase since Phase 7.1 (excepting the pure-frontend Phase 16/18), this phase changes Rust code compiled into the desktop binary, scoped to core/bible, core/service, core/intelligence's bible_adapter, and one apps/desktop/src-tauri command; core/sermon, core/music, core/presentation are entirely untouched. See fuzzyBookMatchingProof above and docs/phase-20-audit.md.",
        "phase21Update": "This exact artifact (SHA-256 d82c93ef03df79e3b829771fb53076dc39d250af0d6ca39671cd034a9b1de25b) supersedes the Phase 20 installer above. Triggered directly: 'Keep going, start Phase 21 with overlapping Whisper windows + VAD' - the leading item explicitly deferred from Phase 19's response to the operator's own detection-accuracy audit and named again in Phase 20. The problem: WhisperSpeechEngine ran one inference pass every time its buffer reached a fixed ~3s sample-count cap, with no relationship to what was actually being said, so a window boundary could land mid-word purely by chance. The operator's own phrasing named two techniques together; they were evaluated separately and only one was implemented this phase. VAD-triggered flush (implemented): ai/speech/src/whisper.rs gains has_trailing_pause/should_flush_early - a window now flushes to inference as soon as at least 1.5s has buffered and the trailing 0.4s is genuinely quiet (the same is_silence/SILENCE_RMS_THRESHOLD gate Phase 5.3 already established), so most windows now end where the speaker actually paused. The fixed 3s cap is unchanged and checked first, independently - a continuous sentence with no detectable pause still flushes exactly where it always did, so worst-case behavior is identical to the pre-Phase-21 engine. Audio-overlapping windows (deliberately deferred): decoding a shared tail of audio in both an ending window and the next one's start needs some way to reconcile text produced from the same audio decoded twice - real streaming ASR systems solve this with token-timestamp-based stitching, which needs real, timing-sensitive audio to build and validate that this container does not have. Getting it wrong risks duplicated words in the persisted, is_final:true transcript and double-counted Bible/Sermon/Service/Music detections - a strictly worse outcome than the mid-word cuts it would fix - so it was not attempted. A new SpeechEngine::last_feed_was_vad_early_flush() trait method (default false) and a vad_early_flushes diagnostic counter thread through state::SpeechDiagnostics -> commands::SpeechRuntimeDiagnostics -> the frontend, mirroring the Phase 5.3/14 diagnostic-wiring pattern exactly, so a real operator can confirm the mechanism is engaging on real hardware. run_inference itself (the code that talks to whisper.cpp, computes timestamps, and applies the silence/placeholder gates) is untouched - only the decision of when to call it changed, keeping the already-tested inference logic at zero risk. Like every phase since Phase 7.1 (excepting the pure-frontend Phase 16/18), this phase changes Rust code compiled into the desktop binary, scoped to core/ai's SpeechEngine trait and ai/speech's WhisperSpeechEngine, plus the established diagnostics-plumbing pattern in apps/desktop/src-tauri; core/bible, core/sermon, core/music, core/presentation are entirely untouched. See vadTriggeredFlushProof above and docs/phase-21-audit.md.",
        "phase22Update": "This exact artifact (SHA-256 3867af4092cdceba268f742f6bc17b6e11f311055681924c1fc4e28943a3bf56) supersedes the Phase 21 installer above (SHA-256 d82c93ef03df79e3b829771fb53076dc39d250af0d6ca39671cd034a9b1de25b). Triggered directly by operator feedback (with screenshots) naming inaccurate/late/rare (~10%) Bible/sermon detection and a hypothesis that CIP mishandles Nigerian-accented English, plus two follow-up questions this session asked and the operator answered: the desired model strategy (fast base.en/small.en tier, high-quality large-v3-turbo tier, future Yoruba/Igbo/Hausa multilingual support) and the UI direction (broadcast/production dashboard - tracked separately, not part of this artifact). The problem, grounded in code before any fix: WHISPER_MODEL_FILENAME/docs/live-speech.md's own prior wording pointed every operator at ggml-tiny.en.bin, whisper.cpp's smallest, least accurate model (39M parameters) - a real, sufficient explanation for all three symptoms, since detection can only match text Whisper transcribed correctly. A second, previously-unidentified issue: install_whisper_model always renames the operator's selected file to the same fixed destination filename by design, so System Diagnostics' displayed path could never honestly distinguish a large, high-quality installed model from the smallest possible one. This phase adds commands.rs::classify_model_size_tier (a heuristic, never-a-certainty size-based classifier, documented as such) threaded into WhisperModelDiagnostic::Present as a new sizeTierHint field (mirrored in appConfig.ts/PilotDiagnosticsPanel.tsx), a create_speech_engine startup log::warn! when a tiny-class model loads (never a hard block - CIP has never refused to run a model an operator chose to install), and rewrites docs/live-speech.md's model guidance to recommend base.en/small.en as the real-time floor and large-v3-turbo for highest accuracy, naming the real latency-vs-accuracy trade-off since a larger model's inference pass takes longer per VAD-triggered window (Phase 21). The operator's own stated fast+quality dual-tier request is explicitly, honestly scoped out as future work: CIP holds exactly one active WhisperSpeechEngine/WhisperContext for the process's life, and building true concurrent dual-tier inference with output arbitration is real, substantial work this phase does not attempt. The stale Phase 12 'Language support' doc section (still saying zero non-English support after Phase 12 shipped real, whisper.cpp-verified Yoruba/Hausa selection) is corrected in the same pass; Igbo's absence is re-confirmed as a real whisper.cpp vocabulary gap (no Igbo entry in its vendored source at all), not an oversight, and not addressable by any CIP-side configuration. Like the pure-frontend Phase 16/18, this phase's Rust changes are diagnostics-only (commands.rs, lib.rs) - core/bible, core/sermon, core/music, core/presentation, run_inference, VAD-triggered flush, and every detection/matching function are entirely untouched. See modelTierDiagnosticsProof below and docs/phase-22-audit.md.",
        "phase241Update": "Phase 24.1 is a direct continuation of Phase 24's own dual Live/Preview slide panel, closing the one honestly-deferred gap that phase's own audit doc flagged as a known limitation: the queue strip's thumbnails approximated a slide from raw PresentationContent text rather than showing the actual RenderedSlide a Display click would send live. The fix adds one new, thin, read-only Tauri command (get_prepared_item_slide) that reuses this codebase's single existing rendering pipeline (cip_presentation_renderer::render_content) rather than building a second one - consistent with this project's own architectural discipline (see PresentationDisplayPayload's own doc comment: 'no second rendering system in the frontend'). An earlier draft additionally pruned stale queueSlides entries inside the same effect; this was dropped because the synchronous setState call it required is exactly the pattern this project's own lint rule (react(set-state-in-effect)) flags, and the unreachability argument (a stale entry is never read once its item leaves preparedItems) already covers correctness without it. Full regression: Rust fmt/clippy (both configs) clean, cargo test --workspace 365/365 in both configs (unchanged - no new pure logic to test, matching commands::get_presentation_item's own untested-thin-wrapper precedent), frontend typecheck/lint (4 pre-existing warnings, unchanged)/test (303/303, unchanged)/build all clean. See docs/phase-24-1-audit.md for full reasoning and pilot-evidence/24.1/windows/installer-contents-verification.json for the symbol-regression proof (zero deviation across all 22 pre-existing tripwires, one new tripwire confirming get_prepared_item_slide is genuinely compiled in and registered)."
    },
    "releaseCandidateStatement": "This is the Phase 24.3.2 SOFTWARE RELEASE CANDIDATE for Windows x64 (quality-tier backlog visibility status label). Continuing directly from the operator's own follow-up direction after Phase 24.3.1: \"Fix the backlog visibility status label next.\" The gap: SpeechQualityDiagnostics.jobs_dropped_backlog (Phase 24.3) is a cumulative, lifetime counter that only ever grows, giving an operator watching System Diagnostics mid-service no way to tell \"this counter is 12 because of one bad minute early in the service, everything's fine now\" apart from \"this counter is 12 because the quality model has been too slow for this hardware for the entire service and is still dropping jobs right now\" - both produce the identical displayed number. This phase adds consecutive_jobs_dropped (a streak, reset the moment the quality worker actually processes any job) and a derived QualityBacklogState label (normal/busy/falling_behind/overloaded), computed by a new pure classify_quality_backlog function (5 new unit tests, boundary-exact at each threshold plus a recovers-back-to-normal property test) and surfaced in PilotDiagnosticsPanel. Purely additive diagnostics - zero changes to any existing command signature, event payload, or migration; the quality worker's own backpressure behavior (try_send, drop-newest-on-full-channel) is completely unchanged. Full Environment A regression (fmt/clippy/test in both feature configs - 373/373 backend tests, up from 368; frontend typecheck/lint/test/build) passes clean. One symbol-regression deviation this rebuild (retry_paraphrase_or_semantic_with_fuller_context, 2→1), investigated via nm/demangling and confirmed a benign codegen/inlining artifact - the implicated functions' own source is byte-for-byte unchanged since the last verified rebuild. Environment C for the dual-tier pipeline as a whole remains open - this fix improves diagnostics visibility, it does not itself constitute real-hardware confirmation. Prior release candidate history: This is the Phase 24.3.1 SOFTWARE RELEASE CANDIDATE for Windows x64 (quality-tier language-conditioning fix). Following Phase 24.3's true dual-tier Whisper work, the operator was offered several viable next options and picked this one: SpeechEngine::transcribe_once previously always decoded using the quality engine's own separately-configured default language, never the language the fast tier's own real decode of that exact audio actually reported - a real gap on any service using \"auto\" language detection, or simply where the two tiers' configured languages differed. This phase adds a language_hint parameter to transcribe_once, threaded from the fast tier's already-detected TranscriptSegment.language through the existing QualityJob plumbing, so both tiers now condition their decode of the same audio on the same, real, honest per-window evidence - never a guess, and never overriding the quality engine's own default for any other decode. Full Environment A regression (fmt/clippy/test in both feature configs; frontend typecheck/build) passes clean, with zero symbol-regression deviation on this rebuild (an improvement over Phase 24.3's own rebuild, which had one investigated-and-confirmed-benign deviation). Environment C for the dual-tier pipeline as a whole remains open - this fix makes it more correct once confirmed, it does not itself constitute that confirmation. Prior release candidate history: This is the Phase 24.3 SOFTWARE RELEASE CANDIDATE for Windows x64 (true dual-tier Whisper: an optional second, higher-quality speech model that re-transcribes what the fast tier already showed live). The operator picked this as the last of several deferred, latency/accuracy-focused items offered this session: \"True dual-tier Whisper (fast detector model + separate high-quality transcript model running concurrently) - this is literally what you originally asked for as your model strategy, and it's still not built.\" A follow-up question on what should happen to the quality model's output was answered: also re-run Bible detection on the corrected text. Investigation confirmed the actual gap: AppState held exactly one active WhisperSpeechEngine, with no mechanism for a second, concurrently-loaded model. This phase adds one - a genuinely separate WhisperContext, given the fast tier's own already-finalized window audio via two new SpeechEngine trait methods (transcribe_once, take_last_final_window_audio), running on its own thread so a slower model's decode time never blocks the live pipeline. The quality tier's output becomes a new, linked transcript segment - never an in-place edit of the original, matching this codebase's own existing 'never edit a record after the fact' principle - and re-runs Bible detection, with the existing 60-second suggestion-dedup window already handling the overlap with whatever the fast tier found, zero new dedup logic required. Entirely opt-in: an operator who never installs a second model file gets zero behavior change. Full Environment A regression (fmt/clippy/test in both feature configs; frontend typecheck/lint/test/build) passes clean. Environment C (a real operator confirming the quality tier's corrections are useful, on real hardware, at an acceptable resource cost, with real microphone audio) has NOT been performed - see docs/phase-24-3-audit.md and physicalHardwareStatement item 34 below. Prior release candidate history: This is the Phase 24.2 SOFTWARE RELEASE CANDIDATE for Windows x64 (real, bounded interim Whisper transcripts). The operator asked what other work remained; offered \"overlapping/interim transcription windows\" (deferred since Phase 19/21 pending real timing-sensitive audio) among the honestly-deferred items, and - since that label actually covers two separable techniques with very different risk profiles - was asked which to pursue. Chose interim transcripts, the lower-risk half: unlike audio-overlapping windows (which decodes the same audio twice and needs real-timing-dependent reconciliation this environment cannot validate), interim decoding only ever decodes a non-overlapping prefix of one window's audio, always fully superseded by that window's own final decode - nothing to reconcile, nothing that needed real microphone timing to validate safely. Investigation found the entire pipeline (the SpeechEngine trait's is_final contract, the real-time worker's is_final gating, the frontend's interim-discard logic) had already anticipated and correctly supported interim segments since Phase 1.2 - WhisperSpeechEngine itself was the only gap, since it never actually produced one. This phase closes exactly that gap: one real, bounded (at most one extra decode pass per window) interim whisper.cpp decode, never fabricated text, sharing its window's real final segment's id per that field's own documented contract. Real-browser event-simulation verification (Tauri's own official mocking API) confirmed the Live Transcript panel shows the provisional interim line and correctly replaces it with the settled, scored final segment once the window closes. See docs/phase-24-2-audit.md and interimTranscriptProof above for full detail.\n\nPrevious statement, retained for provenance: This is the Phase 24.1 SOFTWARE RELEASE CANDIDATE for Windows x64 (real RenderedSlide rendering for the Live/Preview stage's queue strip). Triggered directly by the operator's own \"Continue there next\" instruction following Phase 24's completion report, which explicitly offered this as the next honestly-deferred item: Phase 24 shipped the dual Live/Preview panel (the Live panel showing the real on-screen slide, the Preview panel showing the operator's last-previewed item) but its queue strip of prepared items could only show a raw-text approximation of each item's content, not the actual word-wrapped RenderedSlide a Display click would send live - because no command anywhere rendered a slide for a merely-prepared (as opposed to active or previewed) item. This phase adds get_prepared_item_slide, a thin read-only Tauri command that reuses the exact same render_content pipeline every other slide on screen is already built from, and wires it into the queue strip via a small per-item fetch-and-cache effect. Zero new events, zero new migrations, zero schema changes - PresentationCard and the real display window are entirely untouched. Real-browser verification (headless Chromium, mocked Tauri bridge) confirmed both Live Service and Service Replay now render real wrapped body text and translation footers in the queue strip. See docs/phase-24-1-audit.md and queueStripRealSlideProof above for full detail.\n\nPrevious statement, retained for provenance: This is the Phase 22 SOFTWARE RELEASE CANDIDATE for Windows x64 (honest, size-based model-tier diagnostics + model-recommendation docs). Triggered directly by operator feedback, with screenshots: 'The interface is shallow, UI/UX needs improvement... Bible detection is not working well, 1. It's not accurate 2. It appears late 3. The rate at which it appears is 10%... I don't think CIP understand the English of a Nigerian Pastor.' Root cause, grounded in code before any fix: this project's own default/documented Whisper model (ggml-tiny.en.bin) is whisper.cpp's smallest, least accurate model - detection can only match text Whisper transcribed correctly, so this is a real, sufficient explanation for all three symptoms, independent of the accent hypothesis specifically. A second issue found while investigating: install_whisper_model always copies the operator's selected file to the same fixed destination filename by design, so System Diagnostics could never honestly say which model tier was actually installed. This phase adds a heuristic, size-based classify_model_size_tier function, a new sizeTierHint field on WhisperModelDiagnostic::Present (Rust + TS, surfaced in both the diagnostics panel and the post-install message), and a startup log warning when a tiny-class model loads. docs/live-speech.md and docs/first-use.md are rewritten to recommend base.en/small.en as the real-time floor and large-v3-turbo for highest accuracy instead of steering every deployment toward tiny.en, with an honest, explicit scoping of what a true concurrent fast+quality dual-tier engine (the operator's own stated request) would require and why it is deferred, not implemented here - CIP holds exactly one active WhisperSpeechEngine for the process's life. The stale Phase 12 'Language support' doc section (still claiming zero non-English support after Phase 12 shipped real Yoruba/Hausa selection) is corrected in the same pass, and Igbo's absence is re-confirmed as a real whisper.cpp vocabulary gap, not an oversight. Test counts: cargo test --workspace clean in both feature configs (cip-ai-speech 27/27 unchanged, cip-desktop 365/365 up from 359 - the 6 new classify_model_size_tier/diagnose_whisper_model tests); frontend typecheck 0 errors, lint the same 5 pre-existing warnings (unchanged), test 303/303 (unchanged - purely additive diagnostic field), build clean. This phase's Rust changes are diagnostics-only (commands.rs, lib.rs) - run_inference, VAD-triggered flush, and every core/bible detection/matching function are entirely untouched, and core/sermon, core/music, core/presentation are entirely untouched. See knownLimitations below for the honest remainder, including everything explicitly deferred - most critically, true concurrent dual-tier inference is not implemented, and this exact artifact has not yet been installed or launched on real Windows hardware.\n\nPrevious release candidate statement, retained for provenance: This was the Phase 21 SOFTWARE RELEASE CANDIDATE for Windows x64 (VAD-triggered flush for Whisper windows). Triggered directly: 'Keep going, start Phase 21 with overlapping Whisper windows + VAD' - the audit's own top-ranked item, explicitly deferred out of Phase 19 and Phase 20 pending a safely-scoped design. WhisperSpeechEngine previously ran inference only when its buffer reached a fixed ~3s sample-count cap, with no relationship to what was being said, so a window boundary could land mid-word purely by chance. The operator's own phrasing named two techniques together; only one was implemented this phase, deliberately. VAD-triggered flush (implemented): a window now flushes as soon as at least 1.5s has buffered and the trailing 0.4s is genuinely quiet, so most windows end where the speaker actually paused - the fixed 3s cap is unchanged and still fires for continuous speech, so worst case is identical to before. Audio-overlapping windows (deliberately deferred): decoding a shared audio tail across two windows needs token-timestamp-based text reconciliation this container has no real, timing-sensitive audio to build or validate - getting it wrong risks duplicated words in the persisted transcript, a strictly worse outcome than the mid-word cuts it would fix. A new vadEarlyFlushes diagnostic counter, wired through the established Phase 5.3/14 pattern, lets a real operator confirm the mechanism engages on real hardware. Test counts: cip-ai-speech 27/27 with --features whisper (up from 19, the 8 new tests), cip-desktop unaffected at 359/359 in both feature configs; frontend 303/303 unchanged. Phase 21 adds zero new Tauri commands, zero new events, zero new migrations. See vadTriggeredFlushProof above and docs/phase-21-audit.md for full reasoning, including everything explicitly deferred.\n\nPrevious release candidate statement, retained for provenance: This was the Phase 20 SOFTWARE RELEASE CANDIDATE for Windows x64 (fuzzy book-name matching + a manual context-correction UX fix). Triggered by two direct operator requests in one message: 'start Phase 20 with fuzzy book-name matching' (the leading item explicitly deferred from Phase 19's own audit response) plus a real UX complaint, with a screenshot, that the Correct Active Context manual-entry field required typing the exact internal database book code rather than the full name the operator actually knows. A new ReferenceKind::FuzzyBook detection tier (core/bible, core/service, core/intelligence) does length-scaled Levenshtein-distance near-miss matching against single-word canonical book names only, re-validates every guess against the real Bible data before ever suggesting anything, and never mutates the active Scripture context - deliberately scoped narrower than the audit's full ask (multi-word/numbered books remain alias-only) with the reasoning documented in docs/phase-20-audit.md. correct_scripture_context (apps/desktop/src-tauri) now resolves the operator's typed book text through the same alias table the live detector uses, so the full name, an abbreviation, or the code all work. Test counts: cip-core-bible 98/98 (up from 85, the 13 new fuzzy-matching tests), cip-core-service/cip-core-intelligence/cip-desktop unaffected apart from new FuzzyBook coverage; frontend 303/303 unchanged. Phase 20 adds zero new Tauri commands, zero new events, zero new migrations. See fuzzyBookMatchingProof above and docs/phase-20-audit.md for full reasoning, including everything explicitly deferred to a future phase.\n\nPrevious release candidate statement, retained for provenance: This was the Phase 19 SOFTWARE RELEASE CANDIDATE for Windows x64 (general ordinal normalization, in response to the operator's own detection-accuracy audit). Not triggered by a new bug report - the operator ran their own unsolicited five-stage funnel audit of the repository explaining why live Scripture detection misses most references, with file:line citations, and asked 'Want me to start implementing any of them?'. Every cited claim was re-verified directly against the current code first (docs/phase-19-audit.md), catching one real factual error (Sermon/Service/Music intelligence DO run live automatically via route_segment_to_live_intelligence_engines, established since Phase 3.8.7.5 - not 'effectively dead in a service' as the audit claimed) and one footnote (bare-verse fragments are matched by an existing pattern but resolve to Unresolved and are never persisted without active context). Of the audit's own findings, only the one genuinely safe and fully verifiable-without-hardware item was implemented: general ordinal-word normalization (core/bible/src/normalize.rs) - first/second/third and digit-suffix forms 1st/2nd/3rd now convert to digits ahead of detection, closing a real gap (digit-suffix forms like '1st Corinthians' were never in book_alias.rs's literal alias table and were invisible to the detector before this phase). The audit's own top-ranked items - overlapping Whisper windows + VAD-triggered flush, emitting interim transcripts, and fuzzy/near-miss book-name matching - were explicitly deferred with reasoning in docs/phase-19-audit.md: the first two touch the live capture/segmentation path this container cannot safely validate without real hardware, and fuzzy matching is a dedicated-phase-sized feature. Test counts: cip-core-bible 85/85 (up from 82, the 3 new ordinal tests), cip-desktop unaffected at 359/359 in both feature configs. Phase 19 adds zero new Tauri commands, zero new events, zero new migrations, zero frontend changes - it touches exactly one file, core/bible/src/normalize.rs. See ordinalNormalizationProof above and docs/phase-19-audit.md for full reasoning, including everything explicitly deferred to a future phase.\n\nPrevious release candidate statement, retained for provenance: This was the Phase 18 SOFTWARE RELEASE CANDIDATE for Windows x64 (loopback-aware NO SIGNAL/LOW SIGNAL guidance), a direct response to a real Windows pilot report: an operator selected Stereo Mix (Realtek(R) Audio) - a loopback device that mirrors system playback, not a microphone - to capture a YouTube church-service video's audio, started listening, and the Audio & Speech panel showed NO SIGNAL throughout while System Diagnostics confirmed 22669 audio chunks genuinely received (a live, continuously-running capture stream, not a stalled one) but 0/0 inferences (75/75 windows classified as silence). Before concluding anything, this phase traced the full capture path end to end (docs/phase-18-audit.md): device selection (integrations/audio/src/lib.rs::find_device matches the exact selected device, never silently falls back to a global default), stream negotiation (default_input_config() is called on that specific Device instance, not a global default), downmix (downmix_to_i16 correctly averages every interleaved channel per frame using cpal's own negotiated channel count, not a hardcoded mono assumption), RMS computation (a standard root-mean-square, correctly clamped), and resampling (the diagnostics screenshot's own '480 samples @ 48000 Hz, resampled to 160 samples' is exactly the correct 48000-to-16000 Hz ratio, confirming Phase 3.8.6's own resampling fix is working as designed). Whisper's own independent silence gate (ai/speech/src/whisper.rs::is_silence, SILENCE_RMS_THRESHOLD = 0.01 - a second, separately-implemented RMS computation over the resampled 16kHz buffer, in a different crate) also independently agreed every window was silent. Two independently-implemented RMS computations, on two different buffers, in two different crates, agreeing rules out a computation bug in either path being the cause (a bug would have to coincidentally reproduce in both independent implementations) and instead points at genuinely near-silent PCM data reaching the application - i.e. a real-world Windows Stereo Mix quirk (its own OS-level recording level muted or at 0%, a common out-of-the-box state on many OEM installs; or system audio not actually routed through the same device Windows treats as the default playback output that Stereo Mix mirrors) that no cross-platform audio library has any API to detect or correct. What this phase found genuinely addressable: the existing NO SIGNAL/LOW SIGNAL message (Phase 14, calibrated from a different real operator report about a physical microphone) assumed a physical microphone - 'move it closer, raise its gain' - advice that is not just unhelpful but actively wrong for a loopback device, which has no physical position and no per-device gain control in CIP itself. describeAudioSignal (apps/desktop/src/lib/format.ts) now takes an optional deviceName parameter, populated from the backend's own resolved AudioEngineStatus.selectedDevice (accurate even when the operator left CIP's device picker on 'Default device'); when the name matches a known Windows loopback/monitor device pattern (stereo mix, wave out mix, what u hear, loopback, monitor of - case-insensitive), both NO SIGNAL and LOW SIGNAL messages are replaced with guidance that actually applies - the device's own Windows recording level and default-output routing - instead of microphone-gain advice that cannot apply. Every existing caller/test defaults deviceName to null and keeps its exact original physical-microphone wording unchanged. This is built on top of Detection Accuracy Analytics (Phase 17), Live Transcript Show/Hide (Phase 16), Transcript Stall Visibility + Fuller-Context Bible Detection (Phase 15), Whisper Hallucination Filtering (Phase 14), Church Knowledge Base (Phase 13), Multi-language Whisper (Phase 12), Local Congregant Companion View (Phase 11), Church/User Roles & Permissions (Phase 10), Bible Translation Registry v2 (Phase 9), Production Integration (Phase 8), the Phase 7 Real Audio Fingerprinting arc (7.1-7.3), the Phase 6 Operator Ergonomics arc (6.1-6.8), the Phase 5 Reliability & Trust arc (5.1-5.4), True Semantic Bible Search (Phase 4.4), Instant Bible Detection (Phase 4.3), Live Bible Detection Performance/SIMD (Phase 4.2), lexical Paraphrase Detection (Phase 4.1), the Phase 3.10.x multi-screen pillar, the Sermon Harvest bundle (Phase 3.9), and the Phase 3.8.x live-speech-stability arc, all still verified working in this exact artifact. It is a real, verifiable build artifact - not a placeholder, not a simulation, not a fabrication - and every claim in this manifest is backed by direct inspection of this exact packaged .exe: every prior-phase Rust-side symbol/string count, including Phase 17's own Detection Accuracy Analytics functions, was re-confirmed present and unchanged from the Phase 17 baseline (whisper_full_with_state stable at 96 for a 12th consecutive rebuild, retry_paraphrase_or_semantic_with_fuller_context reconfirmed at its Phase-17-corrected count of 2), and this rebuild's own zero-new-Rust-symbol result confirms Phase 18 changed no Rust code, exactly as expected for a pure frontend phase. Correctness for the new code rests on 5 new frontend unit tests in format.test.ts (loopback NO SIGNAL guidance never mentions 'microphone', loopback LOW SIGNAL guidance mentions 'volume' not a microphone gain adjustment, case-insensitive/alias detection for Stereo Mix and What U Hear, and two tests confirming a normal microphone device and a null device name both keep the exact original wording). Full regression: no Rust touched (cargo fmt/clippy/test unaffected, not re-run beyond confirming git status shows no Rust files changed); frontend typecheck 0 errors, lint same 5 pre-existing warnings (unchanged), 303/303 tests passed (up from 298, the 5 new tests), build clean. Phase 18 adds zero new Tauri commands, zero new events, zero new migrations, zero backend changes of any kind. See knownLimitations below for the honest remainder - most critically, this phase does not and cannot fix the underlying Windows audio routing, only point the operator at where to look, and this exact artifact has not yet been installed or launched on real Windows hardware. See docs/phase-18-audit.md for the full breakdown.",
    "modelPackagingStatement": "NO Whisper model file and NO embedding model files (model.safetensors/tokenizer.json) are bundled with this installer, and none could be obtained to bundle: this container's egress policy blocks the standard model hosts (huggingface.co), and no such files exist anywhere in this repository to embed even if bundling logic were added. An in-app 'Select Existing Model File' button (native file picker + install_whisper_model command, Phase 3.8.7.1) validates a candidate Whisper file by actually loading it as a real Whisper model before installing it atomically; the Phase 4.4 counterparts (install_embedding_model_file/install_embedding_tokenizer_file) copy the embedding model/tokenizer files into place the same way (full pair-validation happens once both are present, via get_embedding_capabilities). The operator downloads the canonical files themselves (their own Windows machine's internet access, not this container's) and installs them with one click each. No automatic download and no network dependency were added to the SHIPPED APPLICATION itself - the app never initiates any network request. See modelProvisioningProof/semanticBibleSearchProof and pilot-evidence/3.8.7.1/build/model-provisioning-evidence.json, pilot-evidence/4.4/ for the full reasoning.",
    "physicalHardwareStatement": "CONFIRMED on real Windows hardware by the operator for the Phase 3.8.7.5 artifact (Live Intelligence Router + segmentation) via the screenshots that triggered Phase 3.8.7.8: Service Replay successfully detected MAT 6:9 and displayed it on the presentation window, proving the downstream Bible detection/persistence/presentation pipeline genuinely works end to end on real hardware. Two earlier artifacts (SHA-256 7e531239ab18df0ed19f871ec2936c97a896a3a863c136506a80e172480b1bf1, the Phase 24.2 real-interim-transcript rebuild; and SHA-256 722961a3c490f85e41d99ca7a0511ad2edaa18ae474e3b839e3c027900214589, the Phase 24.3 true-dual-tier-Whisper rebuild) were never installed or launched on real Windows hardware either; THIS exact artifact (SHA-256 0aaf6f73e5970568d5a6882e7d5ad1cf3eeb514e7e9415b1779ea2e2875c18d6) - the Phase 24.3.2 quality-tier-backlog-visibility-status-label rebuild - has NOT yet been installed or launched on real Windows hardware, and neither has the Phase 24.3.1 quality-tier-language-conditioning-fix rebuild it supersedes (SHA-256 ca982fcbe3c761424b5f20f41e5d688676e304c051730ca24e63c313ada8b8ca), nor has the Phase 24.1 real-queue-strip-slide rebuild before that (SHA-256 2b16b218d0307d3ca9c45b405902dffd800fcf20e892e95cc34e78a4a9ef85bb), nor has the Phase 22 model-tier-diagnostics rebuild it supersedes (SHA-256 3867af4092cdceba268f742f6bc17b6e11f311055681924c1fc4e28943a3bf56), nor has the Phase 21 VAD-triggered-flush rebuild (SHA-256 d82c93ef03df79e3b829771fb53076dc39d250af0d6ca39671cd034a9b1de25b), the Phase 20 fuzzy-book-matching rebuild, or the Phase 19 ordinal-normalization rebuild it superseded, and the True Semantic Bible Search embedding-model code path it carries forward unchanged still has never run against a real model.safetensors file anywhere, in this container or otherwise (blocked model host - see modelPackagingStatement); Phase 7.1's fingerprinting algorithm still has never been tested against real recorded audio anywhere (no such audio exists in this repository), neither Phase 7.2's enrollment UI nor Phase 7.3's Remove button has ever been exercised by a real operator against a real file, Phase 8's own OBS/vMix clients have never been exercised against real OBS or vMix software (neither is installed in this container), Phase 9's own ai_processing_allowed gate has never been exercised by a real operator clicking Generate Verse Embeddings against a real production database, Phase 10's own operator-account bootstrap/login/Admin-gate flow has never been exercised by a real operator on real Windows hardware, Phase 11's own companion server has never been enabled by a real operator nor opened from a real phone browser on a real church LAN, Phase 12's own language selection has never been exercised against real Yoruba or Hausa speech by a real operator on real Windows hardware, Phase 13's own Church Knowledge Base has never been exercised by a real operator accepting a real Sermon Intelligence finding and confirming it survives a restart on real Windows hardware, Phase 14's own hallucination-filtering fix has never been re-verified against a second real pilot session confirming the Live Transcript panel no longer shows placeholder captions as real speech and that the confidence badge genuinely varies, Phase 15's own transcript-stall-indicator and fuller-context paraphrase retry have never been exercised against a real quiet/unclear stretch or a real paraphrase spoken without a book name on real Windows hardware, Phase 16's own Show/Hide toggle has never been exercised by a real operator during a real service, Phase 17's own Detection Accuracy dashboard has never been reviewed by a real operator against a real service history on real Windows hardware, and this phase's own loopback-aware guidance message has never been seen rendered by a real operator selecting Stereo Mix (or another loopback device) on real Windows hardware. It has been verified at Environment A only: Phase 21 changed Rust code scoped to core/ai's SpeechEngine trait and ai/speech's WhisperSpeechEngine, plus the established diagnostics-plumbing pattern in apps/desktop/src-tauri and two frontend files - the full backend regression suite was re-run (cargo fmt/clippy/test, both feature configs) and passed clean, cip-ai-speech now at 27/27 with --features whisper (up from 19, the 8 new VAD-triggered-flush tests), cip-desktop unaffected at 359/359 in both feature configs; the full frontend suite was also re-run (typecheck/lint/test/build all clean, 303/303 tests unchanged - the new field is purely additive), and the packaged installer was directly extracted and inspected (DLLs still present, every prior-phase Rust-side symbol/string count confirmed unchanged from the Phase 17-20 baseline via strings, whisper_full_with_state stable at 96 for a 15th consecutive rebuild - see vadTriggeredFlushProof above - last_feed_was_vad_early_flush, the new trait method, directly confirmed present; should_flush_early/has_trailing_pause were inlined by the release-mode optimizer as expected for small pure functions). The decisive next steps remain the operator's own real-hardware tests: (1) carried forward from Phase 4.4 - install a real Whisper model AND a real all-MiniLM-L6-v2 model.safetensors/tokenizer.json pair, run generate_verse_embeddings, then speak a conceptual paraphrase and confirm it is correctly surfaced as a Pending suggestion; (2) carried forward from Phase 5.1 - complete a real service and confirm History's Service Report panel renders correctly; (3) carried forward from Phase 5.2 - speak (or replay) a paraphrase of a verse twice within about a minute and confirm the Needs Attention queue shows a single suggestion with a 'Confirmed x1' badge and a visibly higher confidence than the first mention alone would have produced; (4) carried forward from Phase 5.3 - run a live or replayed service with genuine pauses between sentences and confirm the transcript no longer shows hallucinated text during silent gaps, and that the Pilot Diagnostics panel's silent-windows-skipped count rises during those gaps without ever rising during actual quiet speech; (5) carried forward from Phase 5.4 - reject a Paraphrase/Semantic suggestion, speak (or replay) the same wording again within about a minute, and confirm the Needs Attention queue does not show it again while History's Service Report and Scripture & Findings list show the echo count rising; (6) carried forward from Phase 6.1 - with items from more than one domain present in the Needs Attention queue, confirm the heading's legend text matches the top item's domain and that pressing A/R acts on that exact item, not a different one; (7) carried forward from Phase 6.2 - click Display once and confirm the button reads 'Confirm Display?' with nothing yet projected, click it again and confirm it fires and an 'Undo (blank screen)' banner appears, then click Undo and confirm the real screen blanks; (8) carried forward from Phase 6.3 - with a live feed containing more than a few items, type a word that only appears in one item's summary into the search box and confirm the feed narrows to just that item, then clear the box and confirm every item returns; (9) carried forward from Phase 6.4 - trigger a real audio or speech error (e.g. unplug the microphone, or point the Whisper model path at a nonexistent file) and confirm Operator Mode's notice shows the actual error text, not just the bare 'AUDIO ERROR'/'SPEECH ERROR' label; (10) carried forward from Phase 6.5 - trigger a real command failure (e.g. approve a suggestion while offline, or attempt an action during a network hiccup), confirm the banner names the failed action, then click the x and confirm it clears without needing to trigger another action first; (11) carried forward from Phase 6.6 - on first launch (or after clearing app storage), confirm the walkthrough appears and does not reappear on the next launch after dismissal; with either the Bible dataset or a Whisper model not yet installed, confirm the setup notice names the right item(s) and that 'Open Diagnostics Mode' switches modes; once both are installed, confirm the notice disappears; (12) carried forward from Phase 6.7 - open Diagnostics Mode and confirm Pending Suggestions and Service Timeline are visible immediately while the other 9 panels start collapsed; click each summary and confirm it expands/collapses correctly; on Service History, confirm Show/Hide toggles the history list without also collapsing the panel; (13) carried forward from Phase 6.8 - with a Bible reference in the Needs Attention queue, click Edit and confirm the action buttons are replaced by an input pre-filled with the current reference, type a different valid reference, click Save, and confirm the queue updates to the new reference; click Cancel on a different item mid-edit and confirm it reverts to the action buttons unchanged; confirm pressing A/R on the top item still displays/rejects it exactly as before, unaffected by Edit's presence; (14) carried forward from Phase 7.1 - create an acoustic-model.json manifest naming one or more real WAV reference recordings of songs already present in a Music dataset, restart the app, confirm Diagnostics Mode's acoustic status reports Available (not Unavailable/Error) with the expected number of songs enrolled; play (or have the app capture) one of those songs live and confirm it is correctly recognized as a Pending suggestion with a plausible confidence score; separately, play an unrelated song or ambient service audio and confirm it does NOT produce a false match; (15) carried forward from Phase 7.2 - in the Music Intelligence panel's 'Reference recordings' section, search for a real song already in a Music dataset, select it from the results, click 'Select audio file & enroll', pick a real WAV file via the native file picker, and confirm the song appears in the enrolled list with the message 'Enrolled. Restart CIP for it to take effect.'; restart, then repeat item 14's verification against that specific enrollment; (16) carried forward from Phase 7.3 - with at least one enrollment present in the enrolled list, click its Remove button and confirm it disappears from the list with the message 'Removed. Restart CIP for it to take effect.' and its WAV file is gone from the model directory; restart CIP and confirm Diagnostics Mode's acoustic status no longer counts that song among its enrolled songs, and that song is no longer recognized when played live; (17) carried forward from Phase 8 - with a real OBS and/or vMix instance running on the same machine or network, open the Production Integration panel, enable OBS (or vMix), enter its host/port/password (OBS) or host/port/input (vMix) and a real text source/title name that already exists in that OBS scene or vMix input, click Test Connection and confirm it reports success against the real application; click Save; then display a real verse or slide in CIP and confirm the named OBS text source or vMix input's text updates live to match; clear the active presentation in CIP and confirm the same source/input blanks; separately, confirm that leaving both OBS and vMix disabled (the default) produces no connection attempts and no change to CIP's own local Stage/Confidence/Lobby display behavior; (18) new this phase - with a real production build (BSB installed, no dev seed) and a real Whisper AND embedding model both installed, open the panel/action that calls generate_verse_embeddings (or Diagnostics Mode's equivalent) and confirm it still succeeds now that the ai_processing_allowed gate is wired in - BSB's dataset asset records that permission as true, so this should behave identically to before this phase; separately, if any future translation is ever imported without an explicit ai_processing_allowed grant, confirm generate_verse_embeddings reports a clear refusal naming the translation rather than silently doing nothing or crashing. (19) new this phase - with a real installed CIP (fresh, no operator_accounts rows yet), launch it and confirm the app is gated behind a 'create the first Admin account' form rather than opening straight to the normal workspace; create that first account with a display name and a PIN and confirm it logs straight in as Admin without any separate login step; use the nav bar's Log Out control and confirm the app returns to a login form (not the create-first-admin form, since an account now exists); log back in with that Admin account's display name and PIN and confirm the app returns to the normal workspace; while logged in as that Admin, create a second account with the Operator role; log out, log in as the new Operator account, and confirm every day-to-day action (search Scripture, display a verse, approve/reject a suggestion, start/pause/resume a service) still works normally; then attempt a genuinely Admin-gated action (e.g. open Production Integration settings and attempt Set Production Integration Config, or attempt Install Whisper Model) and confirm it is refused with a clear message rather than silently succeeding or crashing; finally, restart the app entirely and confirm it requires logging in again rather than remembering the last session. (20) new this phase - with a real installed CIP, log in as an Admin, open the Congregant Companion View panel, click Start Companion Server, and confirm it reports Running with at least one http:// address (or, on a machine with no detected LAN route, confirm the panel says so honestly and still shows the port); on a phone connected to the same wifi network as the computer, open that address in a browser and confirm the page loads without installing anything; in CIP, display a verse or slide and confirm the phone's page updates to show it within a couple of seconds without a manual refresh; clear the presentation display in CIP and confirm the phone's page returns to 'Nothing is currently being displayed'; on the phone, type something into the notes box, reload the page, and confirm the note is still there (proving it persisted locally on the phone, not via CIP); then click Stop Companion Server in CIP and confirm the phone's page can no longer reach the server (a reload fails to connect); finally, log out and log back in as a non-Admin Operator account and confirm Start/Stop Companion Server are refused. (21) new this phase - with a real installed CIP, log in, open the audio-device/listening controls, and confirm a language selector is present showing English/Yoruba/Hausa/Auto-detect; with a real multilingual Whisper model installed, select Yoruba, speak a sentence in Yoruba, and confirm the transcript is recognizable Yoruba text (not English, not garbage) and that the transcript segment's reported language is \"yo\"; repeat for Hausa; select Auto-detect, speak in English, and confirm the reported language comes back \"en\" without the operator having selected it explicitly; separately, with an English-only (ggml-*.en.bin) model installed instead, confirm the panel honestly shows that the model cannot honor a non-English selection, and that selecting Yoruba/Hausai anyway still produces English transcription rather than silently failing or crashing; finally, confirm changing the language selection while actively listening takes effect on the next few seconds of audio without needing to stop and restart listening. (22) new this phase - with a real installed CIP, log in, preach (or replay) a sermon and let Sermon Intelligence detect a Theme finding, accept it from the Needs Attention queue, and confirm it now appears immediately in History's new Church Knowledge Base section under Most-preached themes, attributed to the sermon just given; accept a Takeaway or Key Statement finding from the same sermon and confirm it appears in the recent-findings feed but never under Most-preached themes; reject a different Theme finding and confirm it never appears anywhere in the Church Knowledge Base; restart the application entirely and confirm every previously accepted finding is still shown correctly grouped by theme and by speaker, proving it survived the restart rather than only having been visible for the remainder of that one session; preach a second sermon on the same theme label and accept its own Theme finding, and confirm the Most-preached themes entry now shows an occurrence count of 2 and lists both sermons, not a duplicate entry. (23) new this phase - on the same or similar Windows hardware and microphone setup that produced this phase's own triggering report, start listening during a real service or a realistic replay with genuine silent gaps and quiet-but-real speech, and confirm the Live Transcript panel no longer shows '[BLANK_AUDIO]', '(speaking in foreign language)', '[inaudible]', '[LAUGHTER]', or similar bracketed placeholder captions as if they were real speech; confirm the confidence badge next to each transcript segment now genuinely varies (not a constant 75%) and reads visibly lower on garbled or quiet audio than on clear speech; confirm Pilot Diagnostics now shows a nonSpeechPlaceholdersSkipped count that rises specifically during placeholder-caption events without ever rising during genuine clear speech; and confirm the input-level readout now shows 'LOW SIGNAL' with a concrete microphone-positioning suggestion when input is quiet-but-real (roughly 1%-15%), rather than the same bare 'SIGNAL CAPTURED' text shown at any non-silent level before this phase. (24) new this phase - on the same or similar Windows hardware and microphone setup that produced this phase's own triggering screenshots, start listening during a real service or a realistic replay with a genuinely quiet or unclear stretch of at least 30-40 seconds, and confirm the Live Transcript panel now shows a 'No new transcript in Ns...' message directly under the transcript list instead of appearing to freeze with no explanation; separately, speak (or replay) a sentence that closely echoes a specific verse's wording without citing its book/chapter/verse and without using enough of that wording in any single ~3s window alone, and confirm a Paraphrase suggestion now appears in the Needs Attention queue once the fuller ~15s accumulated window closes, where it would not have appeared from the raw window alone; confirm no duplicate suggestion appears for a reference the raw window already caught on its own. (25) new this phase - with a real installed CIP, log in and start listening during a real service, locate the Show/Hide button in the Live Transcript panel's own heading, click Hide, and confirm the transcript list is replaced by a one-line note confirming detection keeps running while Bible/Sermon/Service/Music detection all continue to surface new suggestions/findings exactly as before; click Show and confirm the full transcript list reappears with every segment received while it was hidden, not just new ones; restart the app and confirm the panel opens in whichever state (collapsed or expanded) it was last left in, proving the preference genuinely persisted via localStorage. (26) new this phase - with a real installed CIP and at least one completed real service in its history, log in and open History; confirm the new 'Detection Accuracy' section at the top loads and shows real overall/by-confidence-level/by-detection-method counts matching what was actually approved, edited, and rejected during that service (cross-check against the Scripture & Findings list on that same service's own page); if more than one service has any decided suggestions, expand 'Trend by service' and confirm each entry's counts match that specific service's own history; approve, edit, and reject a few more Bible suggestions during a new live service, return to History, and confirm the Detection Accuracy totals increase to reflect them without needing to restart the app. (27) new this phase - with a real installed CIP, select Stereo Mix (or another loopback/monitor device, if present) as the audio input device, start listening with no real audio playing, and confirm the Audio & Speech panel's NO SIGNAL message now reads the loopback-specific guidance (pointing at the device's own Windows recording level and default-output routing) rather than the old 'move the microphone closer' wording; separately, check that device's own Windows recording level (Sound Settings > Recording > that device > Levels) - if it is muted or at 0%, raise it, confirm audio is actually playing through this computer's default output device, and confirm CIP's own input-level meter then rises above NO SIGNAL and transcription resumes, closing the loop on whether the underlying capture pipeline itself works once real audio genuinely reaches it; separately, select a normal physical microphone and confirm the original 'move the microphone closer or raise its gain' wording still appears unchanged for NO SIGNAL/LOW SIGNAL on that device. (28) new this phase - with a real installed CIP, start listening during a real service (or a realistic replay) and say a digit-suffix-ordinal book reference that was never detectable before this phase, e.g. '1st Corinthians chapter thirteen verse four' or '2nd Timothy chapter three', and confirm it is now correctly detected and surfaced in the Needs Attention queue; separately confirm the pre-existing word-form alias ('First Corinthians...') still works exactly as before, and that a non-Bible use of an ordinal past third (e.g. 'fourth of July') is not mis-normalized into a stray digit. (29) new this phase - with a real installed CIP, start listening during a real service (or a realistic replay) and say a book reference using a name Whisper is likely to mis-transcribe close to (but not exactly) a real single-word book name - e.g. 'Roman chapter 8 verse 28' where Whisper renders 'Romans' as 'Roman' - and confirm it now surfaces as a Pending suggestion (visibly lower confidence than an exact citation) where it previously would have produced nothing at all; separately confirm an exact citation ('Romans 8:28') is never reclassified as fuzzy. Separately, open the Correct Active Context manual-entry field and confirm typing the book's full name ('John') or a common abbreviation ('Jn') now works identically to typing the internal code ('JHN'), where previously only the exact code worked. (30) new this phase - with a real installed CIP, log in and start listening during a real service and open System Diagnostics; speak several sentences with genuine pauses between them and confirm the new 'windows flushed early - a natural pause was detected' diagnostic count rises above zero as the service proceeds; separately, listen for whether words right at those pause points now sound less garbled/truncated in the Live Transcript panel compared to a prior session's experience with the same speaker/microphone before this phase; separately, speak one long continuous sentence with no pause anywhere near a few seconds long and confirm transcription still completes normally (falling back to the fixed ~3s cap exactly as before this phase). (31) new this phase - with a real installed CIP, open System Diagnostics and use 'Select Existing Model File' to install a base.en or small.en model (downloaded separately, since CIP never bundles or auto-downloads one) and confirm the displayed sizeTierHint correctly reads a base-class or small-class description rather than tiny-class; separately, start a real service and subjectively compare Bible/sermon detection accuracy, latency, and rate on a Nigerian-accented speaker against a prior session that used the tiny.en default, confirming the recommendation in docs/live-speech.md's 'Model selection and quality' section actually holds on real audio. (32) new this phase - with a real installed CIP, during a real (or realistic replay) service, prepare two or more items (approve a detected/manual scripture suggestion and Prepare it, without yet displaying it) and confirm the Live/Preview stage's queue strip shows each item's real, word-wrapped verse text and translation footer - not a single-line raw snippet - matching what clicking Display on that card actually sends to the projector/monitor output. (33) new this phase - with a real installed CIP (--features whisper build, a real model installed) and a real microphone, start listening during a real service and speak one continuous sentence at least 2 seconds long; confirm the Live Transcript panel shows a dimmed, provisional line with in-progress text partway through the sentence, and confirm that line is replaced by a normal, timestamped, confidence-scored entry once the sentence completes (or a natural pause triggers the window to close) - and that the final text is sensible even if the interim text it replaced was rougher or incomplete. (34) new this phase - with a real installed CIP (--features whisper build) and TWO real Whisper model files installed (one at the fast tier's default path, a second, typically larger one installed via System Diagnostics' 'Select Quality-Tier Model File...' button), start listening during a real service and speak a complete sentence; confirm the Live Transcript panel's line for that sentence gains a small 'corrected below' badge within a few seconds, that a second, corrected line actually appears below it with real, different (or confirming) text, and that CIP's overall responsiveness (audio capture, fast-tier transcription, UI) is not noticeably degraded by the second model running concurrently on the same machine. (35) new this phase - re-run item (34) above (two real Whisper models installed, one fast/one quality-tier) with a service whose language selection is set to \"auto\" (or where the two tiers are configured with different languages), speaking a sentence in a non-English supported language (Yoruba or Hausa); confirm the corrected line that appears is genuinely decoded in the same language as the original line, not silently decoded as English by the quality tier regardless of what the fast tier detected. (36) new this phase - with a real installed CIP (--features whisper build) and a quality-tier model installed, deliberately cause the quality worker to fall behind (e.g. install a very large/slow quality model, or produce final windows in rapid succession) and confirm System Diagnostics' new 'Backlog status' row transitions Normal → Busy → Falling behind → Overloaded as consecutive drops accumulate, and recovers back to Normal once the worker catches up - proving the label reflects real, live worker behavior rather than a static value.",
    "githubReleaseStatus": "NOT CONFIGURED - no .github/workflows directory exists in this repository, and no GitHub Release has been created for this artifact. This installer is available only via this repository's working tree (release/windows/), not via GitHub Releases or any CI/CD pipeline.",
    "obtainingWithoutDeveloperTooling": "The file at release/windows/Church Intelligence Platform_0.1.0_x64-setup.exe can be copied/transferred directly (e.g. via a git clone of this repository, a direct file download, or a USB drive) to the target Windows machine and run there. Running the installer on Windows requires none of: Claude Code, Rust, Cargo, Node.js, npm, or the CIP source tree - only the .exe file itself. A real Whisper model file (see modelPackagingStatement) must be placed separately by the operator for transcription to work.",
    "knownLimitations": [
        "UNSIGNED release candidate - Windows SmartScreen will show an 'unknown publisher' warning on first run",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware - Environment A (automated build-time verification, including compiled-in symbol checks for this phase's additions) only. The operator's own real-hardware re-test, on a machine with at least two monitors, is the decisive, pending gate - see physicalHardwareStatement.",
        "Monitor identity is CIP's own best-effort session identity, not a real OS-issued hardware id - Tauri exposes no stable monitor identifier. compute_monitor_id prefers the OS-reported name, falling back to a position/resolution fingerprint. Two identically-named, identically-positioned monitors swapped between sessions could be indistinguishable to this scheme - see displayRegistryProof.",
        "No literal OS fullscreen toggle - the presentation window is positioned/sized to exactly cover the assigned monitor's bounds, but no platform fullscreen API is called. A window manager or taskbar could still be visually present depending on OS/desktop-environment behavior - a deliberate scope boundary, not an oversight.",
        "No live monitor hot-plug detection - list_displays reflects whatever the OS reports at the moment it's called; a monitor plugged/unplugged afterward is only reflected on the next explicit Refresh in the Display Setup panel. Live disconnect/reconnect handling is Phase 3.10.4's scope, not this phase's.",
        "Existing open presentation windows are not moved retroactively when a role assignment changes - only the next window opened for a screen mapped to that role is affected.",
        "DisplayRole::Stage (the operator's own 'speaker-facing information' taxonomy) has no corresponding content stream yet - only DisplayScreen::Stage (the primary congregation-facing output) maps to DisplayRole::Projector for placement purposes. A genuinely separate speaker-monitor content stream remains future scope.",
        "The 'two-monitor case allows projector selection' and 'window placement opens directly on the selected monitor' gates are proven correct in isolated unit tests (merge_displays, resolve_role_position) against synthetic monitor fixtures, not exercised on real multi-monitor hardware in this single-display container - see displayRegistryProof.",
        "Presentation Router (Phase 3.10.3) is a per-screen Live/Held toggle, not true independent content per screen - a Held screen freezes on its last content rather than showing something different from Live screens, a deliberate scope boundary the user chose explicitly over the alternative (breaking presentation.rs's at-most-one-Active-item invariant) - see presentationRouterProof.",
        "Route mode (Live/Held) is session-only, not persisted - every screen resets to Live on app restart, matching every other live-session field in AppState. An operator wanting a screen (e.g. Lobby) to stay Held across a restart cannot today - see docs/phase-3-10-3-presentation-router.md.",
        "No visual indicator on the display window itself for whether it is currently Live or Held - only the operator's own Presentation card shows this via the toggle button's label. The congregation/room-facing window looks identical either way.",
        "The Presentation Router's routing logic (screens_to_broadcast) is proven in isolated unit tests; the full real multi-window path (open two screens, Hold one, confirm independence, un-Hold, confirm catch-up) has not been exercised on real Tauri windows in this container - consistent with this project's standing 'no tauri::test harness' testing boundary for window-lifecycle behavior.",
        "Multi-Window Lifecycle (Phase 3.10.4) has no real-time push notification when a monitor disconnects or reconnects - Tauri exposes no such API this session found. The operator must explicitly click Reposition (or Close/Open) after a reconnect or role reassignment for an already-open screen to snap back onto the right monitor - see multiWindowLifecycleProof.",
        "What actually happens to an already-open WebView2 window's on-screen position when its monitor is physically unplugged is Windows/driver behavior this container cannot reproduce or verify - documented honestly as unknown rather than assumed. The new Reposition button is the operator's recovery action regardless of what that OS behavior turns out to be.",
        "The Reposition button has no on-screen indicator of *why* a screen might need it (e.g. 'this screen's monitor is currently disconnected') beyond its tooltip - a future phase could cross-reference the Display Registry's live connection state directly on the screen's own row.",
        "Sermon Harvest only ever covers the currently active sermon, in the same app session it was delivered in - IntelligenceFinding.sermon_id linkage lives only in the in-memory FindingQueue, never persisted (a deliberate Phase 2.0 design). Harvesting a past-session sermon is a real, separate future need, not attempted this phase - see sermonHarvestProof.",
        "Sermon Harvest presents its bundle inline in the UI only - it does not yet export to a file (PDF/text/markdown). The vision document's 'one click produces [...]' framing implies a save-to-file artifact; the underlying data is fully present in the response, only the export step remains.",
        "Sermon Harvest does not group elements by SermonElementKind (Points/Quotes/Illustrations/Prayer Points/etc.) - that taxonomy is currently only a text prefix in each finding's summary field, not a structured field on IntelligenceFinding. Re-deriving it via string-prefix parsing would be fragile and was deliberately not attempted.",
        "Exactly three fixed display screen roles (Stage/Confidence Monitor/Lobby-Overflow) - not an operator-configurable arbitrary number of screens. A deliberate scope boundary, not a limitation of the underlying window mechanism - see multiScreenProof.",
        "The Confidence Monitor shows only what PresentationItem already carries (template, source-suggestion presence, status) - it does not show a 'next up' preview, since this application has no concept of a queued/ordered presentation sequence today.",
        "No per-screen custom templates or branding (e.g. a church logo overlay specific to the Lobby screen) - a real, separate future feature, not attempted this phase.",
        "No network/NDI/OBS/HDMI-matrix output for the new screens - explicitly out of scope since the original local-display foundation (docs/presentation.md), unchanged by this phase.",
        "Of the five pillars the user asked for from the project's master architecture document, Sermon Harvest and multi-screen presentation output are delivered in this artifact. Semantic/paraphrase Bible detection, real audio fingerprinting, and multi-language support remain NOT STARTED - no code, no audit, for any of the three. This is stated plainly rather than implied complete.",
        "OVERLOAD_THRESHOLD_MS itself remains 10 seconds, unchanged - Phase 3.8.7.7 only changed whether an isolated overload crossing resets the segmenter, not the threshold value itself, per the operator's own explicit instruction not to blindly raise it. On hardware where inference is even slower than this operator's (avg 14,991ms), or where genuine sustained overload occurs (consecutive crossings), the audio-discard/segmenter-reset safety behavior is unchanged from Phase 3.8.7.3/3.8.7.5.",
        "Bounded transcript segmentation uses a fixed 12-20s time window only - no pause/silence-based early flushing, since AudioEngine/WhisperSpeechEngine expose no voice-activity signal today. A real pause-aware 'hybrid segmenter' remains a distinct, larger, future design - see docs/phase-3-8-7-5-audit.md.",
        "Cross-Domain Correlation and Content Intelligence remain NOT wired to the live path, deliberately - both are explicitly documented as 'never automatic' by design since Phase 2.4/2.7/2.8, and this phase does not reverse that decision.",
        "Automatic Altar Call detection remains unimplemented - SermonSectionKind::AltarCall exists only as an operator-assignable label with no phrase detector. A future focused classifier phase, not attempted here.",
        "This phase connects existing engines to the live pipeline and bounds the transcript unit - it does not change whisper.cpp/tiny.en's inherent transcription accuracy, or Sermon/Service/Music's own detection accuracy/thresholds. 'The Feed now shows non-Bible findings and transcript entries read as complete segments,' not 'perfect detection,' is the honest bar for the operator's next real-hardware test.",
        "The performance impact of calling three additional engines per ~15-18s logical segment is reasoned (their cost is deterministic pattern-matching, negligible next to this hardware's own 13.9s average Whisper inference), not measured on real hardware in this container - see docs/phase-3-8-7-5-audit.md's performance-consideration section.",
        "NO Whisper model file is bundled - the operator must download one themselves and install it via the 'Select Existing Model File' button (Phase 3.8.7.1), or place it manually at the documented path. This container cannot bundle or auto-download one - see modelPackagingStatement.",
        "In-app automatic model download remains unimplemented - this container cannot reach the model host to build or verify a download-and-checksum-verify path end-to-end.",
        "Environment B (Xvfb GUI reproduction) remains unavailable in this session's container (pre-existing, isolated as a container limitation in Phase 3.8.5, not this phase's regression)",
        "The two Windows/whisper build fixes from Phase 3.8.6 (ggml lib-naming, mingw threading-model alternative) plus the Phase 3.8.6.1 DLL-bundling fix are all container/toolchain-level workarounds, not source patches - see scripts/build-windows-whisper.sh; a future container rebuild or a whisper-rs/whisper.cpp/mingw-w64 version upgrade could change whether any of them are still needed",
        "Music Library is legitimately empty in a production build - no licensed production song dataset exists in this repository; Service Replay's default flow does not exercise Music Intelligence for this reason",
        "The Display shortcut is Bible-only, in the Needs Attention queue only - the diagnostics-mode Pending Suggestions panel still shows Approve, and Music/Sermon/Service/Content/Correlation actions are unchanged. This phase does not address Whisper transcription quality/hallucination on real hardware, which the same operator screenshots also raised - that remains a separate, not-yet-scoped investigation.",
        "See docs/phase-3-8-7-8-bible-display-shortcut.md for this phase's evidence; docs/phase-3-8-7-5-audit.md and pilot-evidence/3.8.7.5/ for the Live Intelligence Router; docs/phase-3-8-7-4-audit.md for the engine-by-engine wiring audit that identified the router's insertion point; docs/phase-3-8-7-3-live-speech-stability.md and pilot-evidence/3.8.7.3/ for the backpressure phase's",
        "Paraphrase detection (Phase 4.1) is lexical/keyword-overlap matching only, not semantic/neural understanding - a paraphrase sharing little or no vocabulary with its source verse (e.g. 'Jesus said we should love our enemies' for Matthew 5:44) will not be found. No embedding model is reachable in this network-restricted container; this remains a documented, not-yet-started gap.",
        "Phase 4.2's Whisper SIMD fix reduces per-chunk inference latency but does not guarantee real-time (sub-3-second) transcription on every machine - CPU generation, background load, and model size all still matter. This exact rebuilt artifact has not yet been installed or launched on real Windows hardware; the operator's own re-test is the decisive, pending gate for whether live Bible detection now works as described.",
        "Phase 4.3's fast detection lane means transcript_segments now has both raw ~3s rows and 12-20s batch rows for the same span of speech (the batch row is required for Sermon Intelligence's own foreign key) - History's transcript list will show both granularities. The live operator-facing transcript feed does not have this problem (only the fast lane's events reach it). This exact rebuilt artifact has not yet been installed or launched on real Windows hardware; the operator's own re-test is the decisive, pending gate.",
        "No real embedding model has ever been loaded or run anywhere in this project's history - this environment's egress policy blocks the standard model host, exactly as already documented for the Whisper model. Every Phase 4.4 test uses either pure math (cosine_similarity on hand-constructed vectors) or a deterministic fake EmbeddingEngine; CandleEmbeddingEngine::load/embed's actual runtime correctness against a real model.safetensors file is unverified. This is the single largest open risk Phase 4.4 carries forward - see semanticBibleSearchProof.",
        "MIN_SEMANTIC_SIMILARITY (0.55) is documented from published all-MiniLM-L6-v2 benchmark ranges, not empirically calibrated against a labeled dataset - no real model inference was possible in this container. Revisit once real operator feedback on live services exists.",
        "generate_verse_embeddings reports no incremental progress while it runs (synchronous on the calling command thread; Tauri dispatches command handlers off the main UI thread so the app itself does not freeze, but the frontend has no progress signal until it completes) - for a full-Bible translation on CPU this can take minutes.",
        "Semantic manual Bible search and cross-reference intelligence are NOT wired up this phase - only the live detection pipeline reaches try_semantic. search_bible (the Bible Library's manual search box) remains FTS/keyword-only. Both fall out of the same embedding infrastructure already built and are natural, low-effort follow-ups, not attempted this phase.",
        "No hot-swap of a running embedding engine - installing a new model/tokenizer pair takes effect on CIP's next launch, mirroring install_whisper_model's own documented limitation exactly.",
        "No confidence-ring UI for Semantic-kind suggestions yet - the Ambiguous candidate-ranking UI pattern already exists in the frontend; extending it to semantic hits is presentation-layer work only, not attempted this phase.",
        "Pre-existing, unrelated test flake: config::tests::whisper_model_path_honors_the_env_override fails intermittently under the default parallel cargo test runner because it mutates a process-wide environment variable another test reads concurrently - it passes in isolation and under --test-threads=1. This predates Phase 5.1 (config.rs was not touched this phase) and is documented rather than silently ignored - see docs/phase-5-1-post-service-observability-report.md.",
        "The Post-Service Observability Report's live-diagnostics half (speech/embedding pipeline counters) is process-lifetime, not service-scoped, by deliberate design - an operator running two back-to-back services in one app session will see the same cumulative counters in both reports. The suggestion/detection/timeline half of the report has no such limitation - see serviceReportProof.",
        "get_service_report has no caching - it re-runs its aggregation queries on every call. Fine for a single service's row counts, not something to call in a tight polling loop.",
        "The Service Report has no export (PDF/text) - it is view-only inside History's new panel; the underlying data is fully present in the response, only a save-to-file step remains.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the Service Report has never been rendered against a real operator's completed service - see physicalHardwareStatement.",
        "CONFIRMATION_SCORE_BONUS (0.1) and MAX_CONFIRMED_SCORE (0.9) are documented policy choices, not empirically calibrated - consistent with every other confidence threshold in this codebase (MIN_PARAPHRASE_SCORE, MIN_SEMANTIC_SIMILARITY), no real operator feedback on live services has informed these numbers yet.",
        "Temporal confirmation only ever fires within the existing 60-second suggestion-dedup window - a genuinely repeated paraphrase minutes apart (past the window) is treated as a brand-new, unconfirmed suggestion, exactly as dedup already treated it before Phase 5.2.",
        "The 'Confirmed x N' badge only appears in the Needs Attention queue (LiveChurchBrain.tsx) - the diagnostics-mode Pending Suggestions panel does not yet surface confirmation state.",
        "No disconfirmation mechanism - if a later segment's wording clearly contradicts an earlier heuristic guess, Phase 5.2 has no way to lower confidence; only repetition of the same reference raises it.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and Temporal Confirmation has never been exercised against a real repeated paraphrase in a live or replayed service - see physicalHardwareStatement.",
        "SILENCE_RMS_THRESHOLD (0.01) is a documented policy choice, not empirically calibrated against any real microphone recording - consistent with every other confidence/detection threshold in this codebase. It has never been exercised against real audio in a live sanctuary environment; a real room's noise floor, a specific microphone's gain staging, or background HVAC/organ hum could all shift what 'silence' means in practice.",
        "Voice Activity Detection (Phase 5.3) is RMS-energy thresholding only - noise suppression and acoustic echo cancellation, the other two technologies named in the operator's own 'audio pipeline hardening' roadmap item, remain entirely unaddressed. Noise suppression would require a new native dependency; AEC is inherently a real-hardware-only concern with no way to construct a meaningful automated test for it in this container.",
        "VAD uses a single fixed threshold, not an adaptive/rolling noise floor - a persistently noisy room could see effectively no windows ever classified as silent, while an unusually quiet one could (incorrectly, if a pastor speaks very softly) classify real speech as silence.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and Voice Activity Detection has never been exercised against real microphone audio with genuine silent gaps in a live or replayed service - see physicalHardwareStatement.",
        "Rejection-echo tracking (Phase 5.4) is scoped to Paraphrase/Semantic suggestions only, matching Phase 5.2's own precedent - a rejected explicit citation's repeat is unaffected (dedup already suppressed it exactly as before, with no echo tracking added).",
        "Only the most recent Rejected suggestion for a reference is ever echoed - if an operator rejects the same reference on two separate, non-adjacent occasions in one service, only the most recent rejection's counter increments for a redetection.",
        "The rejection echo only ever fires within the existing 60-second dedup window - a genuinely repeated false-positive minutes after a reject (past the window) is treated as a brand-new, unconfirmed suggestion, exactly as dedup already treated every other repeat before this phase.",
        "No UI surface for rejection echoes in the live Needs Attention queue - a rejected suggestion is filtered out of the live queue entirely, so the echo count is only ever visible in History's post-service view, not during the live service itself.",
        "rejection_echo_count is observability only - it never feeds back into confidence scoring, never auto-rejects a future suggestion, and never changes detection behavior in any way.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the Wrong-Verse Feedback Loop has never been exercised against a real rejected-then-repeated detection in a live or replayed service - see physicalHardwareStatement.",
        "Phase 6.1's keyboard-shortcut fix is scoped to A/R only - 'E' (edit) and 'P' (preview) remain Diagnostics-mode-only, since editing a reference has no equivalent action in the domain-generic unified action model yet.",
        "7 more ergonomics gaps from Phase 6's own audit remain unaddressed: no confirmation/undo on the one-click Display action, no text search over the live feed, buried audio/speech error visibility, no dismiss/context on the generic error banner, no onboarding, and a still-dense Diagnostics Mode - each a candidate for a future Phase 6.x slice.",
        "No visual on-screen indicator of which item is 'first' in the Needs Attention queue beyond its own existing ordering - an operator must still visually identify the top card; the legend describes the action, not which card it applies to.",
        "Direct strings-based binary verification of this phase's new frontend text (the shortcut legend, e.g. 'A = Display, R = Reject for the first item') is not possible - Tauri embeds the built frontend bundle into cip-desktop.exe in a compressed form plain strings(1)/nm(1) cannot search, confirmed by also failing to find pre-existing, unchanged strings ('Needs Attention', 'Display'). Correctness instead rests on a fresh Vite build hash, the installer's changed SHA-256, and a full 231/231 frontend test pass - see keyboardShortcutsProof.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the keyboard-shortcut fix has never been exercised against a real multi-domain Needs Attention queue - see physicalHardwareStatement.",
        "Undo (Phase 6.2) blanks the display screen; it does not restore the item that was showing before the mistaken Display - no code anywhere tracks 'what was showing previously,' so a true restore remains future scope, stated honestly rather than implied.",
        "The confirm guard (Phase 6.2) is scoped to Bible's Display action only - the one unified action that immediately projects to a real screen. No other action (approve/reject/accept/acknowledge/review/dismiss) gained a confirmation step.",
        "Both the confirm window (4s) and the undo window (8s) are documented policy choices, not tuned against real operator behavior - consistent with every other threshold in this codebase.",
        "The Undo banner (Phase 6.2) disappears once its 8-second window elapses even if the operator hasn't looked at the screen yet - there is no persistent action log to recover a missed Undo window from; the Presentation card's own Stop button remains available manually regardless.",
        "6 more ergonomics gaps from Phase 6's own audit remain unaddressed after Phase 6.2 (feed search, error visibility, error-banner context, onboarding, Diagnostics Mode density, unified-queue Edit support) - each a candidate for a future Phase 6.x slice.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the Display confirm/undo mechanism has never been exercised against a real misclick on a real live projector - see physicalHardwareStatement.",
        "Feed search (Phase 6.3) matches only summary/detailLine text, not domain or status - those are already covered by the existing filter chips, so searching them too would duplicate an existing control.",
        "Feed search is substring matching only, with no fuzzy/typo-tolerant matching - a misspelled search term will not match.",
        "The feed search box has no debounce - filtering re-runs on every keystroke via useMemo, acceptable given the feed is capped at 50 items (MAX_VISIBLE_INTELLIGENCE_ITEMS); this could need revisiting if that cap ever grows substantially.",
        "5 more ergonomics gaps from Phase 6's own audit remain unaddressed after Phase 6.3 (error visibility, error-banner dismiss/context, onboarding, Diagnostics Mode density, unified-queue Edit support) - each a candidate for a future Phase 6.x slice.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and feed search has never been exercised against a real live feed with more than a handful of items - see physicalHardwareStatement.",
        "The real audio/speech error text (Phase 6.4) is an internal error message, not a rewritten operator-facing remedy - e.g. 'no audio device available' says what's wrong, not necessarily the exact fix. The existing 'retry below'/'will clear on the next successful chunk' phrasing still carries the guidance; this phase only adds the diagnostic detail alongside it.",
        "audio_error_text/speech_error_text (Phase 6.4) reflect the same process-lifetime persistence their underlying state fields already had - a currently-Error status shows whatever text is currently set, never a stale one (guarded by error_text_if), but the fields themselves are not time-limited or auto-expired independent of that guard.",
        "4 more ergonomics gaps from Phase 6's own audit remain unaddressed after Phase 6.4 (error-banner dismiss/context, onboarding, Diagnostics Mode density, unified-queue Edit support) - each a candidate for a future Phase 6.x slice.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the real-error-text surfacing has never been exercised against a genuine audio/speech failure - see physicalHardwareStatement.",
        "humanizeBusyKey (Phase 6.5) is mechanical, not a curated label set - for keys embedding non-UUID text (e.g. a Bible reference in search-preview-${reference}), the label reads literally ('Search preview ROM 8:28') rather than a hand-tuned phrase. Readable, not polished English - a deliberate tradeoff to avoid a per-call-site lookup table for ~40+ actions.",
        "Dismissing the error banner (Phase 6.5) does not retry the failed action - it only clears the message; the operator must re-trigger the original action themselves if they still want it to happen.",
        "3 more ergonomics gaps from Phase 6's own audit remain unaddressed after Phase 6.5 (onboarding, Diagnostics Mode density, unified-queue Edit support) - each a candidate for a future Phase 6.x slice.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the error banner dismiss/context change has never been exercised against a real command failure - see physicalHardwareStatement.",
        "The onboarding walkthrough (Phase 6.6) is a fixed 4-step summary, not interactive - it does not highlight the real UI elements it describes, and it cannot be reopened once dismissed short of clearing browser storage. A 'replay walkthrough' affordance is a reasonable future addition, not attempted this phase.",
        "The setup notice (Phase 6.6) covers exactly two items (Bible dataset, Whisper model) - it does not attempt a full readiness score covering displays, audio device selection, or database health; those already have their own always-visible signal (SystemStatusStrip's Display item, or PilotDiagnosticsPanel for full detail) and were judged not to need a second, redundant pointer.",
        "localStorage persistence for the onboarding flag (Phase 6.6) is per-browser-profile, not per-install - clearing the Tauri webview's storage will show the walkthrough again. This is the expected, accepted behavior for a 'shown once' affordance with no server-side account to persist against.",
        "All 8 ergonomics gaps from Phase 6's own original audit are now scoped - onboarding was the last unscoped one. 2 remain unaddressed after Phase 6.6 (Diagnostics Mode density, unified-queue Edit support) - each a candidate for a future Phase 6.x slice.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the onboarding walkthrough/setup notice have never been exercised on a real first launch - see physicalHardwareStatement.",
        "Collapsed state (Phase 6.7) is not persisted - every Diagnostics Mode panel resets to its default (open for Pending Suggestions/Service Timeline, closed for the other 9) on every app restart. No localStorage or other per-panel memory was added - a deliberate scope boundary, consistent with every other session-only UI state in this codebase.",
        "No 'collapse all'/'expand all' control in Diagnostics Mode (Phase 6.7) - a technician wanting to see everything at once (or nothing) must still click each of the 9 panels individually. A reasonable future addition, not attempted this phase.",
        "The choice of which 2 panels stay open by default (Pending Suggestions, Service Timeline) is fixed, not operator-configurable (Phase 6.7) - a technician who cares most about a different panel must still expand it every time Diagnostics Mode is entered fresh.",
        "1 more ergonomics gap from Phase 6's own audit remains unaddressed after Phase 6.7: unified-queue Edit support - a candidate for a future Phase 6.x slice.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the Diagnostics Mode collapse behavior has never been exercised on real Windows hardware - see physicalHardwareStatement.",
        "Only the bible domain has Edit (Phase 6.8) - music/sermon/content/correlation findings have no equivalent free-text-edit command to wire up; this mirrors Diagnostics Mode's own existing scope exactly, not a new boundary this phase introduces.",
        "The 'E' keyboard shortcut is not extended to the unified queue (Phase 6.8) - resolveUnifiedShortcutAction only resolves 'A'/'R' positionally; Diagnostics Mode's own 'E'/'P' shortcuts remain Diagnostics-Mode-only, same as before this phase. A future slice could add a third positional key if the operator asks for it.",
        "Editing a non-scripture-kind bible suggestion (e.g. a paraphrase match) through the new unified-queue Edit button (Phase 6.8) will fail the same way it already does in Diagnostics Mode - edit_suggestion always parses the typed text as a book/chapter/verse reference; this is pre-existing behavior, not a new gap.",
        "All 8 ergonomics gaps from Phase 6's own original audit are now closed. No further Phase 6.x slices are planned unless a new gap is found.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the unified-queue Edit flow has never been exercised on real Windows hardware - see physicalHardwareStatement.",
        "No real recorded audio exists anywhere in this repository or container (Phase 7.1) - the fingerprinting algorithm is proven correct on synthetic multi-tone signals only. Its real-world false-positive/false-negative rate against actual congregational singing, live instrumentation, and room acoustics is genuinely unknown until Environment C testing with real reference recordings happens - this is this phase's own central, most consequential limitation.",
        "No enrollment UI (Phase 7.1) - creating acoustic-model.json and gathering reference WAV files is a manual, file-system-level operator task; a future phase could add an in-app 'record/import a reference clip for this song' flow the way Bible/Whisper model installation already has a native file picker (Phase 3.8.7.1). Deliberately not attempted here to keep this phase's scope to 'does the algorithm work.'",
        "The fingerprint manifest has no schema-version field (Phase 7.1) - a genuinely future concern only if the fingerprint hash packing (window size/hop size/band edges) ever changes, since that would invalidate every previously-enrolled fingerprint; documented in fingerprint.rs's own module docs rather than solved preemptively for a version-1 format.",
        "MIN_VOTES (the minimum landmark-hash agreement threshold, Phase 7.1) is a fixed constant, not operator-tunable - chosen conservatively from the synthetic test suite's own vote counts; real-world audio's actual vote distribution may call for tuning once Environment C data exists. Not made configurable preemptively, matching this project's 'don't add a tunable before real evidence says it's needed' discipline.",
        "Enrollment reads the entire reference WAV file into memory at once (Phase 7.1) - fine for hymn/song-length clips (a few minutes), not designed for hour-long source files; no such use case exists in this project's scope.",
        "The other four master-architecture gaps docs/phase-7-audit.md identified (multi-language support, church/user roles & permissions, internet/hybrid intelligence, OBS/vMix/livestream integration) remain open - still the user's choice for a future Phase 7.x or later top-level phase.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the real audio fingerprinting recognizer has never been enrolled or queried against real recorded audio anywhere, in this container or otherwise - see physicalHardwareStatement.",
        "Removing an enrollment is not supported (Phase 7.2) - an operator can fix a bad enrollment by re-enrolling the same songId (upsert replaces it), but there is no dedicated 'remove' command or button. A small, clean follow-up, not attempted here to keep this phase's diff focused.",
        "No live re-recording from within the app (Phase 7.2) - enrollment imports an existing WAV file; it does not capture audio live via cpal. Matches every other model-provisioning flow in this codebase (Whisper, embedding model/tokenizer all import an existing file too).",
        "Still no real recorded audio anywhere in this repository or container (Phase 7.2) - Phase 7.1's central limitation is unchanged; this phase only makes it easier for an operator to supply real audio themselves.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the new enrollment UI has never been exercised by a real operator picking a real WAV file - see physicalHardwareStatement.",
        "No confirmation prompt before removing (Phase 7.3) - the Remove button acts immediately, matching every other operator-decision button in this panel (Accept/Reject on findings).",
        "Still no real recorded audio anywhere in this repository or container (Phase 7.3) - the same central limitation carried forward since Phase 7.1; this phase only completes the enrollment CRUD lifecycle, it does not supply any real audio to exercise it against.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the new Remove button has never been exercised by a real operator against a real enrollment - see physicalHardwareStatement.",
        "Production Integration (Phase 8) pushes plain text only - the currently-displayed slide's heading/body/footer, newline-joined - into one operator-named OBS text source or vMix input/field. No scene switching, no source-visibility toggling, no recording/streaming start-stop control, and no NDI output; all deliberate scope boundaries, not gaps - see docs/phase-8-audit.md.",
        "Production Integration's OBS/vMix configuration is in-memory only (AppState, Mutex-guarded) and does not persist across an app restart - an operator must re-enter host/port/credentials and re-enable each target every session, matching this phase's own deliberate 'live-editable, no restart required to change it' design choice, at the cost of not surviving a restart.",
        "vMix's own SetText HTTP API returns a bare 200 OK regardless of whether the named Input or SelectedName field actually exists in the running vMix instance - a documented property of vMix's own API, not a limitation of this client. An operator who misspells an input name will see 'success' in CIP with no visible change in vMix.",
        "Neither OBS nor vMix is installed in this container, and this exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware - Phase 8's obs-websocket and vMix HTTP clients have only ever been exercised against real, protocol-correct in-process test servers (Environment A), never against the real applications (Environment C) - see physicalHardwareStatement item 17.",
        "Bible Translation Registry v2 (Phase 9) enforces only ai_processing_allowed - the other seven UsagePermissions fields (distribution_allowed, offline_storage_allowed, projection_allowed, api_allowed, commercial_allowed, llm_prompt_allowed, training_allowed) are recorded and queryable via UsagePermissions::permits_* but gate nothing yet, since no other call path in this codebase currently needs to restrict them. A deliberate, honestly-scoped first enforcement point, not a claim that the other seven are enforced somewhere non-obvious.",
        "Phase 9's UsagePermissions has no operator-facing UI to view or edit after import - today it is set only via TranslationInput.usage at import time (or left fully unset, e.g. the KJV dev-fixture and the pre-Phase-9 dev seed path). An operator cannot today discover, from within the app, why generate_verse_embeddings might refuse to run for a translation lacking this evidence.",
        "Phase 9 adds zero new Bible text and pursues zero real licensing agreements - docs/bible-translation-licensing-roadmap.md names the routes (YouVersion Platform, API.Bible, direct publisher licensing for NIV/NKJV/NLT/NASB/ESV, a Bible Society of Nigeria partnership for Yoruba/Igbo/Hausa) but none has been contacted, registered with, or licensed from - all remain Unknown in the registry and refuse import per LicensingStatus's existing gate, unchanged by this phase.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and the ai_processing_allowed gate has never been exercised by a real operator clicking Generate Verse Embeddings against a real production database - see physicalHardwareStatement item 18.",
        "Phase 10 (Church/User Roles & Permissions) adds only two closed roles (Admin/Operator) - a third role (e.g. a read-only Viewer) is a reasonable future addition, not designed here, since nothing in this codebase needs it yet.",
        "Phase 10 has no password recovery - an Admin locked out of their PIN has no in-app recovery path. No account editing, deactivation, or deletion - list/create only; an operator who has left the team cannot be removed from the account list today.",
        "Phase 10's ensure_admin gates cover exactly seven existing commands judged to be configuration rather than day-to-day operation; audit_events/timeline record what happened but not which operator triggered it - per-action audit attribution would need current_operator threaded into every existing write path, a real, separate, future addition deliberately not attempted here to keep this phase's diff focused on the access-control mechanism itself.",
        "Phase 10's LoginScreen is a workflow control, not itself the security boundary - the real boundary is the seven ensure_admin calls inside the Rust commands; a determined attacker able to invoke Tauri commands directly, outside this app's own UI, is not what the login screen defends against.",
        "The LoginScreen UI was not visually verified via a real Xvfb+screenshot pass in this phase - full Rust/TS regression (including every access-control code path: bootstrap, both gate outcomes, PIN verification success/failure) is green, but a real rendered screenshot of the login/account-creation forms was not captured. Judged proportionate since it is ordinary React form content, not a new windowing/pixel-placement mechanism, but stated honestly rather than silently implied verified.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and Phase 10's own operator-account bootstrap/login/Admin-gate flow has never been exercised by a real operator against a real production build - see physicalHardwareStatement item 19.",
        "Phase 11 (Local Congregant Companion View) has no authentication, no rate limiting, and no connection cap on the companion server itself - a deliberate, disclosed trade-off for a LAN-only, read-only, no-PII surface; anyone on the same LAN can open the page while it's running, the same thing anyone in the room can already see on the projector.",
        "Phase 11's companion server has no QR code generation and no mDNS/Bonjour auto-discovery - the operator reads the address off the desktop panel and shares it manually (a slide, a spoken announcement).",
        "Phase 11's companion page polls every two seconds rather than pushing updates - a missed poll is at most a two-second lag, never a stuck stale state, but it is not instantaneous.",
        "Phase 11's companion server shows only the current presentation item, matching Stage's own single-item model - no history of what was previously displayed.",
        "Phase 11's companion server and LAN-address detection are IPv4 only - an IPv6-only network would not reach it.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and Phase 11's own companion server has never been enabled by a real operator nor opened from a real phone browser on a real church LAN - see physicalHardwareStatement item 20.",
        "Phase 12 (Multi-language Whisper) offers no Igbo option anywhere - whisper.cpp's own 100-language vocabulary (verified directly against its vendored source) has no Igbo entry at all, a hard limitation of the Whisper model architecture itself, not a missing CIP configuration option. A future phase could revisit this only if Whisper itself gains Igbo support upstream, or if CIP ever adopts a different STT backend with real Igbo training data.",
        "Phase 12's language selection has no per-service history and does not lock onto a detected language across inference windows - Auto-detect re-detects independently every ~3-second window, so a brief false detection during a pause or noise is possible and self-corrects on the next window rather than being remembered.",
        "Phase 12 does not translate - whisper.cpp's own translate flag is untouched; a Yoruba or Hausa sermon is transcribed in that language, never machine-translated to English text.",
        "Phase 12's language selection does not change core/bible's own reference-detection logic, which remains English-text-oriented - a correctly-transcribed Yoruba or Hausa sermon would still need an English (or otherwise-matching) spoken reference for CIP's Bible-reference detection to fire.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and Phase 12's own language selection has never been exercised against real Yoruba or Hausa speech by a real operator - see physicalHardwareStatement item 21.",
        "Phase 13's Church Knowledge Base only includes findings an operator explicitly accepted - a Detected or Reviewed (but never accepted) finding, however confident, never enters saved_sermon_findings and never appears in the knowledge base. This is deliberate, not an oversight - see docs/phase-13-audit.md's persistence-justification precedent - but it does mean a church that never explicitly accepts Sermon Intelligence findings during a live service will see an empty Church Knowledge Base regardless of how much was actually said.",
        "Phase 13 does not backfill history - findings from services that happened before this phase shipped are gone; they were never durable in the first place, so this phase cannot honestly recover what was never saved.",
        "Phase 13's theme grouping is exact-label match only - 'Waiting on God' and 'On Waiting for God' are two separate entries, not merged. No semantic similarity is attempted in this phase.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and Phase 13's own Church Knowledge Base has never been exercised by a real operator accepting a real finding and confirming it survives a restart - see physicalHardwareStatement item 22.",
        "No audio gain normalization/AGC was added in Phase 14 (deliberately deferred, not an oversight) - a genuinely quiet microphone still produces a quiet, lower-confidence transcript; this phase makes that honestly visible (LOW SIGNAL, a real low confidence score, a visible placeholder-discard counter) rather than fixing the underlying audio level.",
        "Phase 14's non-speech placeholder list (NON_SPEECH_PLACEHOLDERS in ai/speech/src/whisper.rs) is a documented, not exhaustive, set - whisper.cpp can in principle emit other bracketed captions this list does not yet include; new ones observed in a real session should be added the same way these were, from direct evidence.",
        "Phase 14's placeholder filter does not catch garbled-but-real-looking hallucinated phrases that are not one of whisper.cpp's known fixed captions - the real confidence score now at least reports these honestly lower than clean speech, but does not remove them from the transcript; a confidence-threshold cutoff was deliberately not added without further real operator feedback on what threshold is actually useful.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and Phase 14's own hallucination-filtering fix has not yet been re-verified against a second real pilot session confirming the Live Transcript panel no longer shows placeholder captions and that the confidence badge genuinely varies - see physicalHardwareStatement item 23.",
        "Phase 15's fuller-context retry cannot catch a paraphrase sharing almost no vocabulary with its verse (a 'conceptual' paraphrase) - that remains the semantic fallback's job, which still requires an operator-provisioned embedding model this project has never verified against real hardware.",
        "Phase 15's stall indicator explains that nothing new has been transcribed recently and offers the most likely honest reason (a quiet/unclear microphone signal) - it cannot distinguish a genuinely stopped listening session from a long real pause in speech with any more precision than the underlying transcript_freshness signal already can.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and Phase 15's own transcript-stall indicator and fuller-context paraphrase retry have not yet been re-verified against a second real pilot session on the same or similar hardware - see physicalHardwareStatement item 24.",
        "The Show/Hide preference is a pure client-side localStorage value - it is per-browser-profile/per-install, not synced anywhere, consistent with every other UI preference in this project (e.g. the onboarding-seen marker). Two operators running CIP on two different machines for the same service would each need to set their own preference independently.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and this phase's own Show/Hide toggle has not yet been exercised by a real operator during a real service - see physicalHardwareStatement item 25.",
        "Detection Accuracy Analytics is scoped to Bible detection only - Music/Sermon/Content route through IntelligenceFinding, which never durably records a rejected item outside their own accepted-only saved_sermon_findings/saved_content_candidates tables, so no honest rejection-rate can be computed for those domains today. See docs/phase-17-detection-accuracy-analytics.md.",
        "The dashboard is only as informative as the operator behavior behind it - it will read 'Nothing decided yet' until a real operator has approved, edited, or rejected suggestions during real services; this container has no such history to show yet.",
        "The detection-method correlation (byDetectionKind) is best-effort: a suggestion with no originating transcript segment (e.g. a manual context correction) is counted in overall/byConfidenceLevel but reported honestly in unmatchedDetectionKindCount rather than guessed into a method it wasn't actually observed in.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and this phase's own Detection Accuracy dashboard has not yet been reviewed by a real operator against a real service history - see physicalHardwareStatement item 26.",
        "This phase does not and cannot fix the underlying Windows audio-routing issue behind the operator's own 'No sound was detected' report - it only tells the operator where to look (the device's own Windows recording level, and output routing). Whether Stereo Mix ever actually captures audio on that specific machine remains outside CIP's control and unverified by this phase.",
        "The loopback device-name pattern list (stereo mix, wave out mix, what u hear, loopback, monitor of) is a best-effort match against known real Windows loopback device names, not exhaustive - an unusual or renamed loopback device would still get the generic physical-microphone-oriented message.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and this phase's own loopback-aware guidance message has not yet been seen rendered by a real operator on real Windows hardware - see physicalHardwareStatement item 27.",
        "Phase 19's ordinal normalization closes one narrow, real gap (digit-suffix ordinal book prefixes) - it does not move the needle on the operator's own audit's biggest identified contributors to missed detection: Whisper's fixed 3s non-overlapping window with no VAD-aligned flush, zero fuzzy/near-miss tolerance for mispronounced or partially-heard book names, and uncalibrated fallback confidence thresholds. Those remain open, explicitly deferred in docs/phase-19-audit.md with reasoning, not silently dropped - fuzzy book-name matching is the leading candidate for a future phase.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and this phase's own ordinal normalization has not yet been exercised by a real operator saying a digit-suffix-ordinal book reference during a real service - see physicalHardwareStatement item 28.",
        "Fuzzy book-name matching only covers the 48 single-word canonical book names - the 18 multi-word/numbered books (1/2/3 Samuel/Kings/Chronicles/Corinthians/Thessalonians/Timothy/Peter/John, Song of Solomon) remain reachable only through the existing exact alias table, not this phase's fuzzy matcher. This is a deliberate, documented scope boundary (see docs/phase-20-audit.md), not an oversight.",
        "The fuzzy-match similarity threshold/edit-distance budget is documented reasoning, not empirically calibrated against real Whisper mishearing data - no labeled real-service audio-to-transcript corpus exists in this repository to calibrate against yet, the same honest limitation every confidence threshold in this codebase (paraphrase, semantic, now fuzzy-book) carries.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and neither this phase's own fuzzy book-name matching nor its corrected manual context-correction field has yet been exercised by a real operator during a real service - see physicalHardwareStatement item 29.",
        "Audio-overlapping windows (the other half of the operator's own 'overlapping windows + VAD' request) are not implemented - a word spoken with genuinely no pause anywhere near the hard 3s cap can still be cut, exactly as before Phase 21. This is a deliberate, documented scope boundary (see docs/phase-21-audit.md's design-decision section on the duplicate-transcription risk that technique carries), not an oversight.",
        "Phase 21's 1.5s minimum / 0.4s trailing-pause thresholds are documented reasoning, not empirically calibrated against real speech-timing data - no labeled real-service audio exists in this repository to calibrate against yet, the same honest limitation every other threshold in this codebase carries.",
        "This exact rebuilt artifact has NOT yet been installed or launched on real Windows hardware, and this phase's own VAD-triggered flush has not yet been exercised by a real operator during a real service - see physicalHardwareStatement item 30.",
        "Phase 22: classify_model_size_tier is a heuristic, never a certainty - a heavily quantized large-class model file (q5_0/q5_1/q8_0) can land in a smaller tier's byte range than an unquantized model of that same smaller tier, so sizeTierHint can only narrow down what is installed, never prove it.",
        "Phase 22: true concurrent dual-tier Whisper inference (a fast low-latency detector model plus a separate high-quality transcript model, running at the same time) is NOT implemented - this is exactly what the operator asked for as their own stated model strategy. CIP holds exactly one active WhisperSpeechEngine/WhisperContext for the process's life; building a second context, an output-arbitration policy, and validating the reconciliation needs real microphone audio this container does not have. The single-model base.en/small.en/large-v3-turbo recommendation is the real, available lever today; the dual-tier architecture is explicitly deferred future work, not silently omitted.",
        "Phase 22: the claim that a larger Whisper model improves detection accuracy on Nigerian-accented English rests on whisper.cpp's own published model comparisons, not a measurement taken in this container - no real microphone audio or real model weights are available here (the standard model host is blocked by this environment's egress policy, as documented since Phase 1.2). The decisive test is a real operator installing a larger model and confirming the improvement on a real service - see physicalHardwareStatement item 31.",
        "Phase 24.1: a failed get_prepared_item_slide fetch is silently swallowed - the affected queue card falls back to the prior raw-text approximation rather than showing an error. This is a display convenience, not a safety-relevant path: the actual Display click still sends the item's real persisted content regardless of whether its thumbnail rendered correctly.",
        "Phase 24.1: this exact change has not been confirmed on real Windows hardware running the actual compiled desktop app - verification used a mocked Tauri bridge in headless Chromium, not the real WebView2 renderer real operator hardware uses. See physicalHardwareStatement item 32.",
        "Phase 24.2: a mid-buffer interim decode has less audio context than the eventual final decode of the complete window, so an interim segment's text can legitimately differ from (and is sometimes less accurate than) the final segment that later replaces it. This is inherent to decoding a shorter prefix, not a defect - an interim segment is always superseded, never treated as authoritative by anything downstream.",
        "Phase 24.2: interim decoding roughly doubles worst-case per-window inference cost (one interim pass plus the eventual final pass) for any window that runs at least 1.5s without a detected pause - bounded (never more than 2x, never unbounded) but a real, measurable CPU cost on slower hardware.",
        "Phase 24.2: audio-overlapping windows (the other half of the operator's own original \"overlapping windows + VAD\" request) remain explicitly not implemented - this phase closes only the interim-transcript half; see docs/phase-24-2-audit.md for why that piece is harder (it needs real, timing-sensitive audio this environment cannot obtain to validate safely).",
        "Phase 24.2: this exact change has not been confirmed on real Windows hardware running the actual compiled desktop app with a real Whisper model and real microphone audio - verification used a mocked Tauri event bridge in headless Chromium, proving the frontend rendering and event plumbing are correct, not that whisper.cpp's own mid-sentence decode quality is good enough to be useful on a real, accented voice. See physicalHardwareStatement item 33.",
        "Phase 24.3: true dual-tier Whisper (a second, optional quality-tier speech model) has never run against real microphone audio with two real model files installed on real hardware - everything this phase proves is that the code compiles, typechecks, and its pure/persistence logic is correct, not that the quality tier's corrections are actually useful or that running two models concurrently is tolerable on modest pilot hardware.",
        "Phase 24.3: a second loaded Whisper model is a real, ongoing resource cost (roughly double the RAM of whichever two model sizes are configured, plus the quality worker's own CPU) for the entire lifetime of a listening session once an operator installs one - entirely opt-in (zero cost with no quality model installed), but a real cost once one is.",
        "Phase 24.3: the quality worker can fall behind and silently drop jobs under load (by design, so a slower model never blocks the fast tier) - jobsDroppedBacklog in diagnostics is the only signal, with no UI distinction between 'hardware too slow for this model' and 'transient spike'.",
        "Phase 24.3: no history/replay view of past corrections exists yet - transcript_corrections persists the link durably, but the live 'corrected below' badge only exists in LiveChurchBrain's in-memory state for that session; reviewing history after a service shows the corrected segment's own text among the ordinary transcript with no visual indication it was a correction.",
        "Phase 24.3.1: fixes the quality-tier language-conditioning gap (transcribe_once now accepts a language_hint from the fast tier's own real per-window detection) but has, like the rest of the Phase 24.3 dual-tier pipeline, never been confirmed against real microphone audio in a non-English/auto-detected language with two real models installed on real hardware.",
        "Phase 24.3.2: adds consecutive_jobs_dropped and a derived backlogState label (normal/busy/falling_behind/overloaded) so an operator can distinguish a transient drop from sustained backlog - a diagnostics-visibility fix only; it does not change the quality worker's own backpressure behavior and, like the rest of Phase 24.3, has never been confirmed against real microphone audio with two real models installed on real hardware."
    ],
    "paraphraseDetectionProof": {
        "trigger": "User selected 'Semantic/paraphrase Bible detection (Recommended)' as the first Phase 4 target after docs/phase-4-master-plan-gap-audit.md graded it NOT STARTED against the CIP Master Architecture v1.0 document's Section 8.4 worked example.",
        "scopeDecision": "The gap audit itself assumed an embedding model + vector search would be required. Phase 4.1's own architecture audit re-confirmed no embedding/vector library (candle/onnxruntime/hnsw/faiss/usearch/tantivy) exists anywhere in Cargo.lock and no model host is reachable from this container. Rather than fabricate a 'semantic' system that is secretly still string matching, this phase built and honestly labeled the lexical/keyword-overlap slice of the gap - exactly the master plan's own Section 8.4 example (a near-quotation paraphrase) - leaving the harder 'conceptual references' tier (little/no shared vocabulary) as a documented, not-yet-started gap.",
        "fix": "New core/bible::paraphrase module (significant_words/stem/score_overlap - pure, deterministic, dependency-free). New ReferenceKind::Paraphrase variant. New BibleProvider::find_similar_verses default trait method built entirely from the existing search() method (zero migration, every implementation gets it for free). New try_paraphrase fallback in process_transcript_segment, gated on no suggestion already produced, min 4 significant words, min 0.75 overlap score - never mutates active Scripture context, produces only a Pending suggestion like every other detection kind.",
        "dedupFix": "Wiring this in surfaced a real interaction the existing regression suite caught: the pre-existing suggestion-dedup window would silently suppress an explicit citation if a Paraphrase guess for the same verse had already fired moments earlier (the pastor paraphrases a verse, then reads it directly). Fixed with a new DetectionCategory (Explicit vs Paraphrase) so dedup only suppresses a repeat within the same category, never across categories in either direction.",
        "regressionProof": "Backend: cargo fmt --check clean, cargo clippy --workspace --all-targets -- -D warnings clean (default and --features whisper), cargo test --workspace and cargo test --workspace --features whisper both fully green (zero failures across every crate). Frontend: tsc -b 0 errors, oxlint 0 errors (same pre-existing warning pattern), vitest 212/212 passed (+1 new domain-contract test), vite build clean.",
        "performanceMeasurement": "Timed against the real production BSB dataset (31,086 verses) in an unoptimized debug build: 28-329ms per segment, only ever paid when nothing else in the segment already produced a suggestion. See pilot-evidence/4.1/build/backend-frontend-regression.json for the full per-segment table.",
        "offlineDependencyProof": "No network call, no model download, no new crate dependency anywhere in this phase's diff - core/bible::paraphrase imports only std::collections::HashSet."
    },
    "liveBibleDetectionPerformanceProof": {
        "trigger": "Operator reported that on real Windows hardware, live detection produced nothing across every domain except Sermon Intelligence, with System Diagnostics showing avg Whisper inference 14,518ms/chunk (worse than 4x real-time), max 19,023ms, 51 overload events, 742,044ms of audio discarded. Cross-referenced against docs/phase-3-8-7-7-audit.md (avg 14,991ms, same apparent machine profile, Environment C never confirmed) - a known, previously-flagged hardware/build limitation, not a Phase 4.1 regression. Operator's explicit direction: fix real-time Bible detection performance directly (not reroute to Service Replay), plus add a Bible-search book-name prefix filter.",
        "rootCause": "whisper.cpp's ggml CMake build forces GGML_NATIVE_DEFAULT=OFF whenever CMAKE_CROSSCOMPILING is TRUE (always true for this project's Linux-to-Windows builds), which defaults GGML_AVX/GGML_AVX2/GGML_FMA/GGML_F16C all OFF - every Windows build this project has shipped only ever used the SSE4.2 baseline for Whisper inference.",
        "fix": "New scripts/whisper-windows-simd.cmake toolchain file force-sets GGML_AVX/AVX2/FMA/F16C ON via CMake cache variables (the only mechanism that fits whisper-rs-sys's existing WHISPER_/CMAKE_-prefixed env-var forwarding contract). Its introduction was found, via direct A/B rebuild comparison, to silently suppress the `cmake` Rust crate's own auto-detection of CMAKE_SYSTEM_NAME/CMAKE_SYSTEM_PROCESSOR and its compiler selection (both gated behind 'if CMAKE_TOOLCHAIN_FILE is not already defined' in cmake-rs's own source) - the toolchain file now also restores both explicitly. ai/speech/src/whisper.rs additionally sets an explicit thread count (available_parallelism, capped at 8) instead of whisper.cpp's own min(4, cores) default.",
        "directBinaryEvidence": "x86_64-w64-mingw32-nm against cip-desktop.exe extracted from this exact installer finds ggml_cpu_has_avx2/fma/f16c resolved as defined symbols (previously undefined-reference link failures); x86_64-w64-mingw32-objdump -d finds genuine AVX/AVX2 YMM-register instructions compiled into the binary - see pilot-evidence/4.2/.",
        "bibleSearchFilter": "New filterBooksByPrefix helper (apps/desktop/src/lib/libraryHelpers.ts) filters the already-loaded, provider-validated Bible book list client-side by display-name prefix, case-insensitive, wired into the Bible Library's Browse tab. Chapter/verse listing was already fully built in Phase 3.6 - no backend change needed for that part of the request.",
        "regressionResult": "cargo fmt/clippy (default and --features whisper) clean; cargo test --workspace: every crate green, zero failures; cip-ai-speech --features whisper: 7/7 passed (unchanged); frontend tsc/oxlint/vitest (218/218, 6 new)/build all clean.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware re-test: does inference latency meaningfully improve, does live Bible detection now produce results, does the new search filter behave as expected."
    },
    "instantBibleDetectionProof": {
        "trigger": "Operator shared two competitor-product ('Pewbeam') videos showing near-instant live Bible reference detection and said CIP should be better. Frame extraction (ffmpeg, not assumed) confirmed the videos show the competitor, not CIP, reading references explicitly and briefly. Root-caused CIP's own gap to a second, previously undiscovered latency source beyond Phase 4.2's SIMD inference fix.",
        "rootCause": "Phase 3.8.7.5's TranscriptSegmenter concatenates raw ~3s Whisper segments into a bounded 12-20 second logical window before Bible detection ever runs on the text - independent of, and larger than, inference speed itself.",
        "fix": "New finalize_bible_only (apps/desktop/src-tauri/src/commands.rs) runs the existing, unmodified handle_final_transcript Bible Intelligence Core pipeline on each raw, already-final ~3s Whisper segment immediately, persisting its own transcript_segments row (satisfying the real, foreign_keys=ON-enforced FK scripture_detections/ai_suggestions depend on) and emitting TranscriptUpdated - the Live Transcript panel now updates roughly every ~3s instead of every ~15-20s. The 12-20s batch (finalize_and_route_segment) no longer re-runs Bible detection - only Sermon/Service/Music-text routing, which genuinely needs full sentences, unchanged.",
        "freeLocalModelConstraint": "Per the operator's explicit instruction, no cloud/subscription ASR or LLM service (Deepgram, AssemblyAI, Google/Azure Speech, Gemini Live, Groq-hosted models, GPT-4o-mini, Claude Haiku) was evaluated or added - whisper.cpp remains the only speech engine; this phase changes when CIP acts on its output, not what produces it.",
        "directBinaryEvidence": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer finds the literal function name 'finalize_bible_only' in the binary's string table, and the trimmed finalize_and_route_segment's mangled symbol resolves correctly - see pilot-evidence/4.3/.",
        "regressionResult": "cargo fmt/check/clippy -p cip-desktop: clean. cargo test -p cip-desktop: 280 passed, 0 failed (pipeline.rs/segmentation.rs - the modules containing the actual detection/dedup/batching logic - are byte-for-byte unchanged this phase).",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware re-test: does live Bible detection now surface a reference within a few seconds, matching the competitor demo's feel."
    },
    "semanticBibleSearchProof": {
        "trigger": "The user's own explicit free-local-model constraint, restated after the Phase 4.3 competitive analysis, plus docs/phase-4-master-plan-gap-audit.md grading 'Conceptual/semantic Scripture matching' NOT STARTED and Phase 4.1's own documented boundary (lexical overlap cannot find a paraphrase sharing little/no vocabulary with its source verse). User confirmed the model choice explicitly: 'Yes, proceed with all-MiniLM-L6-v2.'",
        "architectureDecision": "candle (pure-Rust ML framework) chosen over ONNX Runtime after a real, disposable smoke-compile test confirmed candle/tokenizers cross-compile cleanly to x86_64-pc-windows-gnu with no CMake/C++ build step of its own - this real Windows rebuild confirmed the finding held: zero new toolchain fixes were needed for the semantic-search feature, unlike whisper-rs-sys's own multi-fix cross-compile history.",
        "fix": "New EmbeddingEngine trait (core/ai, mirrors SpeechEngine). New core/bible::semantic module (cosine_similarity, VerseEmbeddingStore trait, best_semantic_match). New ReferenceKind::Semantic. New ai/embeddings crate additions: NullEmbeddingEngine (default) + CandleEmbeddingEngine (real local backend, feature-gated) + pure pooling module. New core/service::try_semantic fallback, reached only via a new process_transcript_segment_with_semantic_search entry point - the existing process_transcript_segment is completely untouched (additive, not breaking). New database migration 0013 (bible_verse_embeddings, keyed by verse_id + model_id so a model switch never mixes vectors from two different models). New apps/desktop wiring: embedding model/tokenizer provisioning config (mirrors whisper_model_path exactly), SqliteVerseEmbeddingStore on its own dedicated connection, generate_verse_embeddings command, and live wiring into finalize_bible_only/process_test_transcript whenever an embedding model is actually loaded.",
        "concurrencyFix": "rusqlite::Connection is not Sync, but VerseEmbeddingStore requires Send + Sync (a trait-object bound the live pipeline needs). An initial design borrowing the live pipeline's already-locked connection failed to compile with a direct E0277 error during this phase's own verification pass, caught before it ever reached a real build. Fixed by giving SqliteVerseEmbeddingStore its own dedicated connection, mirroring SqliteBibleProvider's exact shape and AppState's established 'every independent read path gets its own connection' discipline - see semantic-search-evidence.json for the full reasoning.",
        "neverOverridesExistingDetection": "try_semantic is attempted only after both an explicit citation AND the lexical paraphrase fallback have already failed - verified by dedicated tests (an_explicit_citation_is_never_second_guessed_by_the_semantic_fallback, the_plain_entry_point_never_attempts_the_semantic_fallback) and never mutates the active Scripture context (semantic_detection_never_mutates_the_active_context) - the same never-override discipline Phase 4.1's paraphrase fallback already established.",
        "freeLocalModelConstraint": "Per the operator's explicit instruction, no cloud embedding API (OpenAI, Cohere, Voyage) or subscription of any kind was evaluated or added - every candidate considered was local-only from the start.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer finds: the mangled trait-impl symbols for CandleEmbeddingEngine's is_ready/model_id/dimensions/embed and its load constructor; the SqliteVerseEmbeddingStore-as-VerseEmbeddingStore impl; generate_verse_embeddings_for_translation and the generate_verse_embeddings command; drop-glue for the new EmbeddingCapabilities/EmbeddingGenerationSummary structs; the literal string 'all-MiniLM-L6-v2'; the embedded bible_verse_embeddings migration SQL; and all four new command names (get_embedding_capabilities, install_embedding_model_file, install_embedding_tokenizer_file, generate_verse_embeddings) in the invoke handler's registered command-name table. Prior-phase symbols confirmed unaffected: finalize_bible_only (1 occurrence, Phase 4.3), whisper_full_with_state (96 occurrences, identical to the Phase 4.3 baseline), ggml_cpu_has_avx2/fma/f16c still resolved as defined symbols (Phase 4.2). See pilot-evidence/4.4/.",
        "regressionResult": "cargo fmt --check clean. cargo clippy --workspace --all-targets -- -D warnings clean under BOTH default features and --features whisper,semantic-search together. cargo test --workspace: every crate green under both feature configurations, zero failures. cip-desktop alone: 289 passed (up from 280 at the Phase 4.3 baseline).",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact, and cannot be verified in this container at all: this environment's egress policy blocks the standard model host, so no real model.safetensors/tokenizer.json pair has ever been loaded or run anywhere in this project's history. The decisive pending gate is the operator's own real-hardware test: install a real model pair, run generate_verse_embeddings, and confirm a conceptual paraphrase (little/no shared vocabulary with its source verse) is correctly surfaced as a Pending suggestion."
    },
    "serviceReportProof": {
        "trigger": "The operator's own roadmap synthesis (cross-referencing docs/phase-4-master-plan-gap-audit.md against a competitive critique document) named 'Reliability & Trust' as the Phase 5 theme; a post-service summary was chosen as the smallest, lowest-risk, immediately-useful slice, mirroring exactly how Phase 3.9 (Sermon Harvest) was scoped.",
        "design": "New apps/desktop/src-tauri/src/service_report.rs (SuggestionStats/DetectionKindCount/TimelineCategoryCount/LiveDiagnosticsSnapshot/ServiceReport structs + build_service_report()) is a pure aggregation function with zero detection logic of its own - it only calls existing persistence::list_suggestions/get_service, two new lightweight GROUP BY count queries (persistence::scripture_detection_kind_counts, timeline::count_events_by_category), and reads a cloned snapshot of AppState's already-existing SpeechDiagnostics/EmbeddingDiagnostics.",
        "honestlyScopedDiagnostics": "SpeechDiagnostics/EmbeddingDiagnostics are AppState fields that accumulate across every start_listening call in one run of the app, never reset per service. LiveDiagnosticsSnapshot's field names and the module's own doc comment say 'since app launch,' never claiming false service-scoped precision - consistent with this project's standing Environment A/B/C discipline against inflated software-only readings. avg_inference_duration_ms is derived identically to commands::get_pilot_diagnostics's own existing derivation, verified via a dedicated test so the two can never silently drift apart.",
        "filesChanged": "persistence.rs gained scripture_detection_kind_counts (1 new function). timeline.rs gained count_events_by_category (1 new function). New service_report.rs (5 structs + build_service_report + 8 unit tests). commands.rs gained the get_service_report command. lib.rs registers the new module and command. Frontend: domain/service.ts gained SuggestionStats/DetectionKindCount/TimelineCategoryCount/LiveDiagnosticsSnapshot/ServiceReport; lib/commands.ts gained getServiceReport(); HistoryView.tsx fetches the report (in a separate, failure-isolated call from its existing Promise.all) and renders a new Service Report panel with a collapsed, explicitly-labeled 'Live pipeline diagnostics (since app launch, not this service alone)' section.",
        "noExistingBehaviorChanged": "Zero changes to any existing command's signature or behavior, zero changes to any existing database table/column/write path (both new persistence functions are read-only SELECT ... GROUP BY queries against tables that already existed), zero changes to SpeechDiagnostics/EmbeddingDiagnostics's own fields or update sites, zero changes to HistoryView's existing fetch/render logic beyond one new additive .then()/.catch().",
        "regressionResult": "cargo fmt --check clean. cargo clippy --workspace --all-targets -- -D warnings clean under both default features and --features whisper,semantic-search. cargo test --workspace (single-threaded, to route around a pre-existing, unrelated config.rs env-var test-parallelism flake) fully green under both feature configurations; cip-desktop alone gained 8 new tests, all in service_report.rs, all passing (297/297 total). Frontend: tsc -b 0 errors, oxlint 0 new warnings, vitest 220/220 passed (218 pre-existing + 2 new commands.test.ts cases), vite build clean.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer finds 2 occurrences of the literal command name 'get_service_report' (including its position in the invoke handler's registered command-name table, contiguous right after 'get_service'). x86_64-w64-mingw32-nm finds the mangled function symbol cip_desktop_lib::commands::get_service_report, plus Serialize-impl symbols for SuggestionStats/DetectionKindCount/TimelineCategoryCount/LiveDiagnosticsSnapshot and drop-glue for ServiceReport - direct proof this phase's code is genuinely compiled into this exact binary. Prior-phase symbols confirmed unaffected: whisper_full_with_state (96 occurrences, identical to the Phase 4.4 baseline), finalize_bible_only (1), get_embedding_capabilities (2), all-MiniLM-L6-v2 (2). See pilot-evidence/5.1/.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - this is read-side-only aggregation of data formats already exercised on real hardware in earlier phases, so residual risk is small, but the report has never been rendered against a real operator's completed service."
    },
    "temporalConfirmationProof": {
        "trigger": "The operator's own roadmap item, named directly: 'Temporal confirmation/sliding re-score,' the second slice of the 'Reliability & Trust' Phase 5 theme after Phase 5.1's observability report.",
        "audit": "Tracing process_transcript_segment (core/service) and handle_final_transcript (pipeline.rs) found every detection scored exactly once, from a single transcript segment, with no mechanism that ever revisits that score - the existing 60-second suggestion-dedup window already detects a same-category repeat but only ever suppressed it, never treating the repeat as evidence.",
        "latentBugFound": "DetectionCategory had only Explicit and Paraphrase variants - ReferenceKind::Semantic fell into the Explicit bucket by omission, so a repeated Semantic guess was checked only against DIRECT/VERSE/SEQUENTIAL_REFERENCE detection rows, never against another SEMANTIC_REFERENCE row, and could never dedup against itself. Fixed with a new DetectionCategory::Semantic and its own SQL branch, restoring the same self-dedup guarantee Paraphrase already had - a real correctness fix found during this phase's audit, not merely a prerequisite for confirmation.",
        "design": "New database column ai_suggestions.confirmation_count (migration 0014, default 0). New core/ai::Suggestion.confirmation_count field, always 0 at construction. New persistence.rs functions: find_pending_suggestion_for_reference (the most recent still-Pending suggestion matching a reference) and confirm_suggestion (bumps confidence_score/confidence_level/confirmation_count: new_score = (current + bonus).min(max_score).max(current), so the score only ever rises, is capped, and confirmation_count increments unconditionally). pipeline.rs's dedup loop now classifies Explicit/Paraphrase/Semantic (previously a 2-way split) and, when a Paraphrase/Semantic repeat is suppressed, confirms the original suggestion via these two functions. New policy constants CONFIRMATION_SCORE_BONUS (0.1) and MAX_CONFIRMED_SCORE (0.9, deliberately below the ~0.97 an explicit citation earns).",
        "neverTouchesDecidedSuggestions": "find_pending_suggestion_for_reference filters to SuggestionStatus::Pending only - once an operator approves/edits/rejects a suggestion, a later redetection can never mutate that decision. Verified by a dedicated test (find_pending_suggestion_for_reference_ignores_non_pending_suggestions).",
        "explicitCitationsUnaffected": "Direct/Chapter/Verse/Sequential citations are never confirmation-eligible - only Paraphrase/Semantic detections are. Verified by a dedicated pipeline.rs acceptance test (a_repeated_explicit_citation_is_never_confirmation_boosted) proving a repeated citation's confirmation_count stays 0 and its score is unchanged.",
        "directBinaryProof": "x86_64-w64-mingw32-nm against cip-desktop.exe extracted from this exact installer finds the mangled function symbols cip_desktop_lib::persistence::confirm_suggestion and cip_desktop_lib::persistence::find_pending_suggestion_for_reference. x86_64-w64-mingw32-strings finds 5 occurrences of the literal string 'confirmation_count' (the new column/field name). Prior-phase symbols confirmed unaffected: get_service_report (2 occurrences, Phase 5.1 baseline), whisper_full_with_state (96, unchanged), finalize_bible_only (1), get_embedding_capabilities (2). See pilot-evidence/5.2/.",
        "regressionResult": "cargo fmt --check clean. cargo clippy --workspace --all-targets -- -D warnings clean under both default features and --features whisper,semantic-search. cargo test --workspace (single-threaded, routing around the pre-existing config.rs test-parallelism flake documented in Phase 5.1) fully green under both feature configurations. New test coverage: 9 in persistence.rs, 2 acceptance tests in pipeline.rs (including a real repeated-paraphrase-confirms test against the actual dev-seed dataset), 1 in core/ai::suggestion. Frontend: tsc/oxlint clean (no new warnings), vitest 220/220 passed, vite build clean.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: speak (or replay) a paraphrase of a verse twice within about a minute and confirm the Needs Attention queue shows a single suggestion with a 'Confirmed x1' badge and a visibly higher confidence than the first mention alone would have produced."
    },
    "audioVadProof": {
        "trigger": "The operator's own roadmap item, named directly: 'Audio pipeline hardening (noise suppression/AEC/VAD),' the third slice of the 'Reliability & Trust' Phase 5 theme after Phase 5.1's observability report and Phase 5.2's temporal confirmation.",
        "audit": "Tracing WhisperSpeechEngine::run_inference (ai/speech/src/whisper.rs) found every fully-buffered ~3s window is unconditionally handed to whisper.cpp's state.full(), whether or not it contains actual speech - wasting real inference time (up to ~15s per call on the real Windows hardware measured in docs/phase-3-8-7-7-audit.md) on silence, and risking Whisper's documented tendency to hallucinate plausible-looking text on silent/near-silent input rather than reliably returning empty output.",
        "scopeDecision": "The roadmap item names three distinct technologies. Noise suppression would require a new native dependency and DSP code with no way to validate its actual audio-quality effect without real microphone recordings and a human listener - not attemptable here with any honesty. AEC only matters when a capture device's own output leaks back into its input - a real-hardware-only failure mode with no way to construct a meaningful automated test in this container. VAD via RMS-energy thresholding is pure arithmetic on the sample buffer already in memory: zero new dependencies, a deterministic and fully unit-testable classification function. VAD alone is this phase's scope; the other two remain explicitly deferred.",
        "design": "New SILENCE_RMS_THRESHOLD constant (0.01, ai/speech/src/whisper.rs), deliberately conservative and documented as not empirically calibrated - set near the digital noise floor rather than tuned to filter quiet speech, since silently dropping real soft speech is judged a far worse failure than occasionally spending one inference on true silence. New pure functions rms_level(samples: &[i16]) -> f32 and is_silence(samples: &[i16]) -> bool, directly unit-testable without any WhisperContext. The VAD gate sits inside run_inference(), placed after duration_ms/start_ms are computed (needed for elapsed_ms/timestamp continuity regardless of outcome) and before whisper.cpp is ever called - a silent window still advances elapsed_ms and clears the buffer exactly as the non-silent path does, so no downstream segment's start_ms/end_ms is ever distorted.",
        "traitAndDiagnostics": "New SpeechEngine::last_feed_was_silence() default-false trait method (core/ai/src/speech_engine.rs), following the same pattern Phase 3.8.7.3 established for last_feed_triggered_inference() - only WhisperSpeechEngine overrides it, since it is the only engine that internally buffers audio; ScriptedSpeechEngine/NullSpeechEngine inherit the default unchanged. New silent_windows_skipped counter (state.rs's SpeechDiagnostics, commands.rs's SpeechRuntimeDiagnostics), incremented in the speech-worker loop independently of the existing triggered_inference branch (the two conditions are mutually exclusive per feed_audio call, checked separately), surfaced in the Pilot Diagnostics panel's Inferences line.",
        "neverDistortsTimestamps": "Verified by direct code inspection: the silent-window path and the inferred-window path both compute duration_ms/start_ms identically before branching, and both advance elapsed_ms and clear the buffer identically after - only the state.full() call itself and its downstream segment construction are skipped on the silent path. No existing timestamp-dependent test needed modification.",
        "directBinaryProof": "x86_64-w64-mingw32-nm against cip-desktop.exe extracted from this exact installer finds the mangled trait-impl symbol for WhisperSpeechEngine's SpeechEngine::last_feed_was_silence, alongside the pre-existing last_feed_triggered_inference/discard_buffered_audio/required_sample_rate_hz/flush/is_ready/feed_audio impls and the run_inference/load functions unaffected. rms_level/is_silence (small, single-call-site private functions) were inlined by the release-mode optimizer, as expected, and are instead verified via 4 dedicated unit tests. x86_64-w64-mingw32-strings finds 1 occurrence of 'silentWindowsSkipped' (the new SpeechRuntimeDiagnostics field's serde camelCase name) - direct proof the new diagnostics field is genuinely compiled into this exact binary. Prior-phase symbols confirmed unaffected: confirm_suggestion/find_pending_suggestion_for_reference (2 occurrences, Phase 5.2), get_service_report (2, Phase 5.1), whisper_full_with_state (96, unchanged), finalize_bible_only (1), get_embedding_capabilities (2). See pilot-evidence/5.3/.",
        "regressionResult": "cargo fmt --check clean. cargo clippy --workspace --all-targets -- -D warnings clean under both default features and --features whisper,semantic-search. cargo test --workspace (single-threaded, routing around the pre-existing config.rs test-parallelism flake documented in Phase 5.1) fully green under both feature configurations - cip-ai-speech went from 7 to 11 passing tests (the 4 new VAD unit tests), every other crate's count unchanged. Frontend: tsc/oxlint clean (same 4 pre-existing warnings, unrelated to this phase), vitest 220/220 passed (unchanged count - the frontend change is a diagnostics-display conditional with no dedicated test), vite build clean.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact, and the classification threshold itself has never been exercised against real audio in this container at all (WhisperSpeechEngine::load() always fails without a real ggml/gguf model file, a standing limitation since Phase 1.2). The decisive pending gate is the operator's own real-hardware test: run a live or replayed service with genuine pauses between sentences and confirm the transcript no longer shows hallucinated text during silent gaps, and that silent_windows_skipped rises during those gaps without ever rising during actual quiet speech."
    },
    "wrongVerseFeedbackLoopProof": {
        "trigger": "The operator's own roadmap item, named directly: 'Wrong-verse feedback loop,' the fourth slice of the 'Reliability & Trust' Phase 5 theme after Phase 5.1's observability report, Phase 5.2's temporal confirmation, and Phase 5.3's VAD.",
        "audit": "Tracing reject_suggestion (commands.rs), update_suggestion_status (persistence.rs), and the dedup/confirmation branch in pipeline.rs::handle_final_transcript_inner found that rejecting a suggestion had zero downstream effect anywhere in the codebase - nothing ever read the Rejected status. Worse: because the dedup window checks scripture_detections (not suggestion status) and Phase 5.2's confirm_suggestion path only looks up still-Pending suggestions, a same-category repeat of an already-rejected reference within the 60s window was silently dropped with no trace at all - not unique to Rejected (Approved/Edited have the identical gap), but the one case that actually discards the operator's explicit correction with no record.",
        "designChoice": "Two designs were put to the operator directly: (1) a silent echo counter - keep suppressing the repeat exactly as today, but record that it happened; (2) re-surface a new, dampened suggestion each time so the operator can reconsider. The operator chose (1), with an explicit flow: detection -> same reference+category -> was it rejected within the window? -> silently suppress + increment rejection echo counter + record diagnostic information.",
        "design": "New database column ai_suggestions.rejection_echo_count (migration 0015, default 0). New core/ai::Suggestion.rejection_echo_count field, always 0 at construction. New persistence.rs functions: find_rejected_suggestion_for_reference (the most recent Rejected suggestion matching a reference, mirroring find_pending_suggestion_for_reference exactly but filtered to Rejected) and record_rejection_echo (increments rejection_echo_count unconditionally; never touches status/confidence/kind). pipeline.rs's dedup-suppression branch now falls back to this lookup whenever no Pending suggestion exists for a Paraphrase/Semantic repeat - dedup's own suppression (the continue) is completely unchanged either way.",
        "neverResurrectsADecidedSuggestion": "record_rejection_echo only ever updates the rejection_echo_count column - verified by a dedicated test (record_rejection_echo_increments_the_count_without_touching_status_or_score) asserting the suggestion's status and confidence score are byte-identical before and after recording an echo.",
        "diagnosticInformation": "apps/desktop/src-tauri/src/service_report.rs's SuggestionStats (Phase 5.1's Post-Service Observability Report) gained rejection_echoes: u64 - the sum of every suggestion's rejection_echo_count for the service, computed in the same read-only aggregation pass that already builds the rest of the report. Verified by a dedicated test (report_sums_rejection_echoes_across_every_suggestion) summing echoes across two separately-rejected suggestions. History's HistoryView.tsx surfaces both this service-level total and a per-suggestion 'echoed xN' badge.",
        "directBinaryProof": "x86_64-w64-mingw32-nm against cip-desktop.exe extracted from this exact installer finds the mangled function symbols cip_desktop_lib::persistence::find_rejected_suggestion_for_reference and cip_desktop_lib::persistence::record_rejection_echo. x86_64-w64-mingw32-strings finds 5 occurrences of the literal string 'rejection_echo_count' (the new column/field name) and 1 occurrence of 'rejectionEchoCount' (the frontend-facing serde field name). Prior-phase symbols confirmed unaffected: silentWindowsSkipped (1 occurrence, Phase 5.3), confirm_suggestion/find_pending_suggestion_for_reference (2, Phase 5.2), get_service_report (2, Phase 5.1), whisper_full_with_state (96, unchanged), finalize_bible_only (1), get_embedding_capabilities (2). See pilot-evidence/5.4/.",
        "regressionResult": "cargo fmt --check clean. cargo clippy --workspace --all-targets -- -D warnings clean under both default features and --features whisper,semantic-search. cargo test --workspace (single-threaded, routing around the pre-existing config.rs test-parallelism flake documented in Phase 5.1) fully green under both feature configurations - cip-desktop went from 307 to 313 passing tests (4 new in persistence.rs, 1 new acceptance test in pipeline.rs, 1 new in service_report.rs). Frontend: tsc/oxlint clean (same 4 pre-existing warnings, unrelated to this phase), vitest 220/220 passed, vite build clean.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: reject a Paraphrase/Semantic suggestion, speak (or replay) the same wording again within about a minute, and confirm the Needs Attention queue does not show it again while History's Service Report and Scripture & Findings list show the echo count rising."
    },
    "keyboardShortcutsProof": {
        "trigger": "Following Phase 5.4's close of the 'Reliability & Trust' theme, a dedicated audit opened the next theme, 'Operator Ergonomics' (Phase 6), covering LiveChurchBrain.tsx, workspace/AttentionQueue.tsx, workspace/IntelligenceCard.tsx, and lib/keyboardShortcuts.ts. The operator chose the highest-priority of 8 findings to fix first.",
        "audit": "Keyboard shortcuts (A/R/E/P) always acted on suggestions[0] - the raw, Bible-only pending-suggestion list - regardless of mode. The diagnostics-mode panel correctly documents this, but Operator Mode's primary panel, the Needs Attention queue (attentionQueue, spanning six domains with its own confidence-based ordering), is a completely different list. Pressing 'A' while looking at the Needs Attention queue silently acted on suggestions[0] - not necessarily the item visually at the top, and not even guaranteed to be a Bible item. A solo live operator trusting the shortcut to act on what they see on screen could approve or reject the wrong thing.",
        "designChoice": "Mode-gated shortcut targets, not a shared one: Diagnostics Mode keeps its exact existing suggestions[0]-based A/R/E/P behavior, unchanged (Edit/Preview have no equivalent in the domain-generic unified action model). Operator Mode gets its own A/R behavior against attentionQueue[0], dispatched through the exact same handleUnifiedAction every card's own buttons already call. Domain-generic key mapping ('A' = primary action, 'R' = secondary) via a new pure function resolveUnifiedShortcutAction, so a shortcut can never fire an action a button on screen wasn't already offering. A new visible legend (shortcutLegend) on the queue's heading shows exactly what A/R will do, mirroring the discoverability hint the diagnostics panel already had.",
        "newCode": "apps/desktop/src/lib/keyboardShortcuts.ts: resolveUnifiedShortcutAction(key, actions). apps/desktop/src/lib/attentionQueue.ts: shortcutLegend(queue). apps/desktop/src/components/LiveChurchBrain.tsx: keyboard-shortcut effect relocated after handleUnifiedAction and rewritten to branch on mode. apps/desktop/src/components/workspace/AttentionQueue.tsx: heading renders the legend as a hint span.",
        "newTests": "5 new cases for resolveUnifiedShortcutAction (keyboardShortcuts.test.ts), 6 new cases for shortcutLegend (attentionQueue.test.ts) - both pure-function/no-DOM tests per this project's established no-DOM-testing-environment convention.",
        "regressionResult": "Frontend only - zero Rust files changed this phase, so no Rust regression re-run was needed (confirmed, not assumed - see directBinaryProof below). npm run typecheck: clean. npm run lint: only the 4 pre-existing set-state-in-effect/only-export-components warnings, in files this phase did not touch - no new warnings (verified after moving shortcutLegend out of the .tsx component file specifically to avoid a new react/only-export-components warning it triggered when first placed there). npm run test: 231/231 passing (220 pre-existing + 11 new). npm run build: succeeds.",
        "directBinaryProof": "x86_64-w64-mingw32-nm/strings against cip-desktop.exe extracted from this exact installer confirms every previously-tracked Rust-side symbol/string count is byte-for-byte unchanged from the Phase 5.4 baseline (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, get_service_report: 2, whisper_full_with_state: 96) - direct proof of zero Rust regression. Direct strings-based verification of this phase's new frontend text is NOT possible: searching for the new legend substring 'for the first item' returned 0 matches, and a control search for two other frontend-only strings known to be present and UNCHANGED by this phase ('Needs Attention', 'Display') ALSO returned 0 matches - confirming Tauri's compressed embedded frontend bundle is opaque to plain strings(1)/nm(1) scanning, not a build defect. Correctness for this phase's frontend-only change instead rests on a fresh Vite build producing a new asset bundle hash, the installer's SHA-256 changing from the Phase 5.4 baseline, and the full 231/231 frontend test pass - see pilot-evidence/6.1/windows/installer-contents-verification.json for the complete honest breakdown.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database columns/tables. Diagnostics Mode's A/R/E/P behavior against suggestions[0] is byte-identical to before this phase. resolveUnifiedShortcutAction only ever returns an action already present in actionsFor(domain) - it can never fire an action a button on screen wasn't already offering. The Operator Mode shortcut path dispatches through the exact same handleUnifiedAction function every IntelligenceCard button already calls - never a second command-dispatch mechanism.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: with items from more than one domain in the Needs Attention queue, confirm the legend text matches the top item and that pressing A/R acts on that exact item, not a different one."
    },
    "displayConfirmationUndoProof": {
        "trigger": "Phase 6.1's own audit named this gap explicitly ('no confirmation/undo on the one-click Display action'); this phase opened with a dedicated audit breaking it down further (docs/phase-6-2-audit.md) before any code was written.",
        "audit": "Display fires immediately on click, chaining approve_suggestion -> prepare_presentation -> display_presentation with no confirmation step - by the time the handler returns, content is already on the real projector. This is deliberate (Phase 3.8.7.8 built Display specifically to remove clicks, at the operator's own request), so a blocking confirmation dialog in front of every click would fight the feature's own purpose. A manual Stop button (clear_presentation_display) already existed but only blanks the screen - it does not restore what was showing before, and nothing prompted the operator to use it right after a mistaken Display. No confirmation-dialog pattern existed anywhere else in this codebase.",
        "designChoice": "Put directly to the operator: confirm-before (a guard on every click, at the cost of Display's one-click speed), undo-after (keeps every correct click free, only costs anything on a genuine misclick), or both. The operator chose both.",
        "design": "New apps/desktop/src/lib/confirmGuard.ts: decideConfirmClick(key, pending, now) - a small, generic, pure two-click arm/fire guard (arms on the first click, fires on a second matching click within CONFIRM_WINDOW_MS=4s), keyed by an arbitrary string rather than hardcoded to 'display' so any future action could reuse it. LiveChurchBrain.tsx's handleUnifiedAction wires this in for the bible/'display' branch only - every other unified action (approve/reject/accept/acknowledge/review/dismiss) still fires on its first click, completely unaffected. IntelligenceCard.tsx gained a confirmingKey prop so the one armed button swaps its label to 'Confirm {label}?'. A new post-success 'Undo (blank screen)' banner appears once display_presentation actually succeeds (an earlier throw in the chain skips it), visible for DISPLAY_UNDO_WINDOW_MS=8s or until activeDisplayItem no longer matches - its button calls the exact same clear_presentation_display command the Presentation card's own Stop button already calls; no new 'restore the previous item' capability was invented, since that state isn't tracked anywhere today.",
        "uniformAcrossInputMethods": "Because both a mouse click and the 'A' keyboard shortcut already dispatch through the single handleUnifiedAction function (Phase 6.1's own single-dispatcher discipline), the confirm guard applies identically to both without any keyboard-specific logic - pressing 'A' once arms the same state a click would, and the armed button's label updates regardless of which input method triggered it.",
        "newTests": "5 new cases for decideConfirmClick (confirmGuard.test.ts): arms on the first click, fires on a second matching click within the window, re-arms instead of firing once the window has elapsed, arms the new key instead of firing when a different key is currently armed, arms when nothing was pending at all - a pure-function/no-DOM test per this project's established convention.",
        "regressionResult": "Frontend only - zero Rust files changed this phase (confirmed via git status/diff before the rebuild). npm run typecheck: clean. npm run lint: same pre-existing warnings as before this phase - no new ones (the first draft of the Undo auto-dismiss effect DID trigger a new set-state-in-effect warning via a synchronous setState call for an edge-case branch; fixed by moving that logic into the setTimeout callback instead, then re-verified clean, mirroring Phase 6.1's own precedent of adjusting the implementation rather than accepting a new warning). npm run test: 236/236 passing (231 pre-existing + 5 new). npm run build: succeeds.",
        "directBinaryProof": "x86_64-w64-mingw32-nm/strings against cip-desktop.exe extracted from this exact installer confirms every previously-tracked Rust-side symbol/string count is unchanged from the Phase 6.1 baseline for every functionally-real symbol (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, get_service_report: 2) - direct proof of zero Rust regression.",
        "whisperFullWithStateCountNote": "whisper_full_with_state's nm hit count moved from 96 (Phase 6.1 baseline) to 93 in this rebuild, despite zero Rust/C++ files changing this phase. Broken down by symbol type, this drop is entirely in compiler-generated auxiliary symbols (42 local 't', 31 '.xdata'/'r', 19 'p' entries - closures, thread-state machinery, and exception-unwind metadata the C++ compiler emits for lambdas declared inside whisper_full_with_state's own vendored source) - the single global 'T' (exported, real function) symbol remains present and unique, exactly as in every prior baseline. This count of auxiliary symbols is known to vary slightly build-to-build due to parallel codegen-unit scheduling/deduplication in the underlying C++ toolchain, independent of source changes - documented honestly here rather than silently ignored or falsely claimed unchanged.",
        "frontendTextSearchLimitation": "Direct strings-based verification of this phase's new frontend text ('Confirm Display', 'Undo (blank screen)') is NOT possible: both returned 0 matches, and a control search for two frontend-only strings known to be present and UNCHANGED by this phase ('Needs Attention', exact-line 'Display') ALSO returned 0 matches - reproducing Phase 6.1's own finding that Tauri's compressed embedded frontend bundle is opaque to plain strings(1)/nm(1) scanning. Correctness for this phase's frontend-only change instead rests on a fresh Vite build producing a new asset bundle hash, the installer's SHA-256 changing from the Phase 6.1 baseline, and the full 236/236 frontend test pass - see pilot-evidence/6.2/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database columns/tables. Every unified action other than bible/'display' fires exactly as before this phase. decideConfirmClick never fires an action on its own - it only gates the exact same call that already existed before this phase. Undo calls the pre-existing clear_presentation_display command, the same one the Presentation card's own Stop button has always called.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: click Display once (confirm the button shows 'Confirm Display?' and nothing is projected yet), click it again (confirm it fires and the Undo banner appears), then click Undo (confirm the real screen blanks)."
    },
    "feedSearchProof": {
        "trigger": "Phase 6.1's own audit named this gap directly ('no text search over the live feed'); this phase opened with a dedicated audit (docs/phase-6-3-audit.md) before any code was written.",
        "audit": "IntelligenceFeed.tsx renders the full cross-domain feed (bounded to MAX_VISIBLE_INTELLIGENCE_ITEMS=50 upstream), already filterable by domain via chip buttons (local state + an inline useMemo), but had no text search at all. The 'S' keyboard shortcut's searchInputRef is a completely unrelated, pre-existing feature (Manual Bible Search) - not reusable here. filterBooksByPrefix (lib/libraryHelpers.ts, Phase 4.2) is this codebase's closest precedent: a pure, case-insensitive filter function with a dedicated test file - prefix-only, right for a short book name, but a feed of full sentences needs substring matching so a mid-summary word still matches.",
        "designChoice": "Unlike Phase 6.2, this gap had one clear shape rather than a genuine architectural fork, so implementation proceeded directly to code without putting a design question to the operator.",
        "design": "New apps/desktop/src/lib/intelligenceFeed.ts: searchIntelligenceFeed(items, query) - case-insensitive substring match against summary and detailLine (null-safe), empty/whitespace-only query returns items unchanged, matching filterBooksByPrefix's own convention. IntelligenceFeed.tsx gained a text input that composes with the existing domain filter chips over the same already-fetched items array - no new fetch, no new command, no change to the 50-item cap. The panel remains read-only, per its own existing doc comment.",
        "newTests": "6 new cases for searchIntelligenceFeed (intelligenceFeed.test.ts): mid-string match case-insensitively, detailLine fallback when summary doesn't match, null-safety on a null detailLine, whitespace trimming, empty-query passthrough, no-match empty result - a pure-function/no-DOM test per this project's established convention.",
        "regressionResult": "Frontend only - zero Rust files changed this phase (confirmed via git status/diff before the rebuild). npm run typecheck: clean. npm run lint: same pre-existing warnings as before this phase - no new ones. npm run test: 242/242 passing (236 pre-existing + 6 new). npm run build: succeeds.",
        "directBinaryProof": "x86_64-w64-mingw32-nm/strings against cip-desktop.exe extracted from this exact installer confirms every previously-tracked Rust-side symbol/string count is unchanged from the Phase 6.2 baseline for every functionally-real symbol (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, get_service_report: 2) - direct proof of zero Rust regression. whisper_full_with_state's nm hit count (93) exactly matched the Phase 6.2 baseline this time, including the identical breakdown by symbol type (1 T, 42 t, 31 r, 19 p) - no further drift, supporting Phase 6.2's own explanation that the earlier 96->93 change (see displayConfirmationUndoProof's whisperFullWithStateCountNote) was toolchain non-determinism rather than anything phase-specific.",
        "frontendTextSearchLimitation": "Direct strings-based verification of this phase's new frontend text ('Search the feed', the search input's placeholder) is NOT possible: 0 matches, and a control search for two frontend-only strings known to be present and UNCHANGED by this phase ('Needs Attention', 'Intelligence Feed') ALSO returned 0 matches - reproducing the Phase 6.1/6.2 finding a third time. Correctness for this phase's frontend-only change instead rests on a fresh Vite build producing a new asset bundle hash, the installer's SHA-256 changing from the Phase 6.2 baseline, and the full 242/242 frontend test pass - see pilot-evidence/6.3/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database columns/tables. Pure client-side filter over data already fetched for the existing feed - no new fetch, no change to the 50-item cap. The panel remains read-only - search never becomes a second action surface. Composes with (does not replace) the existing domain filter chips.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: with a live feed containing more than a few items, type a word that only appears in one item's summary and confirm the feed narrows to just that item, then clear the box and confirm every item returns."
    },
    "audioSpeechErrorVisibilityProof": {
        "trigger": "Phase 6.1's own audit named this gap directly ('buried audio/speech error visibility'); this phase opened with a dedicated audit (docs/phase-6-4-audit.md) before any code was written.",
        "audit": "get_live_status's AudioStatusKind/SpeechStatusKind enums already OR together the real error sources (AudioEngineStatus::stream_error + AppState::audio_error for audio; AppState::speech_error for speech) to decide Error - but only the boolean presence, discarding the actual string. Operator Mode's two error notices were hardcoded, generic text that never changed regardless of the real failure. Diagnostics Mode rendered real text for speech only (diagnostics.speech.lastError); diagnostics.audio.streamError was already present in every diagnostics poll but nothing rendered it - a smaller, parallel gap even in the mode meant to have full detail. Real error text is plain and operator-readable ('no audio device available', 'model not found: ...', 'transcription failed: ...'), not raw debug output.",
        "designChoice": "No genuine architectural fork this phase - the real text already existed, was already computed by the exact condition already deciding the enum, and was already operator-readable plain text. Proceeded directly to implementation.",
        "design": "LiveStatus (commands.rs + domain/live.ts) gains two additive fields, audio_error_text/speech_error_text, computed with the exact same source-of-truth already deciding the enum (audio.stream_error preferred, falling back to state.audio_error, for audio; state.speech_error directly for speech) - no new state, no new command, no duplicated condition. New pure helper error_text_if(is_error, text) (commands.rs) - the one rule both fields share: never surface text unless the status actually resolved to Error, even if a stale, not-yet-cleared string is still sitting in state. LiveChurchBrain.tsx's Operator Mode notices now interpolate the real text, falling back to the original generic phrasing if it's ever absent. PilotDiagnosticsPanel.tsx gained a 'Last error: ...' line for audio, mirroring speech's own existing line - the data was already in every poll, this only renders it.",
        "newTests": "3 new cases for error_text_if (commands.rs test module): returns None when not an error even with stale text, returns the text when it is an error, returns None for an error with no text rather than fabricating one. Plus live_status_serializes_camel_case extended with audioErrorText/speechErrorText assertions. Pure-function/no-mutex tests per this project's established 'command tests stop short of standing up a full tauri::test::mock_builder()' convention.",
        "regressionResult": "cargo fmt --check: clean. cargo clippy --workspace --all-targets -- -D warnings: clean under both default features and --features whisper,semantic-search. cargo test --workspace (single-threaded, routing around the pre-existing config.rs test-parallelism flake documented since Phase 5.1): fully green under both feature configurations - cip-desktop went from 313 to 316 passing tests. Frontend: npm run typecheck clean, npm run lint same pre-existing warnings (no new ones), npm run test 242/242 passing (unchanged count - this phase's frontend work is rendering/interpolation, not new pure logic), npm run build succeeds.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer finds 1 occurrence each of the new serde camelCase field names 'audioErrorText' and 'speechErrorText' - direct proof LiveStatus's two new fields are genuinely compiled into this exact binary. error_text_if itself (a small, pure, two-call-site function) was inlined by the release-mode optimizer, as expected and consistent with this project's established pattern for small pure functions - verified instead via its 3 dedicated unit tests. Every prior-phase Rust-side symbol/string count is unchanged from the Phase 6.3 baseline (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, get_service_report: 2, whisper_full_with_state: 93 - identical to both the Phase 6.2 and Phase 6.3 baselines for a third consecutive rebuild) - direct proof this phase's Rust changes are additive only, with no regression to any prior phase's compiled surface.",
        "frontendTextSearchLimitation": "Direct strings-based verification of this phase's new frontend text ('Last error', the new audio diagnostics line's leading text) is NOT possible: 0 matches, and a control search for a frontend-only string known to be present and UNCHANGED by this phase ('Needs Attention') ALSO returned 0 matches - reproducing the Phase 6.1/6.2/6.3 finding a fourth time. Correctness for this phase's frontend-only changes instead rests on a fresh Vite build producing a new asset bundle hash and the installer's SHA-256 changing from the Phase 6.3 baseline - see pilot-evidence/6.4/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database columns/tables. LiveStatus gains only two additive, read-only fields derived entirely from state that already existed. error_text_if's contract (None unless the status is actually Error) prevents a stale error string from ever appearing next to a status that says everything is fine. Operator Mode's generic phrasing remains as a fallback - notices never disappear or break if text is absent. Diagnostics Mode's new line renders data that was already transmitted on every poll - no new field on the wire.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: trigger a real audio or speech error (e.g. unplug the microphone, or point the Whisper model path at a nonexistent file) and confirm Operator Mode's notice shows the actual error text, not just the bare label."
    },
    "errorBannerDismissContextProof": {
        "trigger": "Phase 6's own audit named this gap directly ('no dismiss/context on the generic error banner'); this phase opened with a dedicated audit (docs/phase-6-5-audit.md) before any code was written.",
        "audit": "The live-brain__error banner had no dismiss affordance at all - error cleared only at the start of the next tracked (withBusy) action, regardless of outcome, so a failed action's banner sat on screen indefinitely until the operator happened to trigger some other action. Every setError call site discarded context about which action failed - withBusy's catch block already had a descriptive key in scope (approve-${id}, start-service, etc., already used for button-disabling) but never surfaced it, so two different failures in quick succession produced indistinguishable banners. No generic dismiss-button/toast pattern existed anywhere in the codebase to reuse.",
        "designChoice": "No genuine architectural fork this phase - both the dismiss control and the context label have one clear shape, so implementation proceeded directly without an operator design question.",
        "design": "New pure helper humanizeBusyKey(key) (errorContext.ts): strips a trailing UUID, turns hyphens into spaces, capitalizes - a mechanical transform covering all ~40+ withBusy call sites without a lookup table. withBusy's catch block now prepends humanizeBusyKey(key) to the error message (one call site covers every withBusy-wrapped action); the three other setError sites without a busy key (status poll, history load, timeline load) get an equivalent static label each; the one already-contextual site (FileReader.onerror, which already embeds the filename) is unchanged. The banner gained a small x dismiss button calling the same setError(null) already used elsewhere today - no new clearing mechanism, just a manual trigger for the existing one.",
        "newTests": "6 new cases for humanizeBusyKey (errorContext.test.ts): UUID-suffix stripping and capitalization, multi-word keys with a trailing UUID, plain hyphenated keys with no id suffix, single-word keys, a non-UUID suffix (a Bible reference) left untouched, and the empty-string edge case. Pure-function tests per this project's established testing convention.",
        "regressionResult": "Frontend only - zero Rust files changed this phase (confirmed via git status/diff before the rebuild). npm run typecheck: clean. npm run lint: same pre-existing warnings as before this phase - no new ones. npm run test: 248/248 passing (242 pre-existing + 6 new). npm run build: succeeds.",
        "directBinaryProof": "x86_64-w64-mingw32-nm/strings against cip-desktop.exe extracted from this exact installer confirms every previously-tracked Rust-side symbol/string count is unchanged from the Phase 6.4 baseline (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, get_service_report: 1, audioErrorText/speechErrorText: 1 each) - direct proof of zero Rust regression, consistent with this phase touching no Rust files. whisper_full_with_state's nm hit count (93) exactly matched the Phase 6.2/6.3/6.4 baseline for a fourth consecutive rebuild, further confirming the earlier 96->93 drift was one-time toolchain non-determinism rather than anything phase-specific.",
        "frontendTextSearchLimitation": "Direct strings-based verification of this phase's new frontend text ('Dismiss error', the banner's new context-prefixed messages like 'Load history failed') is NOT possible: 0 matches, and a control search for a frontend-only string known to be present and UNCHANGED by this phase ('Needs Attention') ALSO returned 0 matches - reproducing the Phase 6.1/6.2/6.3/6.4 finding a fifth time. Correctness for this phase's frontend-only change instead rests on a fresh Vite build producing a new asset bundle hash and the installer's SHA-256 changing from the Phase 6.4 baseline - see pilot-evidence/6.5/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database columns/tables - purely a frontend message-formatting and dismiss-UI addition. The dismiss button calls the exact same setError(null) already used elsewhere - no new state, no new clearing path with different semantics. humanizeBusyKey never changes what fires - it only relabels the same key already computed for setBusy/isBusy, purely for display. Every existing setError call site still sets the same underlying error state; only the string content changed.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: trigger a real command failure (e.g. approve a suggestion while offline, or attempt an action during a network hiccup), confirm the banner names the failed action, then click the x and confirm it clears without needing to trigger another action first."
    },
    "onboardingProof": {
        "trigger": "Phase 6's own audit named this gap ('no onboarding') but never scoped it - the last of the 8 original gaps to be broken down. This phase opened with a dedicated audit (docs/phase-6-6-audit.md) before any code was written.",
        "audit": "App.tsx had zero first-run affordance of any kind - no localStorage/first-run flag anywhere in the frontend, every launch identical. The audit found two genuinely different things 'onboarding' could mean: a first-launch workflow walkthrough (explains what to do, once) vs. a setup-readiness checklist (explains whether you're ready, every launch until resolved) - a real fork, the same kind Phase 6.2 put to the operator rather than guessing. While scoping the checklist half, found SystemStatusStrip (Phase 3.5) already renders Bible/Speech/Display readiness in Operator Mode - what was actually missing was a path from that status to the Diagnostics Mode controls that fix it.",
        "designChoice": "Put to the operator via AskUserQuestion: workflow walkthrough vs. setup checklist vs. both. Operator chose both.",
        "design": "lib/onboarding.ts: shouldShowWalkthrough(storedValue) - pure first-run-flag decision, separated from the actual localStorage read/write in components/OnboardingWalkthrough.tsx (wrapped in try/catch - a walkthrough that fails to persist just shows again next launch, never blocks). Rendered once from App.tsx above the section nav; one 'Got it' button dismisses it. lib/setupGaps.ts: computeSetupGaps(bibleInstalled, speechStatus) - a pure function deciding which of two operator-actionable setup items (Bible dataset, Whisper model) remain outstanding, from LiveStatus fields already fetched for SystemStatusStrip - no new command, no new fetch. Deliberately excludes a live speechStatus === 'error', already surfaced loudly elsewhere (Phase 6.4/6.5) - only 'unavailable' (no model configured, the expected first-run state) counts as a gap. LiveChurchBrain.tsx renders a new Operator-Mode-only banner, visible only while setupGaps.length > 0, with an 'Open Diagnostics Mode' button calling the exact same setMode('diagnostics') the header's own toggle already uses.",
        "newTests": "4 new cases for shouldShowWalkthrough (onboarding.test.ts): unseen/empty/other-value all show it, the exact seen marker suppresses it. 5 new cases for computeSetupGaps (setupGaps.test.ts): both ready, Bible-only gap, speech-only gap, both gaps (Bible first), and a live speech error producing no gap. Pure-function tests per this project's established testing convention.",
        "regressionResult": "Frontend only - zero Rust files changed this phase (confirmed via git status before the rebuild). npm run typecheck: clean. npm run lint: same 5 pre-existing warnings as before this phase - no new ones. npm run test: 257/257 passing (248 pre-existing + 9 new). npm run build: succeeds.",
        "directBinaryProof": "x86_64-w64-mingw32-nm/strings against cip-desktop.exe extracted from this exact installer confirms every previously-tracked Rust-side symbol/string count is unchanged from the Phase 6.5 baseline (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, get_service_report: 1, audioErrorText/speechErrorText: 1 each) - direct proof of zero Rust regression, consistent with this phase touching no Rust files. whisper_full_with_state's nm hit count (93) exactly matched the Phase 6.2-6.5 baseline for a fifth consecutive rebuild, further confirming the earlier 96->93 drift was one-time toolchain non-determinism rather than anything phase-specific.",
        "frontendTextSearchLimitation": "Direct strings-based verification of this phase's new frontend text ('Welcome to CIP', 'Open Diagnostics Mode') is NOT possible: 0 matches, and a control search for a frontend-only string known to be present and UNCHANGED by this phase ('Needs Attention') ALSO returned 0 matches - reproducing the Phase 6.1-6.5 finding a sixth time. Correctness for this phase's frontend-only change instead rests on a fresh Vite build producing a new asset bundle hash and the installer's SHA-256 changing from the Phase 6.5 baseline - see pilot-evidence/6.6/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database columns/tables - both additions are purely frontend, reading data (LiveStatus) or storage (localStorage) that already existed or was already fetched. The walkthrough never blocks: dismissible with one click, every control underneath renders normally regardless of its state. The setup notice's one button calls the exact same setMode('diagnostics') the header's own toggle already uses - no new navigation mechanism. computeSetupGaps is read-only derived state - it triggers no fetch, command, or state write of its own.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: on first launch (or after clearing app storage), confirm the walkthrough appears and does not reappear after dismissal; with either setup item missing, confirm the setup notice names it and 'Open Diagnostics Mode' switches modes; once both are installed, confirm the notice disappears."
    },
    "diagnosticsModeDensityProof": {
        "trigger": "Phase 6's own audit named this gap ('still-dense Diagnostics Mode') since Phase 6.1's report but never scoped it further. This phase opened with a dedicated audit (docs/phase-6-7-audit.md) before any code was written.",
        "audit": "Diagnostics Mode's main block rendered 11 top-level panels back to back, always fully expanded: Pending Suggestions, Service Timeline, Manual Bible Search, Music Intelligence, Sermon Foundation, Sermon Intelligence, Sermon Harvest, Cross-Domain Intelligence, Content Intelligence, Service Intelligence, Service History. Two further panels (System Diagnostics, Content Registry/Intelligence Status) were already collapsible via <details> - the rest were plain <section>, with no way to collapse any of them. A technician debugging one domain scrolled past everything else, always fully rendered.",
        "designChoice": "The fix's shape was clear (wrap each in <details>, matching the two already-collapsible panels' precedent); what genuinely forked was the default state. Put to the operator via AskUserQuestion: all default open (purely additive) vs. collapse the least-used ones by default, keeping Pending Suggestions/Service Timeline open. Operator chose the latter.",
        "design": "9 of the 11 panels (Manual Bible Search, Music Intelligence, Sermon Foundation, Sermon Intelligence, Sermon Harvest, Cross-Domain Intelligence, Content Intelligence, Service Intelligence, Service History) converted from <section className=\"live-brain__panel\"> to <details className=\"live-brain__panel\"> (h2 -> summary), collapsed by default (no open attribute). Pending Suggestions and Service Timeline are untouched, still plain <section>, still always visible. Service History's Show/Hide button gained e.stopPropagation() so a click inside its now-clickable <summary> no longer also toggles the outer panel collapse. LiveChurchBrain.css gained a .live-brain__panel summary rule mirroring the existing h2 styling, retroactively fixing the same gap in the two panels that were already <details> before this phase.",
        "newTests": "No new pure logic was introduced this phase - purely structural JSX and CSS. npm run typecheck's success is the authoritative proof every converted <details>/<section> tag pair in the ~1,260-line block still balances correctly - a mismatched tag is a compile error, not a runtime surprise.",
        "regressionResult": "Frontend only - zero Rust files changed this phase (confirmed via git status before the rebuild). npm run typecheck: clean. npm run lint: same 5 pre-existing warnings as before this phase - no new ones. npm run test: 257/257 passing (unchanged count - no new pure logic). npm run build: succeeds.",
        "directBinaryProof": "x86_64-w64-mingw32-nm/strings against cip-desktop.exe extracted from this exact installer confirms every previously-tracked Rust-side symbol/string count is unchanged from the Phase 6.6 baseline (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, get_service_report: 1, audioErrorText/speechErrorText: 1 each) - direct proof of zero Rust regression, consistent with this phase touching no Rust files. whisper_full_with_state's nm hit count (93) exactly matched the Phase 6.2-6.6 baseline for a sixth consecutive rebuild.",
        "frontendTextSearchLimitation": "Unlike Phase 6.2-6.6, this phase introduced no genuinely new frontend strings - the 9 converted panels keep their exact same heading text, only their surrounding element changed from <section>/<h2> to <details>/<summary>. Searching for those pre-existing headings ('Cross-Domain Intelligence', 'Sermon Harvest') and the standard control string ('Needs Attention') all return 0 matches, reproducing the established Tauri-frontend-bundle tooling limitation, though it says nothing about correctness either way here since there is no new text to distinguish a real hit from a control miss. Correctness for this phase's structural, string-identical change instead rests on npm run typecheck's success, the fresh Vite build hash, and the installer's SHA-256 changing from the Phase 6.6 baseline - see pilot-evidence/6.7/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database columns/tables. No panel's content changed - every field, button, and action inside the 9 converted panels renders exactly as before; only the outer wrapper element and default visibility changed. Pending Suggestions and Service Timeline (the two panels with real mid-service actions) are completely unaffected. Service History's Show/Hide button behavior is unchanged from an operator's perspective - only its event no longer also toggles the new outer collapse.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: open Diagnostics Mode and confirm Pending Suggestions and Service Timeline are visible immediately while the other 9 panels start collapsed; click each summary and confirm it expands/collapses correctly without losing any content or control; on Service History, confirm clicking Show/Hide toggles the history list without also collapsing the panel itself."
    },
    "unifiedQueueEditSupportProof": {
        "trigger": "Phase 6's own audit named this gap directly ('unified-queue Edit support') and Phase 6.1's report stated the reason explicitly: editing a reference had no equivalent action in the domain-generic unified action model. This phase opened with a dedicated audit (docs/phase-6-8-audit.md) before any code was written.",
        "audit": "Diagnostics Mode's Pending Suggestions panel already had a full inline Edit flow (editingId/editValue state + commands.editSuggestion, which re-parses typed text as a scripture reference and validates it against the current translation). The Needs Attention queue's action model (actionsFor, one generic one-shot onAction callback) had no edit action and IntelligenceCard had no inline-input rendering path at all - even though item.summary already equals the exact reference text for a scripture-kind bible item, already present in the unified feed, unused.",
        "designChoice": "No genuine architectural fork this phase - the target UI (reuse the existing inline edit pattern from SuggestionCard) was the only reasonable shape. The one real engineering constraint was keyboard-shortcut safety: resolveUnifiedShortcutAction maps 'R' to actions[1] positionally, so edit had to be appended last in actionsFor('bible'), not inserted before reject.",
        "design": "UnifiedItemAction gains 'edit'; actionsFor('bible') returns ['display', 'reject', 'edit']. Only bible gets it - edit_suggestion is inherently scripture-reference-specific, matching Diagnostics Mode's own existing scope (no equivalent command exists for music/sermon/content/correlation). IntelligenceCard gains editingId/editValue/onEditValueChange/onSaveEdit/onCancelEdit props; when editingId equals the item's id, the action-button row is replaced by an inline input + Save/Cancel, mirroring SuggestionCard exactly. AttentionQueue forwards the same props straight through, still never talking to the backend itself. handleUnifiedAction's bible case gained an edit branch that sets the exact same editingId/editValue state Diagnostics Mode's Edit flow already uses (item.summary prefills the input); the new onSaveEdit passed to AttentionQueue calls the exact same commands.editSuggestion Diagnostics Mode's own Save button already calls.",
        "newTests": "2 new cases in actions.test.ts: actionsFor('bible') now returns ['display', 'reject', 'edit'], and a dedicated test confirming edit is last, not before reject, so resolveUnifiedShortcutAction's positional R-to-reject mapping is unaffected.",
        "regressionResult": "Frontend only - zero Rust files changed this phase (confirmed via git status before the rebuild). npm run typecheck: clean. npm run lint: same 5 pre-existing warnings as before this phase - no new ones. npm run test: 258/258 passing (257 pre-existing + 1 new). npm run build: succeeds.",
        "directBinaryProof": "x86_64-w64-mingw32-nm/strings against cip-desktop.exe extracted from this exact installer confirms every previously-tracked Rust-side symbol/string count is unchanged from the Phase 6.7 baseline (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, get_service_report: 1, audioErrorText/speechErrorText: 1 each) - direct proof of zero Rust regression, consistent with this phase touching no Rust files and edit_suggestion (Phase 3.5) being reused entirely unchanged. whisper_full_with_state's nm hit count (93) exactly matched the Phase 6.2-6.7 baseline for a seventh consecutive rebuild.",
        "frontendTextSearchLimitation": "Like Phase 6.7, this phase introduced no genuinely new frontend strings - IntelligenceCard's new edit UI reuses the exact same 'Edit'/'Save'/'Cancel' button labels and 'Edit reference' aria-label Diagnostics Mode's SuggestionCard already has. Searching for 'Edit reference' and the standard control string ('Needs Attention') both return 0 matches, reproducing the established tooling limitation - it says nothing about correctness either way here since there is no new text to distinguish a real hit from a control miss. Correctness instead rests on npm run typecheck's success, the fresh Vite build hash, and the installer's SHA-256 changing from the Phase 6.7 baseline - see pilot-evidence/6.8/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database columns/tables - edit_suggestion (Phase 3.5) is reused entirely unchanged. The unified queue's Edit reuses the exact same editingId/editValue state Diagnostics Mode's Pending Suggestions panel already uses - editing the same Suggestion from either surface can never desync. Every other unified-queue action (display, reject, accept, acknowledge, review, dismiss) is byte-identical to before - Edit is strictly additive to the bible domain's action list. resolveUnifiedShortcutAction's A/R keyboard shortcuts are unaffected, verified by a dedicated test.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: with a Bible reference in the Needs Attention queue, click Edit, confirm the action buttons are replaced by an input pre-filled with the current reference, type a different valid reference, click Save, and confirm the queue updates to the new reference; confirm pressing A/R on the top item still displays/rejects it exactly as before, unaffected by Edit's presence."
    },
    "realAudioFingerprintingProof": {
        "summary": "Phase 7 audited the project's own master-architecture gap list (docs/phase-4-master-plan-gap-audit.md) for what remains after Phase 6's ergonomics arc closed, asked the user which of the five remaining pillars to tackle, and - absent a response - proceeded with the audit's own recommended, most contained option: real audio fingerprinting.",
        "auditFinding": "LocalAcousticMusicRecognizer (integrations/music-acoustic) had been, since Phase 2.2, a deliberately honest scaffold - real configuration/status resolution, but recognize() always returned Unavailable/Error, since no fingerprint algorithm had been chosen or implemented. No acoustic recognition algorithm existed anywhere in the codebase.",
        "designChoice": "No genuine architectural fork - the seam, manifest convention (MODEL_MANIFEST_FILENAME), and desktop wiring (apps/desktop/src-tauri) already existed and required zero changes. The one real decision was which algorithm: spectral landmark (constellation) hashing (Wang 2003, the published Shazam algorithm) - offline, well-understood, testable without real recordings, and pure-Rust-implementable with no native toolchain dependency.",
        "newCode": "integrations/music-acoustic/src/fingerprint.rs (new): STFT-based peak extraction (rustfft), landmark hashing, and offset-histogram voting via a FingerprintIndex. integrations/music-acoustic/src/local.rs (rewritten): LocalAcousticMusicRecognizer::configure() parses an extended manifest ({songs: [{songId, contentId, audioPath}]}), reads each WAV file (hound), downmixes to mono, and enrolls into a real FingerprintIndex; recognize() queries it and maps votes to a capped ConfidenceResult.",
        "newTests": "37 new tests in cip-integrations-music-acoustic: algorithm correctness on synthetic multi-tone signals (self-match, cross-song rejection, time-shift invariance, moderate noise tolerance, silence/empty-index safety, min-votes filtering) plus the full manifest -> real WAV file (hound) -> FingerprintIndex -> recognize() round trip, content_id dataset scoping, and one-bad-reference-file resilience.",
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs (whisper,semantic-search and without). Frontend typecheck/lint/test/build: unchanged - zero frontend files touched this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings/nm against cip-desktop.exe extracted from this exact installer confirms every previously-tracked Rust-side symbol/string count is unchanged from the Phase 6.8 baseline (rejection_echo_count: 5, find_rejected_suggestion_for_reference/record_rejection_echo: 1 each, silentWindowsSkipped: 1, audioErrorText/speechErrorText: 1 each - get_service_report showed a benign line-count artifact, investigated and explained in pilot-evidence/7.1/windows/installer-contents-verification.json, not a real change). whisper_full_with_state's nm hit count (93) exactly matched the Phase 6.2-6.8 baseline for an eighth consecutive rebuild. Unlike every Phase 6.x slice, this phase also required (and found) NEW symbols: LocalAcousticMusicRecognizer's real recognize()/status_reason() implementations, Manifest/ManifestSong's serde Deserialize impls, and 1,308/20 string occurrences of rustfft/hound respectively - direct proof the real implementation is compiled into this exact binary, not just built and tested in this container. Installer size grew by 291,998 bytes and SHA-256 changed from the Phase 6.8 baseline, both expected for a genuine Rust-side dependency addition.",
        "frontendTextSearchLimitation": "Not applicable this phase - zero frontend files were touched, and the rebuilt Vite bundle's asset filenames/sizes are byte-identical to the Phase 6.8 baseline (dist/assets/index-CYX_gW6_.css, dist/assets/index-4mREFPay.js) - correctness for the frontend surface rests on that identity, not a new-string search.",
        "architecturalSafetyNotes": "Zero new Tauri commands, zero new events, zero new database schema - the entire change sits behind AcousticMusicRecognizer, a trait wired end to end since Phase 2.2. core/music (the domain contract crate) is untouched - the algorithm lives entirely in integrations/music-acoustic, honoring the project's own boundary rule that core crates never depend on a specific implementation. NullAcousticMusicRecognizer and ScriptedAcousticMusicRecognizer are byte-identical to before. A build with no manifest configured (the default, every shipped build today) behaves identically to every prior phase - Unavailable, never fabricated recognition.",
        "honestLimitation": "No real recorded audio exists anywhere in this repository or container to enroll or query against - the algorithm is proven correct on synthetic signals only. Its real-world false-positive/false-negative behavior against actual congregational singing, live instrumentation, and room acoustics is genuinely unknown until Environment C testing with real reference recordings happens - more consequential than most prior phases' Environment C gates, since no real audio has ever been enrolled or queried against anywhere.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: create an acoustic-model.json manifest naming one or more real WAV reference recordings of songs already in a Music dataset, restart the app, confirm Diagnostics Mode's acoustic status reports Available, then play (or have the app capture) one of those songs live and confirm it is correctly recognized as a Pending suggestion with a plausible confidence score - and, just as importantly, confirm an unrelated song or ambient service audio does NOT produce a false match."
    },
    "enrollmentWorkflowProof": {
        "summary": "Phase 7.1's own audit named the next gap directly: enrolling a reference recording required hand-writing JSON matching a private manifest schema and manually placing a WAV file - realistically only reachable by whoever built the feature.",
        "auditFinding": "This project had already solved the identical problem twice: Whisper model provisioning (Phase 3.8.7.1) and embedding model/tokenizer provisioning (Phase 4.4), both via a native file picker + an install_* Tauri command that validates, copies, and requires a restart to take effect. No enrollment path reused that pattern for the acoustic manifest. See docs/phase-7-2-audit.md.",
        "designChoice": "No genuine architectural fork - the precedent was already set twice. Two real decisions, both resolved by following it: (1) live reconfigure vs. restart-required - followed the existing 'AppState built once at startup' discipline rather than inventing a new live-reload mechanism this codebase has nowhere else; (2) how an operator identifies a song to enroll - reused search_music (Phase 2.1) as-is.",
        "newCode": {
            "localRs": "integrations/music-acoustic/src/local.rs: Manifest/ManifestSong made pub with Serialize added; enroll_one's WAV validation extracted into shared decode_reference_wav; new pub validate_reference_wav/read_manifest_entries/write_manifest_entries.",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: list_acoustic_enrollments and enroll_acoustic_reference(song_id, content_id, source_path), mirroring install_whisper_model/install_embedding_model_file exactly.",
            "frontend": "domain/music.ts's AcousticEnrollment; commands.ts's listAcousticEnrollments/enrollAcousticReference; LiveChurchBrain.tsx's new 'Reference recordings for real audio fingerprinting' section (song search + native WAV file picker via @tauri-apps/plugin-dialog)."
        },
        "newTests": {
            "rustCrateTestCount": 11,
            "frontendTestCountBeforePhase": 258,
            "frontendTestCountAfterPhase": 261,
            "command": "cargo test -p cip-integrations-music-acoustic && npm run test -- --run",
            "result": "48 passed (Rust crate, 11 new), 261 passed (frontend, 3 new)"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-nm against cip-desktop.exe extracted from this exact installer confirms all 5 expected new symbols present (cip_desktop_lib::commands::enroll_acoustic_reference/list_acoustic_enrollments, cip_integrations_music_acoustic::local::read_manifest_entries/validate_reference_wav/write_manifest_entries), each with a real, distinct address - direct proof this phase's implementation compiled into this exact binary. Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, whisper_full_with_state) is unchanged from the Phase 7.1 baseline - whisper_full_with_state's nm hit count (93) exactly matched the Phase 6.2-7.1 baseline for a ninth consecutive rebuild.",
        "frontendTextSearchLimitation": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe returns 0 matches for this phase's own new UI text ('Reference recordings for real audio fingerprinting', 'Select audio file') - but also 0 matches for the standard control string ('Needs Attention'), reproducing the established Tauri-frontend-bundle tooling limitation a ninth time. Correctness for the frontend surface rests on npm run typecheck's success, the fresh Vite build hash, and the full 261/261 frontend test pass - see pilot-evidence/7.2/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands beyond enroll_acoustic_reference/list_acoustic_enrollments, zero new events, zero new database schema.",
            "core/music (the domain contract crate) is untouched.",
            "NullAcousticMusicRecognizer/ScriptedAcousticMusicRecognizer and every existing acoustic-recognition code path are byte-identical to before this phase.",
            "enroll_acoustic_reference never mutates AppState.acoustic_recognizer - the currently-active recognizer is unaffected until the next restart, exactly matching Whisper/embedding model install precedent."
        ],
        "honestLimitation": "Removing an enrollment is not supported (only add/replace via re-enrolling the same song_id); no live re-recording from within the app (import an existing file only); still no real recorded audio anywhere in this repository or container to actually enroll for a genuine end-to-end test.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the same as Phase 7.1's, now reachable through this phase's own UI: search for a real song, select it, pick a real WAV reference recording via the native file picker, confirm it appears in the enrolled list, restart CIP, confirm Diagnostics Mode's acoustic status reports Available, then play that song live and confirm it is correctly recognized."
    },
    "removeEnrollmentProof": {
        "summary": "Phase 7.2's own 'Known limitations' named the gap directly: 'Removing an enrollment is not supported - an operator can fix a bad enrollment by re-enrolling the same songId (upsert replaces it), but there is no dedicated remove command or button.' Filed as deferred work specifically to keep Phase 7.2's diff focused.",
        "auditFinding": "An operator who enrolled the wrong song, a bad recording, or simply wanted to stop fingerprinting a song had no way to do so short of manually deleting files inside the app's data directory - a filesystem-level operation this project's own established discipline (Phase 3.8.7.1, Phase 4.4, Phase 7.2 itself) treats as something a command should do, not something an operator should be told to do by hand. See docs/phase-7-3-audit.md.",
        "designChoice": "No genuine architectural fork - the direct completion of the CRUD lifecycle Phase 7.2 started (list, add/replace already existed; remove was the only gap). One real decision, resolved by precedent: manifest-entry removal is unconditional/authoritative (it is what actually governs recognition), file deletion is best-effort and non-fatal (logged as a warning, never blocks the command) - the same 'don't leave stale state behind' reasoning write_manifest_entries itself already documents, and the same per-file failure isolation enroll_one already follows.",
        "newCode": {
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: remove_acoustic_reference(song_id) - reads the manifest via read_manifest_entries, errors with an honest 'no enrollment found' AppError if song_id names no entry, writes back the filtered list via write_manifest_entries, then best-effort deletes the entry's audio file (delete failure logged as a warning, never surfaced as a command error). No new crate-level function - composes Phase 7.2's read_manifest_entries/write_manifest_entries as-is.",
            "frontend": "commands.ts's removeAcousticReference(songId); LiveChurchBrain.tsx's enrolled-recordings list gains a per-entry Remove button, restructured onto the existing .live-brain__suggestion-header flex-row pattern already used elsewhere in the Music Intelligence panel."
        },
        "newTests": {
            "rustCrateTestCount": 0,
            "rustCrateTestCountUnchanged": 48,
            "testingBoundaryRationale": "Matches this codebase's established discipline (commands.rs's test module header, reaffirmed in docs/phase-7-3-audit.md): thin command bodies orchestrating already-tested crate-level logic are not separately unit-tested at the Tauri-command layer. install_whisper_model and enroll_acoustic_reference follow the same rule.",
            "frontendTestCountBeforePhase": 261,
            "frontendTestCountAfterPhase": 262,
            "command": "cargo test -p cip-integrations-music-acoustic && npm run test -- --run",
            "result": "48 passed (Rust crate, unchanged), 262 passed (frontend, 1 new)"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-nm -C against cip-desktop.exe extracted from this exact installer confirms the one expected new symbol present (cip_desktop_lib::commands::remove_acoustic_reference) with a real, distinct address, plus every Phase 7.2 symbol (enroll_acoustic_reference, list_acoustic_enrollments, read_manifest_entries, validate_reference_wav, write_manifest_entries) still present and unchanged - direct proof this phase's implementation compiled into this exact binary without disturbing prior functionality. Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, enroll_acoustic_reference, list_acoustic_enrollments) is unchanged from the Phase 7.2 baseline. whisper_full_with_state's strings|grep -c hit count differs (96 vs. the Phase 7.2 baseline of 93) - investigated and confirmed to be the exact same non-deterministic strings/grep line-coalescing artifact this project has already documented and named for this specific symbol (a one-time 96->93 drift first observed at Phase 6.2, now stable at 93 for nine rebuilds before this one); no file in ai/speech, integrations/audio, or any whisper-rs-touching path changed this phase, and nm -C confirms the real exported function symbol itself is unaffected. See pilot-evidence/7.3/windows/installer-contents-verification.json for the full raw-strings review.",
        "frontendTextSearchLimitation": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe returns 0 matches for this phase's own new UI text ('Removed. Restart CIP for it to take effect.') - but also 0 matches for the standard control string ('Needs Attention'), reproducing the established Tauri-frontend-bundle tooling limitation a tenth time. Correctness for the frontend surface rests on npm run typecheck's success, the fresh Vite build hash, and the full 262/262 frontend test pass - see pilot-evidence/7.3/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": [
            "Exactly one new Tauri command (remove_acoustic_reference), zero new events, zero new database schema, zero new crate-level symbols.",
            "core/music (the domain contract crate) is untouched.",
            "list_acoustic_enrollments/enroll_acoustic_reference and every existing acoustic-recognition code path are byte-identical to before this phase.",
            "remove_acoustic_reference never mutates AppState.acoustic_recognizer - the currently-active recognizer is unaffected until the next restart, matching enrollment's own restart-required discipline.",
            "File deletion is strictly ordered after the manifest write succeeds: an entry can never be left half-removed (present in the manifest but its file already gone), and a file-delete failure never leaves the manifest entry behind."
        ],
        "honestLimitation": "No confirmation prompt before removing - the Remove button acts immediately, matching every other operator-decision button in this panel (Accept/Reject on findings). Still no real recorded audio anywhere in this repository or container to actually exercise enroll-then-remove end to end against a genuine acoustic match.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is now the full enroll/list/remove lifecycle: enroll a real reference recording (per Phase 7.2's own gate), confirm it appears in the list, click Remove, confirm it disappears from the list and its WAV file is gone from the model directory, restart CIP, and confirm that song is no longer recognized."
    },
    "productionIntegrationProof": {
        "summary": "docs/phase-4-master-plan-gap-audit.md and docs/phase-7-audit.md both graded 'OBS/vMix/livestream integration' NOT STARTED - integrations/obs and integrations/vmix were 6-line placeholder crates, explicitly out of scope since Phase 1. The user explicitly named Production Integration as the next phase and supplied two external product-analysis documents, both converging on the same target the repo's own audit already identified.",
        "auditFinding": "Nothing fed CIP's live presentation output anywhere beyond its own Tauri display windows - a church already running OBS (for a livestream) or vMix (for a broadcast production) had no way to have CIP push the currently-displayed verse/slide text into that pipeline. See docs/phase-8-audit.md.",
        "designChoice": "Three real decisions, all resolved in the audit: (1) scope is text/title push into an operator-designated source, never scene switching or recording/streaming control - the direct analog of what CIP's own Stage/Confidence/Lobby screens already do; (2) a fresh connection per push, not a persistent session, which also makes config live-editable without a restart (no engine to rebuild, only connection parameters read fresh); (3) best-effort, on its own worker thread, never fatal to CIP's own local display.",
        "protocolChoice": "obs-websocket v5 (the real, published protocol - Hello/Identify/Identified handshake, SHA256-based challenge/response auth, SetInputSettings request) over plain ws://; vMix's real, published HTTP SetText API over plain http://. Both local-network-only by design, so no TLS backend was needed or pulled in - verified via cargo tree before this rebuild and re-confirmed by this build's own compile log showing no rustls/ring/webpki-roots crates compiled.",
        "newCode": {
            "obsCrate": "integrations/obs/src/lib.rs: ObsTarget/ObsError, push_text (real handshake + SHA256 auth + SetInputSettings request/response, 6 tests against a real in-process WebSocket server).",
            "vmixCrate": "integrations/vmix/src/lib.rs: VmixTarget/VmixError, push_text (real SetText GET request, 4 tests against a real hand-rolled HTTP/1.1 server).",
            "productionRs": "apps/desktop/src-tauri/src/production.rs: ProductionIntegrationConfig/ProductionIntegrationStatus (in AppState, in-memory, default disabled), push_to_configured_targets (best-effort, worker-thread, no-op when nothing configured), slide_push_text, test_obs_connection/test_vmix_connection.",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: set_production_integration_config, get_production_integration_status, test_obs_connection, test_vmix_connection - wired into display_presentation and clear_active_presentation, the exact two call sites broadcast_to_live_screens already uses.",
            "frontend": "domain/production.ts; commands.ts wrappers; new components/workspace/ProductionIntegrationPanel.tsx (OBS/vMix host/port/credentials/source fields behind an enable checkbox each, Test Connection buttons, last-push status), wired into LiveChurchBrain.tsx only."
        },
        "newTests": {
            "obsCrateTestCount": 6,
            "vmixCrateTestCount": 4,
            "testingBoundaryRationale": "Neither OBS nor vMix is installed in this container. Both client crates are proven against real, protocol-correct in-process servers (a real tungstenite WebSocket handshake for OBS, a hand-rolled HTTP/1.1 server for vMix) rather than mock traits - the wire format itself is tested. production.rs's own worker-thread dispatch is thin glue over these already-tested clients and follows this codebase's established no-redundant-command-level-tests discipline (commands.rs's test-module header; Phase 7.3's identical reasoning for remove_acoustic_reference).",
            "frontendTestCountBeforePhase": 261,
            "frontendTestCountAfterPhase": 266,
            "command": "cargo test -p cip-integrations-obs -p cip-integrations-vmix && npm run test -- --run",
            "result": "6 passed (obs), 4 passed (vmix), 266 passed (frontend, 5 new)"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-nm -C against cip-desktop.exe extracted from this exact installer confirms all 8 expected new symbols present (cip_desktop_lib::commands::set_production_integration_config/get_production_integration_status/test_obs_connection/test_vmix_connection, cip_desktop_lib::production::push_to_configured_targets/slide_push_text, cip_integrations_obs::push_text, cip_integrations_vmix::push_text), each with a real, distinct address - plus a drop-glue symbol for the worker thread's own spawned closure, direct proof the thread-spawn dispatch itself (not just the function definitions) is compiled into this exact binary. Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, enroll_acoustic_reference, list_acoustic_enrollments, remove_acoustic_reference, whisper_full_with_state) is unchanged from the Phase 7.3 baseline.",
        "frontendTextSearchLimitation": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe returns 0 matches for the standard control string ('Needs Attention'), reproducing the established Tauri-frontend-bundle tooling limitation an eleventh time - so this says nothing about correctness either way. Correctness for the frontend surface rests on npm run typecheck's success, the fresh Vite build hash, and the full 266/266 frontend test pass - see pilot-evidence/8/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": [
            "Exactly four new Tauri commands, zero new events, zero new database schema.",
            "core/presentation and core/music (domain contract crates) are untouched - this phase adds a new sink, not a new domain concept.",
            "broadcast_to_live_screens and every existing DisplayScreen/RouteMode code path are byte-identical to before this phase - the production push is purely additive alongside the existing broadcast, never a replacement.",
            "production_integration_config/production_integration_status are in-memory only, defaulting to disabled - a build with nothing configured behaves identically to before this phase."
        ],
        "honestLimitation": "Text/title push only - no scene switching, no source visibility toggling, no recording/streaming control, no NDI output. Config does not persist across restarts. vMix's own SetText API returns a bare HTTP 200 regardless of whether the named input/field actually exists. Neither OBS nor vMix is installed in this container, so neither client has ever been exercised against the real applications.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: with real OBS and/or vMix running, configure a target in the Production Integration panel, click Test Connection and confirm the named source/input updates, then Save and confirm displaying a real verse/slide in CIP updates that same source/input live, and that clearing the display blanks it."
    },
    "translationRegistryV2Proof": {
        "summary": "User-supplied licensing-strategy research (YouVersion Platform vs. API.Bible vs. direct publisher licensing) with the instruction to consider it and keep going. See docs/phase-9-audit.md.",
        "auditFinding": "generate_verse_embeddings sent every verse of a translation's text into a local AI embedding model with zero licensing check - safe only by accident, since BSB (the only translation ever embedded) happens to be public domain. LicensingStatus is a coarse admission gate (can this be bulk-imported at all); nothing governed what CIP may then do with an already-admitted translation.",
        "designChoice": "UsagePermissions lives alongside LicensingStatus on ContentMetadata/content_registry (same home, different axis). Every field defaults to None/unknown, never permissive. Only ai_processing_allowed is enforced this phase, fail-closed - deliberately the opposite default from ensure_translation_selectable's fail-open, since AI processing needs affirmative evidence, not absence of a denial.",
        "preExistingBugFixed": "generate_verse_embeddings and get_embedding_capabilities both hardcoded the literal DEFAULT_TRANSLATION_ID (\"KJV\", a dev-fixture id never registered in a real production build) instead of resolve_default_translation_id - fixed as part of touching these exact call sites for the licensing gate; previously these commands were silently a no-op against any real production (BSB-only) database.",
        "newCode": {
            "coreContent": "core/content/src/lib.rs: UsagePermissions (14 fields, 8 permits_* helpers), ContentMetadata.usage field.",
            "migration": "database/migrations/0016_translation_usage_permissions.sql: 14 new nullable columns on content_registry.",
            "integrationsContent": "integrations/content/src/lib.rs: SqliteContentRegistry reads/writes every new column.",
            "integrationsBible": "integrations/bible/src/importer.rs: TranslationInput.usage (optional, #[serde(default)]).",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: ensure_ai_processing_permitted (pure, fail-closed), wired into generate_verse_embeddings; both that command and get_embedding_capabilities now resolve the real default translation.",
            "bsbDataset": "database/datasets/bsb/bsb.json + docs/data/bible/BSB/manifest.json: BSB's real usage permissions recorded as evidence (every flag true except training_allowed, left null)."
        },
        "newTests": {
            "coreContentTestCount": 3,
            "integrationsContentTestCount": 2,
            "integrationsBibleTestCount": 2,
            "contentRsTestCount": 1,
            "commandsRsTestCount": 3,
            "bibleProductionDatasetTestCount": 1,
            "testingBoundaryRationale": "ensure_ai_processing_permitted is a pure function tested against a real in-memory SQLite registry (SqliteContentRegistry + cip_database::run_migrations), no tauri::test harness - the same split this codebase already uses for resolve_default_translation_id_from_registry.",
            "command": "cargo test --workspace && cargo test --features whisper (apps/desktop/src-tauri) && npm run test -- --run",
            "result": "977 passed (workspace default), 320 passed (desktop, --features whisper), 266 passed (frontend) - 0 failures across all three"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-nm -C against cip-desktop.exe extracted from this exact installer confirms generate_verse_embeddings, get_embedding_capabilities, resolve_default_translation_id, and UsagePermissions (9 occurrences) all present. ensure_ai_processing_permitted and UsagePermissions::permits_ai_processing themselves show 0 nm hits - both are small, single-call-site functions the release optimizer inlines away, an expected and previously-unremarkable Rust-inlining outcome, not evidence of absence. Direct strings evidence closes that gap: the literal SQL migration text migration 0016 embeds via include_str! is present (ai_processing_allowed: 5, rights_holder: 4, llm_prompt_allowed: 4, training_allowed: 4, 0016_translation_usage_permissions: 1 occurrences) - this can only be true if migration 0016 is compiled into this binary. Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, enroll_acoustic_reference, list_acoustic_enrollments, remove_acoustic_reference, whisper_full_with_state) is unchanged from the Phase 8 baseline, and Phase 8's own Production Integration symbols (set_production_integration_config, test_obs_connection, cip_integrations_obs::push_text, cip_integrations_vmix::push_text) are all still present and unregressed.",
        "frontendTextSearchLimitation": "x86_64-w64-mingw32-strings against the extracted cip-desktop.exe returns 0 matches for the standard control string ('Needs Attention'), reproducing the established Tauri-frontend-bundle tooling limitation a twelfth time - so this says nothing about correctness either way. This phase made no frontend UI copy changes; correctness for the frontend surface rests on npm run typecheck's success, the fresh Vite build hash, and the full 266/266 frontend test pass - see pilot-evidence/9/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events - generate_verse_embeddings and get_embedding_capabilities are the only two existing command bodies changed.",
            "core/bible and core/service (Bible detection) have zero symbol-level changes - licensing lives only in ContentMetadata/content_registry, never in the detection or provider-serving path.",
            "Every prior ContentMetadata construction site (8 files, 11 literals) updated to populate the new field explicitly - reviewed individually, none silently defaulted.",
            "Migration 0016 is purely additive (14 nullable ALTER TABLE ADD COLUMN statements) - confirmed via the literal SQL strings above; no existing content_registry row's data changes shape."
        ],
        "honestLimitation": "Only ai_processing_allowed is actually enforced - the other seven UsagePermissions fields are recorded and queryable but gate nothing yet, since no other call path in this codebase currently needs to restrict them. No operator-facing UI to view/edit UsagePermissions after import. No YouVersion/API.Bible network client code, no new Bible text, no Bible Society of Nigeria contact - see docs/bible-translation-licensing-roadmap.md's Phase B/C, both out of this session's reach (no real API keys or legal authority).",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: with a real installed CIP and BSB loaded, click 'Generate verse embeddings' (or the equivalent Diagnostics Mode action) and confirm it still succeeds now that the ai_processing_allowed gate is wired in, since BSB's dataset asset records that permission as true."
    },
    "rolesPermissionsProof": {
        "summary": "User's explicit instruction: 'Keep going into church/user roles & permissions next' - item 5 in docs/phase-4-master-plan-gap-audit.md's own Phase 4 candidate list, graded NOT STARTED ('no login, no user table, no role enforcement'). See docs/phase-10-audit.md.",
        "auditFinding": "No multi-user model existed at all - the app had no login, no user table, no role enforcement; every operator action, including sensitive configuration (OBS/vMix credentials, Bible dataset import, AI model installation), was equally available to anyone who could open the app.",
        "designChoice": "Two closed roles (Admin/Operator, not a general RBAC framework). Local PIN-based authentication (base64(sha256(salt||pin)), the same algorithm shape Phase 8's OBS auth already uses) - chosen over bcrypt/argon2 since the threat model is a single local desktop machine, not a networked credential. Session-only login (AppState.current_operator, never persisted, identical precedent to screen_route_modes). A bootstrap rule: the first account ever created needs no login and always becomes Admin. A concrete, real gate on exactly the commands that are configuration rather than day-to-day operation.",
        "newCode": {
            "coreAccess": "core/access/src/lib.rs: Role, OperatorAccount, OperatorAccountStore trait, AccessError, generate_salt/hash_pin/verify_pin.",
            "migration": "database/migrations/0017_operator_accounts.sql: operator_accounts table.",
            "integrationsAccess": "integrations/access/src/lib.rs: SqliteOperatorAccountStore.",
            "accessRs": "apps/desktop/src-tauri/src/access.rs: OperatorSession, ensure_admin (pure, fail-closed), create_operator_account (bootstrap logic), login.",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: OperatorAccountSummaryDto (never carries pinHash/pinSalt); 5 new commands (list_operator_accounts, create_operator_account, login, logout, get_current_operator); ensure_admin/ensure_admin_string_err gates added to 7 existing commands.",
            "stateRs": "apps/desktop/src-tauri/src/state.rs: operator_account_store (own connection), current_operator (Mutex<Option<OperatorSession>>, session-only).",
            "errorsRs": "apps/desktop/src-tauri/src/errors.rs: new AppError::Forbidden variant, categorized under LogCategory::Security.",
            "frontend": "domain/access.ts; 5 new commands.ts wrappers; new components/LoginScreen.tsx (full-app gate mirroring WebRuntimeNotice's pattern); App.tsx gains currentOperator state, the login gate, and a Log Out control."
        },
        "newTests": {
            "coreAccessTestCount": 6,
            "integrationsAccessTestCount": 6,
            "accessRsTestCount": 12,
            "frontendCommandsTestCount": 6,
            "frontendContractTestCount": 2,
            "testingBoundaryNote": "ensure_admin/create_operator_account/login are pure functions tested against a real in-memory SQLite store (cip_database::open_in_memory + run_migrations + SqliteOperatorAccountStore) - the same resolve_default_translation_id_from_registry/ensure_ai_processing_permitted pattern already established (no tauri::test harness exists). No existing Rust test called any of the 7 newly-gated commands as a #[tauri::command] wrapper (verified before adding the gates), so the gates could not have broken any pre-existing test.",
            "command": "cargo test --workspace && cargo test --features whisper (apps/desktop/src-tauri) && npm run test -- --run",
            "result": "1001 passed (workspace default, +24 from Phase 9's 977), 332 passed (desktop, --features whisper, +12 from Phase 9's 320), 274 passed (frontend, +8 from Phase 9's 266) - 0 failures across all three"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms every new Phase 10 command name, the DTO type name, and the migration-0017 schema strings are present (ensure_admin: 3, create_operator_account: 3, login: 9, logout: 2, list_operator_accounts: 2, get_current_operator: 2, OperatorAccountSummaryDto: 7, operator_accounts: 9, pin_hash: 4, pin_salt: 4, display_name: 7 occurrences) - direct proof the new crates (core/access, integrations/access), the new access.rs orchestration module, and the five new Tauri commands compiled into and shipped inside the real Windows executable. Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, enroll_acoustic_reference, list_acoustic_enrollments, remove_acoustic_reference, whisper_full_with_state) is unchanged from the Phase 9 baseline, whisper_full_with_state stable at 96 for a 4th consecutive rebuild, and Phase 8/9's own symbols (test_obs_connection, set_production_integration_config, ai_processing_allowed, LicensingStatus) are all still present and unregressed. compute_auth_response, ensure_ai_processing_permitted, and resolve_default_translation_id_from_registry show 0 occurrences - the same 'inlined away by the release optimizer, not absent' pattern already documented for these exact symbols in Phase 9's own installer-contents-verification.json.",
        "frontendTextSearchLimitation": "The frontend bundle (dist/assets/index-*.js) is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe itself in the same way Rust symbols are - LoginScreen.tsx's own text was not independently string-searched in this phase, consistent with every prior phase's installer-contents-verification methodology in this project. This phase's frontend correctness rests on npm run typecheck's success, the fresh Vite build hash, and the full 274/274 frontend test pass - see pilot-evidence/10/windows/installer-contents-verification.json for the complete breakdown.",
        "architecturalSafetyNotes": [
            "5 new Tauri commands, zero new events, one new migration (0017, purely additive - a new table, no existing table's shape changes).",
            "Every one of the 7 newly Admin-gated commands still contains its full pre-Phase-10 logic unchanged below the new gate call - a build running with a logged-in Admin session behaves identically to the Phase 9 installer for every one of those seven commands.",
            "generate_verse_embeddings now carries two independent gates (Phase 9's ai_processing_allowed licensing check and Phase 10's ensure_admin role check) - both symbol groups are present and unregressed, confirming neither gate was accidentally removed by the other's addition.",
            "core/bible, core/service, core/presentation (every domain contract crate) are entirely untouched."
        ],
        "honestLimitation": "No password recovery - an Admin locked out has no in-app recovery path. No account editing/deactivation/deletion - list/create only; an operator who has left cannot be removed from the account list today. No per-action audit attribution - audit_events/timeline record what happened, not which operator triggered it. The login screen is a workflow control, not itself the security boundary - the real boundary is the seven ensure_admin calls inside the Rust commands. No multi-church/multi-tenant data isolation - still one SQLite database per install. The LoginScreen UI was not visually verified via a real Xvfb+screenshot pass in this phase - judged proportionate since it is ordinary React form content, not a new windowing/pixel-placement mechanism, but stated honestly rather than silently implied verified.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: create the first Admin account (bootstrap), log out, log back in as that Admin, then create a second (Operator-role) account, log in as that Operator, and confirm a genuinely gated action (e.g. Set Production Integration Config) is refused - see physicalHardwareStatement item 19."
    },
    "congregantCompanionProof": {
        "summary": "User's own item 11 from a pasted advice list ('a tiny local-network HTTP server broadcasting the current slide/verse to a phone browser... for follow along and save a note'), followed by the explicit instruction 'Keep going into the local congregant companion view next'. See docs/phase-11-audit.md.",
        "auditFinding": "No way for a congregant to follow along with Stage on their own device existed at all - the only output was the projector/TV Stage window itself.",
        "designChoice": "Mirrors Stage's existing Active PresentationItem exactly, never a second content path. No congregant data ever reaches CIP - notes are localStorage-only on the phone, the server has no write endpoint at all. Admin-gated to enable (joining Phase 10's seven gated commands), not to view. Plain std::net::TcpListener, hand-rolled, no new dependency, mirroring Phase 8's own plain-http-for-LAN-local-protocol reasoning. Fixed port 49876 (IANA dynamic/private range). Best-effort LAN address detection via the dependency-free UdpSocket::connect routing-table trick, honestly returning no candidate address rather than fabricating one when no route exists.",
        "newCode": {
            "companionRs": "apps/desktop/src-tauri/src/companion.rs: CompanionSnapshot, CompanionServerHandle, CompanionStatus, CompanionError, spawn_server/stop_server (real Tauri-agnostic TcpListener server with a genuine stop mechanism), enable/disable/status/update_snapshot, the served self-contained HTML page.",
            "stateRs": "apps/desktop/src-tauri/src/state.rs: companion_snapshot (Arc<Mutex<Option<CompanionSnapshot>>>), companion_server (Mutex<Option<CompanionServerHandle>>, None by default).",
            "errorsRs": "apps/desktop/src-tauri/src/errors.rs: new AppError::Companion variant, categorized under LogCategory::Network.",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: CompanionStatusDto; 3 new commands (enable_congregant_companion, disable_congregant_companion - Admin-gated; get_congregant_companion_status - open); companion::update_snapshot hooked into display_presentation and clear_active_presentation, the same two call sites production::push_to_configured_targets already uses.",
            "frontend": "domain/companion.ts; 3 new commands.ts wrappers; new components/workspace/CongregantCompanionPanel.tsx (mirrors ProductionIntegrationPanel's shape); wired into LiveChurchBrain.tsx."
        },
        "newTests": {
            "companionRsTestCount": 9,
            "companionRsRealSocketTestCount": 5,
            "frontendCommandsTestCount": 4,
            "frontendContractTestCount": 2,
            "testingBoundaryNote": "Unlike almost every other Tauri-command-backed feature in this codebase, companion.rs's core server logic is entirely Tauri-agnostic (operates on a plain Arc<Mutex<Option<CompanionSnapshot>>>, never an AppHandle/State), so 5 of its 9 tests bind a real TcpListener on an OS-assigned ephemeral port and make real TcpStream GET requests against it - not pure-function proxies for an untestable command.",
            "command": "cargo test --workspace && cargo test --features whisper (apps/desktop/src-tauri) && npm run test -- --run",
            "result": "1010 passed (workspace default, +9 from Phase 10's 1001), 341 passed (desktop, --features whisper, +9 from Phase 10's 332), 280 passed (frontend, +6 from Phase 10's 274) - 0 failures across all three"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms the exact literal command names in the Tauri invoke_handler registration list (enable_congregant_companion, disable_congregant_companion, get_congregant_companion_status), the CompanionSnapshot/status JSON field names (active, heading, bodyLines, footer, updatedAt), both mutex-poisoned panic messages carrying the source path apps/desktop/src-tauri/src/companion.rs, the UDP routing-table-trick addresses (0.0.0.0:0, 8.8.8.8:80), the operator-facing log lines (congregant companion server enabled/disabled on port), and the served page's own localStorage key (cip-companion-notes) and notes-disclosure copy (saved only on your phone) - comprehensive direct proof this phase's server, commands, and served content compiled into and shipped inside the real Windows executable. Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, enroll_acoustic_reference, list_acoustic_enrollments, remove_acoustic_reference, whisper_full_with_state) is unchanged from the Phase 10 baseline, whisper_full_with_state stable at 96 for a 5th consecutive rebuild, and Phase 8/9/10's own symbols (test_obs_connection, set_production_integration_config, ai_processing_allowed, LicensingStatus, ensure_admin, create_operator_account, login, logout, operator_accounts, pin_hash) are all still present and unregressed.",
        "frontendTextSearchLimitation": "The frontend bundle is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe in the same way Rust symbols are - CongregantCompanionPanel.tsx's own UI copy was not independently string-searched, consistent with every prior phase's methodology. Frontend correctness for this phase rests on npm run typecheck's success, the fresh Vite build hash, and the full 280/280 frontend test pass.",
        "architecturalSafetyNotes": [
            "3 new Tauri commands, zero new events, zero new migrations - no persistence at all, the snapshot is in-memory/session-scoped, identical precedent to production_integration_config/current_operator.",
            "The two companion::update_snapshot call sites sit immediately beside production::push_to_configured_targets's own existing calls in display_presentation/clear_active_presentation - neither call's existing behavior is touched.",
            "core/bible, core/service, core/presentation (every domain contract crate) are entirely untouched - the companion server only ever reads the same RenderedSlide the display-window path already produces.",
            "No new workspace dependency - spawn_server/stop_server use only std::net/std::sync/std::thread."
        ],
        "honestLimitation": "No authentication, no rate limiting, no connection cap on the companion server itself - a LAN-only, read-only, no-PII surface judged not to need either for this phase; anyone on the same LAN can open the page while it's running, the same thing anyone in the room can already see on the projector. No QR code / mDNS auto-discovery - the operator shares the address manually. No push/WebSocket - two-second polling. No history - only the current item. IPv4 only. Address detection can honestly return nothing on a machine with no configured network route.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is the operator's own real-hardware test: enable the companion server, read the shown address, open it on a real phone on the same LAN, and confirm it tracks Stage live (displaying and clearing items) - see physicalHardwareStatement item 20."
    },
    "multiLanguageWhisperProof": {
        "summary": "User's own item 9 from a pasted advice list ('Multi-language Whisper - currently pinned to en...'), followed by the explicit instruction 'Keep going into multi-language Whisper next'. See docs/phase-12-audit.md.",
        "auditFinding": "ai/speech/src/whisper.rs's run_inference never called FullParams::set_language at all - whisper-rs's own default is en (whisper_full_default_params()), so every transcription this codebase has ever run was implicitly English-only, not configurable, not auto-detecting.",
        "premiseVerification": "Checked directly against whisper-rs-sys-0.13.1's vendored whisper.cpp/src/whisper.cpp g_lang table (the real 100-language vocabulary baked into every multilingual model's tokenizer): Yoruba (yo, id 66) and Hausa (ha, id 95) are both real, trained entries. Igbo has no entry at all, under any code - a hard model-architecture limitation, not a missing CIP config option.",
        "designChoice": "English (default), Yoruba, Hausa, and Auto-detect offered - all four verified real Whisper languages. Igbo deliberately excluded with the evidence above. Live-editable via new SpeechEngine::set_language trait method (default no-op), applied starting with the next inference window. Every real inference pass reads back the language whisper.cpp actually used via full_lang_id_from_state + get_lang_str, replacing the permanently-None TranscriptSegment.language field. WhisperContext::is_multilingual() detected once at load time and surfaced honestly. Not Admin-gated - a live-workflow choice like translation/audio-device selection.",
        "newCode": {
            "coreAi": "core/ai/src/speech_engine.rs: new SpeechEngine::set_language trait method, default no-op.",
            "aiSpeechLib": "ai/speech/src/lib.rs: SUPPORTED_LANGUAGES (en/yo/ha/auto, feature-unconditional), is_supported_language.",
            "aiSpeechWhisper": "ai/speech/src/whisper.rs: requested_language field (replaces the dead language: Option<String>), is_multilingual(), run_inference sets language before decoding and reads back the real used/detected language after, set_language trait impl.",
            "stateRs": "apps/desktop/src-tauri/src/state.rs: AppState.speech_language (Mutex<String>, default en), SpeechDiagnostics.model_is_multilingual (Option<bool>).",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: SpeechLanguageOptionDto/SpeechLanguageCapabilitiesDto; get_speech_language_capabilities (open) and set_speech_language (validates + applies immediately).",
            "frontend": "domain/speech.ts; 2 new commands.ts wrappers; language <select> in LiveChurchBrain.tsx plus an honest English-only-model notice."
        },
        "newTests": {
            "aiSpeechLibTestCount": 4,
            "testingBoundaryNote": "Everything requiring a real loaded multilingual model is untestable in this environment for the same documented reason every other Whisper-model test in this codebase already is (model host blocked). SUPPORTED_LANGUAGES's exact contents and is_supported_language's validation logic are pure functions, directly tested - including an explicit igbo_is_never_offered_as_a_supported_language regression guard.",
            "command": "cargo test --workspace && cargo test -p cip-ai-speech --features whisper && npm run test -- --run",
            "result": "1014 passed (workspace default, +4 from Phase 11's 1010), 15 passed (cip-ai-speech, --features whisper, up from 11 pre-Phase-12), 285 passed (frontend, +5 from Phase 11's 280) - 0 failures across all three"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms both new Tauri command names (get_speech_language_capabilities, set_speech_language), the SpeechLanguageCapabilitiesDto JSON field names (currentLanguage, supportedLanguages, modelIsMultilingual), the set_speech_language log/error message text, and - most decisively - the literal human-readable language names Yoruba, Hausa, and Auto-detect from the SUPPORTED_LANGUAGES table itself, proving the exact intended language set compiled into this binary. Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, enroll_acoustic_reference, list_acoustic_enrollments, remove_acoustic_reference, whisper_full_with_state) is unchanged from the Phase 11 baseline, whisper_full_with_state stable at 96 for a 6th consecutive rebuild, and prior-phase symbols (test_obs_connection, ensure_admin, operator_accounts, enable_congregant_companion, companion_snapshot) are all still present and unregressed.",
        "frontendTextSearchLimitation": "The frontend bundle is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe in the same way Rust symbols are - the new language <select> control's own UI copy was not independently string-searched, consistent with every prior phase's methodology. Frontend correctness for this phase rests on npm run typecheck's success, the fresh Vite build hash, and the full 285/285 frontend test pass.",
        "architecturalSafetyNotes": [
            "2 new Tauri commands, zero new events, zero new migrations - no persistence at all, the selection is in-memory/session-scoped, identical precedent to production_integration_config/current_operator/companion_snapshot.",
            "SpeechEngine::set_language's default no-op means NullSpeechEngine/ScriptedSpeechEngine are completely unaffected - the trait addition changes zero existing behavior for either.",
            "WhisperSpeechEngine's pre-Phase-12 default behavior (implicit English, whisper.cpp's own C-level default) is preserved exactly for any caller that never touches the new setting - requested_language defaults to en, the same value whisper.cpp would have used anyway.",
            "core/bible, core/service, core/presentation (every domain contract crate) are entirely untouched."
        ],
        "honestLimitation": "No Igbo support - a hard Whisper model-architecture limitation, verified against the vendored source, not a missing CIP config option. No per-service language history or mid-service auto-switching - one selection applies until changed again; Auto-detect re-detects every inference window independently rather than locking onto a detected language across windows. No translation - whisper.cpp's own translate flag is untouched. No language-specific Bible-reference detection tuning - core/bible remains English-text-oriented regardless of the transcription language.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a real operator selecting Yoruba or Hausa with a real multilingual model installed, speaking in that language, and confirming both the transcript and its reported language field are correct; and separately confirming an English-only model honestly reports modelIsMultilingual: false and still transcribes in English regardless of the selection - see physicalHardwareStatement item 21."
    },
    "churchKnowledgeBaseProof": {
        "summary": "The last remaining item from the user's own pasted advice list ('nothing aggregates it across services yet'), followed by the explicit instruction 'church knowledge base / cross-sermon analytics'. See docs/phase-13-audit.md.",
        "auditFinding": "IntelligenceFinding (every domain, Sermon included) is never persisted to any database table - AppState.intelligence_findings: Mutex<FindingQueue> is constructed once at startup (state.rs:433) and never cleared anywhere in this codebase, so it does not survive an application restart. Sermon Harvest (Phase 3.9) already documents its own inability to do cross-sermon work for exactly this reason.",
        "premiseVerification": "Confirmed IntelligenceFinding already carries sermon_id: Option<Uuid> (added Phase 2.6), set only from IntelligenceContext::active_sermon - no new correlation logic was needed to attribute a finding to its sermon. Confirmed the summary.starts_with(...) text-prefix convention (e.g. 'Theme: ') is already used for logic branching elsewhere in this exact codebase (service.rs, sermon_foundation.rs, sermon.rs, pipeline.rs), not invented for this phase.",
        "designChoice": "New saved_sermon_findings table, written only inside accept_sermon_finding (mirrors Phase 2.7.1's saved_content_candidates/accept_content_candidate precedent exactly - explicit operator acceptance, never automatic). New sermon_knowledge_base.rs module (mirrors harvest.rs's discipline: no detection logic of its own, no invented titles/labels) aggregates theme frequency, sermons-by-speaker, and a recent-findings feed from the durable table plus every persisted sermon. Theme grouping is exact-label match only, no semantic similarity. Open to any operator (read-only, retrospective).",
        "newCode": {
            "migration": "database/migrations/0018_saved_sermon_findings.sql (version 18) - saved_sermon_findings table.",
            "persistenceRs": "apps/desktop/src-tauri/src/persistence.rs: persist_saved_sermon_finding, list_all_saved_sermon_findings, list_sermons.",
            "sermonKnowledgeBaseRs": "apps/desktop/src-tauri/src/sermon_knowledge_base.rs (new module): element_label_for_summary, build_knowledge_base, get_knowledge_base.",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: accept_sermon_finding now persists on accept; new command get_church_knowledge_base.",
            "frontend": "domain/sermon.ts (SermonRef/ThemeFrequencyEntry/SpeakerHistoryEntry/SermonKnowledgeBase); lib/commands.ts (getChurchKnowledgeBase); components/library/HistoryView.tsx (new Church Knowledge Base section)."
        },
        "newTests": {
            "sermonKnowledgeBaseRsTestCount": 9,
            "testingBoundaryNote": "Every new Rust function is either a pure function (element_label_for_summary, build_knowledge_base) or a thin read composed of two already-tested persistence.rs functions (get_knowledge_base) - unlike most other phases in this project, none of it depends on a Whisper model, audio, or any external dependency, so full coverage was possible with no testing-boundary carve-out.",
            "command": "cargo test --workspace && cargo test --features whisper (apps/desktop/src-tauri) && npm run test -- --run",
            "result": "1023 passed (workspace default, +9 from Phase 12's 1014, all in cip-desktop: 350 up from 341), 350 passed (cip-desktop, --features whisper, matching default), 287 passed (frontend, +2 from Phase 12's 285) - 0 failures across all three"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms the new Tauri command name (get_church_knowledge_base), the new table/persistence-function names (saved_sermon_findings, persist_saved_sermon_finding), the SermonKnowledgeBase JSON field names (themeFrequency, sermonsBySpeaker, recentFindings, occurrenceCount, sermonCount), and - most decisively - the literal 'Theme: ' text-prefix string the cross-sermon theme-grouping logic depends on. Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, enroll_acoustic_reference, list_acoustic_enrollments, remove_acoustic_reference, whisper_full_with_state) is unchanged from the Phase 12 baseline, whisper_full_with_state stable at 96 for a 7th consecutive rebuild, and prior-phase symbols (test_obs_connection, ensure_admin, operator_accounts, enable_congregant_companion, companion_snapshot, get_speech_language_capabilities, set_speech_language) are all still present and unregressed.",
        "frontendTextSearchLimitation": "The frontend bundle is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe in the same way Rust symbols are - the new Church Knowledge Base section's own UI copy was not independently string-searched, consistent with every prior phase's methodology. Frontend correctness for this phase rests on npm run typecheck's success, the fresh Vite build hash, and the full 287/287 frontend test pass.",
        "architecturalSafetyNotes": [
            "1 new Tauri command, zero new events, 1 new migration - justified per docs/phase-13-audit.md's persistence-decision precedent (a knowledge base that must survive restarts and span services cannot, by definition, be in-memory).",
            "The only new write path is inside accept_sermon_finding, an already-explicit, already-audited operator action (spec section 36/47: 'no automatic actions') - Detected/Reviewed/Rejected findings are never persisted to saved_sermon_findings.",
            "sermon_knowledge_base.rs contains no detection logic of its own and invents no title/label when one is absent, mirroring harvest.rs's own discipline exactly.",
            "core/bible, core/service, core/presentation, core/music (every other domain contract crate) are entirely untouched."
        ],
        "honestLimitation": "Only operator-accepted findings appear in the knowledge base (deliberate, not an oversight). No retroactive backfill - findings from services before this phase shipped are gone, since they were never durable in the first place. Theme grouping is exact-label match only - no semantic similarity in this phase. A finding accepted with no active sermon context still counts as a Theme occurrence but is attributed to zero sermons.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a real operator accepting a real Sermon Intelligence finding during a real service, restarting the application, and confirming the Church Knowledge Base still shows it correctly grouped by theme and speaker - see physicalHardwareStatement item 22."
    },
    "whisperHallucinationFilteringProof": {
        "summary": "The first genuine Environment C bug report this project has received: two real Windows pilot screenshots showing the Live Transcript panel full of whisper.cpp's own non-speech placeholder captions, every line at an identical hardcoded 75% confidence, a 6% input level, and zero Bible detections. See docs/phase-14-audit.md.",
        "auditFinding": "Every confidence badge was a hardcoded ConfidenceResult::new(0.75, ...) literal in ai/speech/src/whisper.rs. Its own comment ('no per-token confidence exposed by this API') was itself false, confirmed against the vendored whisper-rs-0.14.4 source: WhisperState::full_n_tokens/full_get_token_prob are real, implemented accessors this code simply never called. Separately, the reported bracketed/parenthesized lines ('[BLANK_AUDIO]', '(speaking in foreign language)', '[inaudible]', '[LAUGHTER]') are whisper.cpp's own well-documented non-speech placeholder captions - literal training-data artifacts, not random hallucination - and nothing in this codebase recognized them as not-real-speech before displaying, persisting, and feeding them into Bible/Sermon/Music/Content detection exactly as if the congregation had said them.",
        "premiseVerification": "Confirmed Phase 5.3's SILENCE_RMS_THRESHOLD (1%) correctly let the reported 6% signal through to real inference - the VAD gate is not the defect; the gap is entirely downstream of it. Reviewed all twelve reported transcript lines: seven are pure placeholder captions, three are garbled nonsense, two are coherent but contain no Scripture reference - no evidence supports a separate Bible-detection defect distinct from claims 1 and 2 above.",
        "designChoice": "Whole-string-only placeholder matching (never a partial/substring match, so a segment mixing real words with a bracketed tag is kept in full) via a normalized (lowercase, alphanumeric-only) comparison against a documented NON_SPEECH_PLACEHOLDERS set, mirroring the empty-text discard path already established. Real confidence computed by averaging whisper.cpp's own per-token decode probability across every token in the pass, replacing the hardcoded 0.75 (kept only as a documented, honest fallback if no tokens were readable at all). A new, honest 'LOW SIGNAL' UI band (1%-15% input level) calibrated directly from this real operator's own 6%-produced-garbage evidence. Audio gain normalization/AGC was seriously considered and explicitly deferred - see docs/phase-14-audit.md's 'Explicitly deferred' section for the concrete technical reason (WhisperSpeechEngine buffers many small AudioChunk deliveries into one ~3s inference window, so a per-chunk gain stage would apply inconsistently across that window, a real artifact risk this environment has no real audio to verify against).",
        "newCode": {
            "coreAi": "core/ai/src/speech_engine.rs: new SpeechEngine::last_feed_was_non_speech_placeholder trait method, default false, mirroring last_feed_was_silence's exact precedent.",
            "aiSpeechWhisperRs": "ai/speech/src/whisper.rs: NON_SPEECH_PLACEHOLDERS (20-entry documented set), normalize_for_placeholder_match, is_non_speech_placeholder (pure, tested); run_inference discards pure-placeholder decodes exactly like an empty-text result already was, and computes real per-token-averaged confidence via full_n_tokens/full_get_token_prob; last_feed_was_non_speech_placeholder field + SpeechEngine trait impl.",
            "stateRs": "apps/desktop/src-tauri/src/state.rs: SpeechDiagnostics.non_speech_placeholders_skipped counter.",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: SpeechRuntimeDiagnostics.nonSpeechPlaceholdersSkipped field + mapping; handle_audio_chunk increments it in parallel with the existing silent_windows_skipped check.",
            "frontend": "lib/format.ts (new describeAudioSignal, extracted/tested pure function, new LOW SIGNAL band); LiveChurchBrain.tsx uses it; PilotDiagnosticsPanel.tsx surfaces the new placeholder-discard counter alongside the existing silence counter; appConfig.ts gains nonSpeechPlaceholdersSkipped."
        },
        "newTests": {
            "aiSpeechWhisperRsTestCount": 4,
            "frontendFormatTestCount": 7,
            "testingBoundaryNote": "Every new function is either pure (is_non_speech_placeholder, normalize_for_placeholder_match, describeAudioSignal) or thin diagnostics-counter wiring mirroring an already-established, already-untested-directly pattern (silent_windows_skipped itself has no dedicated integration test either, per this project's own standing 'thin orchestration stays untested directly, the pure logic beneath it is what's tested' convention). Test input uses the exact strings and the exact 6% level from the real pilot screenshots.",
            "command": "cargo test --workspace && cargo test -p cip-ai-speech --features whisper && cargo test --features whisper (apps/desktop/src-tauri) && npm run test -- --run",
            "result": "1023 passed (workspace default, unchanged from Phase 13 - new tests are whisper-feature-gated), 19 passed (cip-ai-speech --features whisper, up from 15), 350 passed (cip-desktop --features whisper, unchanged), 294 passed (frontend, up from 287) - 0 failures across all four"
        },
        "regressionResult": "cargo fmt/clippy/check/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms last_feed_was_non_speech_placeholder and nonSpeechPlaceholdersSkipped directly, plus the full normalized NON_SPEECH_PLACEHOLDERS literal string set compiled into .rodata (blank audio, silence, no audio, no speech, no speech detected, inaudible, speaking in foreign language, foreign language, unintelligible, laughter, laughing, music, music playing, applause, clapping, background noise, background music, static, buzzing, silence in the video) - none of which existed in the Phase 13 binary. is_non_speech_placeholder/normalize_for_placeholder_match were inlined by the release-mode optimizer (small pure functions), consistent with this project's own established precedent (Phase 3.8.7.3's classify_overload/chunk_duration_ms). Every prior-phase Rust-side symbol/string count (rejection_echo_count, find_rejected_suggestion_for_reference, record_rejection_echo, silentWindowsSkipped, audioErrorText, speechErrorText, enroll_acoustic_reference, list_acoustic_enrollments, remove_acoustic_reference, whisper_full_with_state, test_obs_connection, ensure_admin, operator_accounts, enable_congregant_companion, companion_snapshot, get_speech_language_capabilities, set_speech_language, get_church_knowledge_base, saved_sermon_findings) is unchanged from the Phase 13 baseline, whisper_full_with_state stable at 96 for an 8th consecutive rebuild.",
        "frontendTextSearchLimitation": "The frontend bundle is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe in the same way Rust symbols are - the new LOW SIGNAL band's own UI copy was not independently string-searched, consistent with every prior phase's methodology. Frontend correctness for this phase rests on npm run typecheck's success, the fresh Vite build hash, and the full 294/294 frontend test pass.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events, zero new migrations - entirely inside the existing speech pipeline and its diagnostics.",
            "is_non_speech_placeholder's whole-string-only matching can never discard a segment containing real words alongside a bracketed tag.",
            "TranscriptSegment.confidence's type/range is unchanged - only the value computation changed, so no downstream consumer needed updating.",
            "core/bible, core/sermon, core/music, core/service, core/presentation (every domain contract crate) are entirely untouched."
        ],
        "honestLimitation": "No audio gain normalization (deliberately deferred). The non-speech placeholder list is documented but not exhaustive - a new placeholder pattern observed in a future real session should be added the same way these were, from direct evidence. Garbled-but-real-looking hallucinated phrases that are not one of whisper.cpp's known fixed captions are not removed, only now honestly scored lower - no confidence-threshold hiding was added without further real operator feedback on a useful cutoff.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a second real pilot session on the same or similar hardware confirming the Live Transcript panel no longer shows whisper.cpp's own placeholder captions as real speech, and that the confidence badge now genuinely varies with transcription quality - see physicalHardwareStatement item 23."
    },
    "transcriptStallAndFullerContextDetectionProof": {
        "summary": "Nine real Windows pilot screenshots of the Phase 14 artifact showing the Live Transcript panel frozen at the exact same six lines across several minutes of visible video progress, plus a request that CIP detect Scripture even when paraphrased without a book name. See docs/phase-15-audit.md.",
        "auditFinding": "A placeholder-discarded Whisper window (Phase 14's own fix) returns Ok(vec![]), so handle_audio_chunk's `for segment in segments` loop - the only call site for both Bible detection and the 12-20s segmenter accumulator - never executes; TranscriptSegmenter::push never starts its 15s span clock until it receives non-empty text, so a sustained placeholder-only stretch leaves the Live Transcript panel frozen indefinitely with zero operator-visible signal. Separately, Bible detection (finalize_bible_only) only ever ran on each raw ~3s Whisper window, never on the fuller 12-20s accumulated window built for Sermon/Service/Music.",
        "premiseVerification": "Confirmed an already-computed 'seconds since last transcript activity' signal (service.rs::transcript_freshness, 30s threshold) was already polled into the frontend on every status refresh, but only ever rendered in the Service Intelligence section, not next to the Live Transcript panel itself where the screenshots show the operator was actually looking. Reviewed all six visible transcript lines and confirmed none are themselves paraphrases of any specific verse - they are meta-commentary about Bible study - so no correct implementation would have matched them; the real, fixable gap is reduced odds of catching a genuine paraphrase from a single ~3s fragment's limited vocabulary versus the fuller accumulated window.",
        "designChoice": "The Live Transcript panel now repeats the already-existing, already-tested transcriptFreshness value directly under the transcript list when stale (>=30s), with an honest explanation (a quiet/unclear microphone signal, not a stopped app) - no new backend signal, no fabricated transcript content. A fuller-context retry (paraphrase/semantic fallbacks only, same thresholds, never citation detection) now runs once an accumulated 12-20s window closes, but only when none of its own raw sub-windows already found a suggestion - reusing the existing try_paraphrase/try_semantic functions unchanged.",
        "newCode": {
            "coreService": "core/service/src/bible_intelligence.rs: new retry_paraphrase_or_semantic_with_fuller_context (pure, calls only try_paraphrase/try_semantic).",
            "pipelineRs": "apps/desktop/src-tauri/src/pipeline.rs: extracted persist_detections_and_suggestions (shared by the raw-window path and the new fuller-context retry, which never re-persists the transcript segment row).",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: finalize_bible_only now returns bool; new finalize_bible_fuller_context_retry gated on it; a per-listening-session window_had_suggestion flag owned by spawn_speech_worker.",
            "frontend": "LiveChurchBrain.tsx: the Live Transcript panel repeats the already-existing transcriptFreshness signal directly under the transcript list when stale."
        },
        "newTests": {
            "coreServiceTestCount": 4,
            "testingBoundaryNote": "retry_paraphrase_or_semantic_with_fuller_context is pure and fully tested. The commands.rs wiring is thin orchestration mirroring silent_windows_skipped's own already-established untested-directly precedent. The frontend change only displays an already-tested, already-polled value - no new frontend test was added, mirroring Phase 13's own precedent.",
            "command": "cargo test --workspace && cargo test -p cip-desktop --features whisper && cargo test -p cip-ai-speech --features whisper && npm run test -- --run",
            "result": "cip-core-service 35 passed (up from 31, the 4 new tests), cip-desktop 350 passed (unchanged, default and whisper feature configs), cip-ai-speech --features whisper 19 passed (unchanged), frontend 294 passed (unchanged) - 0 failures across all four"
        },
        "regressionResult": "cargo fmt/clippy/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms finalize_bible_fuller_context_retry, retry_paraphrase_or_semantic_with_fuller_context, and persist_detections_and_suggestions directly present. window_had_suggestion (a local bool, not a named function) was inlined by the release-mode optimizer as expected. Every prior-phase Rust-side symbol/string count (through Phase 14's own non-speech-placeholder handling) is unchanged from the Phase 14 baseline, whisper_full_with_state stable at 96 for a 9th consecutive rebuild.",
        "frontendTextSearchLimitation": "The frontend bundle is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe - the new stall-indicator copy was confirmed absent via strings, as expected, and not independently string-searched further, consistent with every prior phase's methodology. Frontend correctness for this phase rests on npm run typecheck's success, the fresh Vite build hash, and the full 294/294 frontend test pass (unchanged, since this UI change only displays an already-tested value).",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events, zero new migrations.",
            "retry_paraphrase_or_semantic_with_fuller_context can never attempt citation detection or mutate the active Scripture context - it calls only try_paraphrase/try_semantic under the same 'only when nothing else already found a suggestion' gating already used elsewhere.",
            "persist_detections_and_suggestions's extraction is a pure refactor - the raw-window path's own behavior is byte-for-byte unchanged.",
            "core/bible, core/sermon, core/music, core/presentation (every domain contract crate this phase does not touch) are entirely untouched."
        ],
        "honestLimitation": "The fuller-context retry raises the odds of catching a genuine paraphrase but is still bounded by the same lexical-overlap/semantic-similarity thresholds as before - a paraphrase sharing almost no vocabulary with its verse still needs the semantic fallback, which still needs an operator-provisioned embedding model never verified on real hardware. The stall indicator explains that nothing new has been transcribed and offers the most likely honest reason - it cannot diagnose every possible cause with more precision than the underlying transcript_freshness signal already could.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a second real pilot session confirming the Live Transcript panel now surfaces a clear 'no new transcript' message instead of silently freezing during a long quiet/unclear stretch, and that a genuine paraphrase spoken without a book name is now more likely to be caught - see physicalHardwareStatement item 24."
    },
    "liveTranscriptShowHideProof": {
        "summary": "The operator's own request, made while reviewing real Windows pilot screenshots of Phase 15's own stall indicator and Bible-detection-to-display pipeline both working live on real hardware: 'Can it pause the live transcript for now focus major on detecting scripture, music and the intelligences.' See docs/phase-16-live-transcript-collapse.md.",
        "premiseVerification": "Traced spawn_speech_worker, finalize_bible_only, and route_segment_to_live_intelligence_engines and confirmed Bible, Sermon, Service, and Music detection are driven entirely by the backend speech worker thread with zero dependency on whether the frontend renders the Live Transcript list - so there is nothing to 'pause' in the pipeline sense without breaking the detection the operator's own wording explicitly wants kept running.",
        "designChoice": "A pure display toggle: a new Show/Hide button in the Live Transcript panel's own heading. Collapsing it replaces the transcript list (and Phase 15's own stall message, which would otherwise repeat under a hidden list) with a one-line note confirming detection keeps running. The preference persists across restarts via localStorage, wrapped in try/catch mirroring OnboardingWalkthrough.tsx's own real-storage-access pattern exactly - a failure to persist just means it defaults back to expanded, never blocks anything. Collapsing never discards already-received transcript segments from component state - they are simply not rendered.",
        "newCode": {
            "transcriptPanelTs": "apps/desktop/src/lib/transcriptPanel.ts (new): isLiveTranscriptCollapsed, a pure predicate mirroring onboarding.ts's own established precedent for a localStorage-backed UI preference.",
            "liveChurchBrainTsx": "apps/desktop/src/components/LiveChurchBrain.tsx: a Show/Hide button in the Live Transcript panel's own heading; readTranscriptCollapsedPreference/persistTranscriptCollapsedPreference module-scope helpers mirroring OnboardingWalkthrough.tsx's own pattern; the panel body now branches on the collapsed state before falling through to the pre-existing empty/populated transcript logic, with Phase 15's own stall message untouched inside that branch.",
            "rust": "None. Zero Rust files changed - confirmed by an empty Rust diff."
        },
        "newTests": {
            "frontendTestCount": 4,
            "testingBoundaryNote": "isLiveTranscriptCollapsed is pure and fully unit-tested (transcriptPanel.test.ts, mirroring onboarding.test.ts's own exact shape). The component wiring (the button, the localStorage read/write) is thin, mirroring OnboardingWalkthrough.tsx's own untested-directly precedent for real localStorage access - this project has no DOM testing environment.",
            "command": "npm run typecheck && npm run lint && npm run test -- --run && npm run build",
            "result": "typecheck 0 errors, lint same 5 pre-existing warnings (unchanged), 298 passed (up from 294, the 4 new tests), build clean. No Rust tests re-run beyond confirming git status shows no Rust files changed."
        },
        "regressionResult": "Frontend typecheck/lint/test/build: all clean, same 5 pre-existing lint warnings as before this phase. No Rust code touched - cargo fmt/clippy/test are unaffected by this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms every prior-phase Rust-side symbol/string count, including Phase 15's own fuller-context detection functions (finalize_bible_fuller_context_retry, retry_paraphrase_or_semantic_with_fuller_context, persist_detections_and_suggestions), is unchanged from the Phase 15 baseline, whisper_full_with_state stable at 96 for a 10th consecutive rebuild. No new Rust symbols were expected or found - this phase's own Rust binary is unchanged in substance from Phase 15 aside from the fresh bundled frontend asset it packages.",
        "frontendTextSearchLimitation": "The frontend bundle is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe in the same way Rust symbols are - the new Show/Hide button copy was not independently string-searched, consistent with every prior phase's methodology. Frontend correctness for this phase rests on npm run typecheck's success, the fresh Vite build hash, and the full 298/298 frontend test pass.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events, zero backend changes of any kind - confirmed by an empty Rust diff and this rebuild's own zero-new-symbol result.",
            "Bible/Sermon/Service/Music detection are architecturally incapable of being affected by this toggle - they are driven entirely by the backend speech worker, which has no dependency on frontend rendering state.",
            "Every prior-phase command and Phase 14/15 symbol remains present and unregressed, confirming this phase's addition did not disturb any prior gate or feature."
        ],
        "honestLimitation": "This is a pure client-side preference (localStorage) - it is per-browser-profile/per-install, not synced anywhere, consistent with every other UI preference in this project. It does not and cannot indicate anything about whether detection is actually still running beyond the operator's own confidence in the architecture - no in-panel heartbeat or live counter was added to the collapsed-state note itself this phase.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a real operator toggling Hide/Show during a real service and confirming Bible/Sermon/Service/Music detection keep working unchanged while the transcript list is hidden - see physicalHardwareStatement item 25."
    },
    "bibleDetectionAnalyticsProof": {
        "summary": "An autonomous continuation ('Keep going, start Phase 17') that re-audited docs/phase-4-master-plan-gap-audit.md for a still-unstarted item directly connected to the operator's own most recent real feedback ('accuracy fair', the report that triggered Phase 15) - the master plan's own 'Analytics' gap, never instrumented. See docs/phase-17-detection-accuracy-analytics.md.",
        "auditFinding": "ai_suggestions (status/confidence_score/rejection_echo_count, since Phase 1.3/5.4) and scripture_detections (detection_type, since Phase 1.5) already persist everything a real accuracy dashboard needs - it was simply never surfaced. pipeline.rs::persist_detections_and_suggestions persists a detection and the suggestion it produces with the exact same transcript_segment_id, for both the raw-window path and the Phase 15 fuller-context retry path - a reliable join key with no schema change required.",
        "premiseVerification": "Confirmed only the Bible domain has complete accept/edit/reject history persisted - Music/Sermon/Content route through IntelligenceFinding, which (outside Phase 13/2.7.1's accepted-only saved_sermon_findings/saved_content_candidates) never durably records a rejected item, so no honest rejection-rate can be computed for those domains. This phase is scoped to Bible detection accuracy only, not a fabricated cross-domain metric.",
        "designChoice": "A pure read-only aggregation (bible_detection_analytics.rs) over already-persisted ai_suggestions/scripture_detections/services rows, correlated in Rust (not SQL) via the (service_id, transcript_segment_id, reference) join key - never re-running detection, never writing anything, never changing a threshold. Exposed via one new read-only Tauri command and a new HistoryView section, mirroring the existing Church Knowledge Base section's cross-service, load-once pattern exactly.",
        "newCode": {
            "biblDetectionAnalyticsRs": "apps/desktop/src-tauri/src/bible_detection_analytics.rs (new): OutcomeCounts (with approval_rate()/correction_rate()), ConfidenceLevelBreakdown, DetectionKindBreakdown, ServiceAccuracyTrendEntry, BibleDetectionAnalytics, build_bible_detection_analytics.",
            "persistenceRs": "apps/desktop/src-tauri/src/persistence.rs: two new bounded cross-service read functions, list_all_suggestions and list_all_scripture_detections_with_segment, mirroring sermon_knowledge_base.rs's own cross-service precedent.",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: new get_bible_detection_analytics command, open to any operator.",
            "frontend": "domain/service.ts gains the mirrored TS types; lib/commands.ts gains getBibleDetectionAnalytics; HistoryView.tsx gains a new 'Detection Accuracy' section (overall/by-confidence/by-detection-method breakdowns plus a collapsible per-service trend)."
        },
        "newTests": {
            "coreDesktopTestCount": 9,
            "testingBoundaryNote": "OutcomeCounts/build_bible_detection_analytics are pure and fully tested, including a real FK-backed transcript segment (not a synthetic id) for the detection-kind correlation test. The two new persistence.rs functions are exercised indirectly through those same tests, mirroring how list_suggestions/scripture_detection_kind_counts themselves have never had dedicated tests either. The frontend rendering is thin display logic over an already-tested value - no new frontend test, mirroring ServiceReport's/SermonKnowledgeBase's own precedent.",
            "command": "cargo test --workspace && cargo test -p cip-desktop --features whisper && npm run test -- --run",
            "result": "cip-desktop 359 passed (up from 350, the 9 new tests), unchanged in both feature configs; frontend 298 passed (unchanged) - 0 failures across all three"
        },
        "regressionResult": "cargo fmt/clippy/test: clean across the full workspace, both feature configs. Frontend typecheck/lint/test/build: clean, same 5 pre-existing lint warnings as before this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms build_bible_detection_analytics, list_all_scripture_detections_with_segment, get_bible_detection_analytics, and OutcomeCounts directly present. list_all_suggestions (a small, straight-line function) was inlined by the release-mode optimizer as expected, mirroring this project's own established precedent for other small pure functions - verified instead via its own dedicated tests. Every prior-phase Rust-side symbol/string count is unchanged from the Phase 16 baseline, whisper_full_with_state stable at 96 for an 11th consecutive rebuild. This rebuild's own evidence file also corrects a stale count: retry_paraphrase_or_semantic_with_fuller_context genuinely matches 2 distinct mangled symbols (the core function and its desktop-layer wrapper, exactly as Phase 15 designed it), not the 1 the Phase 15/16 evidence files recorded - a strings-output artifact, not a missing symbol.",
        "frontendTextSearchLimitation": "The frontend bundle is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe - the new 'Detection Accuracy' section copy was not independently string-searched, consistent with every prior phase's methodology. Frontend correctness for this phase rests on npm run typecheck's success, the fresh Vite build hash, and the full 298/298 frontend test pass.",
        "architecturalSafetyNotes": [
            "One new Tauri command (get_bible_detection_analytics, read-only), zero new events, zero new migrations, zero schema changes.",
            "build_bible_detection_analytics only ever reads ai_suggestions/scripture_detections/services - confirmed by direct source inspection, no INSERT/UPDATE/DELETE statement anywhere in the module.",
            "core/bible, core/sermon, core/music, core/presentation (every domain contract crate this phase does not touch) are entirely untouched."
        ],
        "honestLimitation": "The accuracy figures are only as informative as the operator behavior behind them - this container has no real pilot session's worth of decisions to show yet; the dashboard reads 'Nothing decided yet' until a real operator has approved/edited/rejected suggestions during real services. Music/Sermon/Content accuracy is not covered - see the premiseVerification note above.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a real operator reviewing this dashboard against a real service history and confirming the breakdown matches what they actually did - see physicalHardwareStatement item 26."
    },
    "loopbackAudioSignalGuidanceProof": {
        "summary": "A real Windows pilot report: an operator selected Stereo Mix (Realtek(R) Audio) - a loopback device, not a microphone - to capture a YouTube video's audio, started listening, and got NO SIGNAL throughout while System Diagnostics confirmed a live, continuously-running capture stream (22669 audio chunks received) but 0/0 inferences (75/75 windows classified as silence). See docs/phase-18-audit.md.",
        "auditFinding": "Traced the full capture path end to end before concluding anything: device selection, stream negotiation, channel downmix, RMS computation, and resampling were all confirmed correct by direct source inspection. Whisper's own independent silence gate (a second, separately-implemented RMS computation, in a different crate, over the resampled buffer) also independently agreed every window was silent - two independently-implemented RMS computations agreeing rules out a computation bug in either path and points at genuinely near-silent PCM data reaching the app.",
        "premiseVerification": "No CIP code defect was found. The most likely real-world cause is a well-documented Windows Stereo Mix quirk outside this application's control: the device's own OS-level recording level muted or at 0% (common on OEM installs), or system audio not actually routed through the same device Windows treats as the default playback output that Stereo Mix mirrors. No cross-platform audio library has an API to read or fix a Windows recording device's own level/mute state.",
        "designChoice": "The existing NO SIGNAL/LOW SIGNAL message (Phase 14) assumed a physical microphone ('move it closer, raise its gain') - actively wrong advice for a loopback device with no physical position or gain control. describeAudioSignal now takes an optional deviceName parameter (the backend's own resolved AudioEngineStatus.selectedDevice) and, when it matches a known Windows loopback/monitor device name pattern, gives guidance that actually applies instead. Every existing caller/test defaults deviceName to null and keeps its exact original wording.",
        "newCode": {
            "formatTs": "apps/desktop/src/lib/format.ts: new LOOPBACK_DEVICE_NAME_PATTERNS + isLoopbackDeviceName (pure, tested); describeAudioSignal gains an optional deviceName parameter with loopback-specific NO SIGNAL/LOW SIGNAL branches.",
            "liveChurchBrainTsx": "apps/desktop/src/components/LiveChurchBrain.tsx: the describeAudioSignal call site now passes status.audio.selectedDevice.",
            "rust": "None. Zero Rust files changed - confirmed by an empty Rust diff."
        },
        "newTests": {
            "frontendTestCount": 5,
            "testingBoundaryNote": "isLoopbackDeviceName/describeAudioSignal's loopback branches are pure and fully unit-tested (format.test.ts): loopback NO SIGNAL guidance never mentions 'microphone', loopback LOW SIGNAL guidance mentions 'volume' not a microphone gain adjustment, case-insensitive/alias detection for Stereo Mix and What U Hear, and two tests confirming a normal microphone device and a null device name both keep the exact original wording unchanged.",
            "command": "npm run typecheck && npm run lint && npm run test -- --run && npm run build",
            "result": "typecheck 0 errors, lint same 5 pre-existing warnings (unchanged), 303 passed (up from 298, the 5 new tests), build clean. No Rust tests re-run beyond confirming git status shows no Rust files changed."
        },
        "regressionResult": "Frontend typecheck/lint/test/build: all clean, same 5 pre-existing lint warnings as before this phase. No Rust code touched - cargo fmt/clippy/test are unaffected by this phase.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms every prior-phase Rust-side symbol/string count, including Phase 17's own Detection Accuracy Analytics functions, is unchanged from the Phase 17 baseline, whisper_full_with_state stable at 96 for a 12th consecutive rebuild. No new Rust symbols were expected or found - this phase's own Rust binary is unchanged in substance from Phase 17 aside from the fresh bundled frontend asset it packages.",
        "frontendTextSearchLimitation": "The frontend bundle is bundled into the Tauri resources directory, not searchable as plain strings inside cip-desktop.exe in the same way Rust symbols are - the new loopback-guidance copy was not independently string-searched, consistent with every prior phase's methodology. Frontend correctness for this phase rests on npm run typecheck's success, the fresh Vite build hash, and the full 303/303 frontend test pass.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events, zero backend changes of any kind - confirmed by an empty Rust diff and this rebuild's own zero-new-symbol result.",
            "describeAudioSignal's new deviceName parameter is optional and defaults to null - every existing call site not updated this phase keeps its exact prior behavior.",
            "Bible/Sermon/Service/Music detection are architecturally incapable of being affected by this change - it only reclassifies a status message string in the Live Service UI."
        ],
        "honestLimitation": "This phase does not and cannot fix the underlying Windows audio routing - it only tells the operator where to look. Whether Stereo Mix ever actually captures audio on a given machine remains outside CIP's control and unverified by this phase. The loopback device-name pattern list is best-effort, not exhaustive.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a real operator selecting Stereo Mix (or another loopback device) on real Windows hardware and confirming the new guidance message renders, then checking that device's own Windows recording level per that guidance to see whether it resolves the original report - see physicalHardwareStatement item 27."
    },
    "ordinalNormalizationProof": {
        "summary": "The operator ran their own independent, unsolicited detection-accuracy audit of the repository and asked whether to start implementing any of its findings. Every cited claim was re-verified against the current code first (docs/phase-19-audit.md); this proof section covers the one finding implemented this phase.",
        "auditVerification": "Confirmed accurate: core/bible/src/normalize.rs converted cardinal number words only, never ordinals - 'First Corinthians' worked only because book_alias.rs hardcoded it as a literal per-book alias string, not because of any general rule. One claim needed correction (Sermon/Service/Music intelligence do run live automatically, contrary to the audit's claim) and one needed a footnote (bare-verse fragments are matched but resolve to Unresolved without context and are never persisted) - see docs/phase-19-audit.md for full reasoning on both.",
        "designChoice": "New ORDINALS table + ordinal_value() in core/bible/src/normalize.rs, deliberately scoped to first/second/third and their digit-suffix forms 1st/2nd/3rd only - every canonical Bible book numbering prefix (1/2/3 Samuel, Kings, Chronicles, Corinthians, Thessalonians, Timothy, Peter, John) tops out at three, and chapter/verse numbers are always spoken as cardinals, never ordinals, so a general 1st-99th ordinal parser would add scope with no real use case.",
        "newCode": {
            "normalizeRs": "core/bible/src/normalize.rs: new ORDINALS const + ordinal_value() function, wired into normalize_word_token alongside the existing cardinal-word conversion path.",
            "rust": "core/bible only. Zero other Rust crates changed - confirmed by git diff scoped to a single file.",
            "frontend": "None. Zero frontend files changed this phase."
        },
        "newTests": {
            "backendTestCount": 3,
            "testingBoundaryNote": "ordinal_value/normalize_word_token's ordinal branch are pure and fully unit-tested: converts_ordinal_word_forms_to_digits, converts_digit_suffix_ordinal_forms_to_digits, and never_touches_ordinals_past_third (a negative test confirming 'fourth'/'4th' are deliberately left unconverted).",
            "command": "cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test --workspace (default and --features whisper)",
            "result": "fmt clean, clippy clean (both feature configs), cip-core-bible 85/85 passed (up from 82, the 3 new tests), cip-desktop unaffected at 359/359 in both feature configs, every other crate unchanged. No frontend files touched - npm run typecheck/lint/test/build not re-run beyond confirming git status shows no frontend files changed."
        },
        "regressionResult": "cargo fmt --check clean, cargo clippy --all-targets -- -D warnings clean (default and whisper feature configs), cargo test --workspace clean across the whole workspace. Zero regressions in any other crate.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms every prior-phase tripwire symbol/string count, including Phase 17's own Detection Accuracy Analytics functions and Phase 18's own zero-Rust-change baseline, is unchanged - whisper_full_with_state stable at 96 for a 13th consecutive rebuild. normalize_word_token (the function that calls the new ordinal_value) is directly confirmed present in the binary. ordinal_value itself was inlined by the release-mode optimizer, as expected for a small pure lookup function - consistent with this project's own established precedent for other small pure functions (e.g. classify_overload/chunk_duration_ms in Phase 3.8.7.3, list_all_suggestions in Phase 17), where caller presence plus a fully passing unit-test suite has repeatedly been treated as sufficient evidence rather than a gap.",
        "frontendTextSearchLimitation": "N/A - no frontend files changed this phase.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events, zero new migrations, zero schema changes.",
            "normalize_text is called only on a local copy for detection (core/service/src/bible_intelligence.rs) - this change cannot affect the transcript text persisted or shown to the operator.",
            "Every other domain contract crate (core/sermon, core/music, core/service's non-normalize logic, core/presentation) is entirely untouched."
        ],
        "honestLimitation": "This closes one narrow, real gap (digit-suffix ordinals) - it does not move the needle on the audit's own identified biggest contributors (the 3s Whisper window cut, zero fuzzy tolerance, uncalibrated fallback thresholds). Those remain open, explicitly deferred in docs/phase-19-audit.md with reasoning, not silently dropped.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a real operator saying '1st Corinthians thirteen four' or '2nd Timothy chapter three' during a live service and confirming it is now correctly detected where it previously wasn't - see physicalHardwareStatement item 28."
    },
    "fuzzyBookMatchingProof": {
        "summary": "Two direct operator requests in one message: 'start Phase 20 with fuzzy book-name matching' (the leading item explicitly deferred from Phase 19's own detection-accuracy audit response) plus a real UX complaint, with a screenshot, that the Correct Active Context manual-entry field 'requires pressing the full name of the book' before it will accept a correction.",
        "designChoice": "core/bible/src/book_alias.rs::fuzzy_match_book does length-scaled Levenshtein-distance near-miss matching, scoped deliberately to the single-word canonical book names only (48 of 66) - multi-word/numbered books ('1 Corinthians', '1/2/3 Samuel/Kings/Chronicles/Thessalonians/Timothy/Peter/John', 'Song of Solomon') are excluded, since guessing which numbered variant a bare near-miss word meant is a fundamentally higher-risk problem than this function solves; those remain reachable through the existing exact alias table. core/bible/src/detection.rs's detect_candidates only trusts a fuzzy match immediately followed by a real chapter:verse shape - a fuzzy-matched word with nothing after it (e.g. 'Roman was a great empire') produces nothing. core/service/src/bible_intelligence.rs::resolve_fuzzy_book re-validates the guess against the real Bible data before ever suggesting anything and - unlike a real citation - never mutates the active Scripture context, exactly like the existing paraphrase/semantic fallbacks; a near-miss book name must never become the trusted context a later bare verse would silently inherit. Separately, apps/desktop/src-tauri/src/commands.rs::correct_scripture_context now resolves the operator's typed book text through canonicalize_book (the same alias table the live detector uses) before validating the chapter - previously it required the exact undocumented internal database book code, since the frontend's own .toUpperCase() turned a typed 'John' into the never-matching 'JOHN' against the stored code 'JHN'.",
        "newCode": {
            "bookAliasRs": "core/bible/src/book_alias.rs: new levenshtein/max_allowed_distance helpers + fuzzy_match_book (pure, tested).",
            "detectionRs": "core/bible/src/detection.rs: new ReferenceKind::FuzzyBook variant, DetectedCandidate gains an Option<f32> fuzzy_score field (Eq dropped from its derive - f32 has no Eq), a new WORD_PATTERN-driven fuzzy pass in detect_candidates.",
            "bibleIntelligenceRs": "core/service/src/bible_intelligence.rs: new resolve_fuzzy_book function, wired into process_transcript_segment_inner's match, a new confidence_for_kind arm.",
            "bibleAdapterRs": "core/intelligence/src/bible_adapter.rs: a new exhaustive-match arm for ReferenceKind::FuzzyBook, mirroring Paraphrase's.",
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: correct_scripture_context now calls canonicalize_book before validating the chapter.",
            "frontend": "apps/desktop/src/components/LiveChurchBrain.tsx: the manual-entry field drops its own .toUpperCase() and updates its hint/placeholder text. apps/desktop/src/domain/ai.ts: ReferenceKind gains \"fuzzy_book\"."
        },
        "newTests": {
            "backendTestCount": 22,
            "testingBoundaryNote": "fuzzy_match_book: 8 new tests in book_alias.rs, including a systematic sweep (single-character-dropped typos of every single-word canonical book name never resolve to the *wrong* book). detect_candidates's fuzzy pass: 5 new tests in detection.rs. resolve_fuzzy_book/the orchestrator: 4 new tests in bible_intelligence.rs confirming validation, the never-mutates-context invariant, and that an exact reference is never reclassified as fuzzy. correct_scripture_context's fix reuses an already-exhaustively-tested function (canonicalize_book, unchanged this phase) - no new backend test needed for that half.",
            "command": "cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test --workspace (default and --features whisper) && npm run typecheck && npm run lint && npm run test -- --run && npm run build",
            "result": "fmt clean, clippy clean (both feature configs), cargo test clean in both feature configs (cip-core-bible 98/98, up from 85 - the 13 new fuzzy-matching tests; cip-core-service/cip-core-intelligence/cip-desktop all pass with their new FuzzyBook coverage). Frontend: typecheck 0 errors, lint the same 5 pre-existing warnings (unchanged), test 303/303 (unchanged - no new frontend test needed), build clean."
        },
        "regressionResult": "Full workspace cargo fmt/clippy/test clean in both feature configs; full frontend typecheck/lint/test/build clean. Zero regressions in any other crate.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms every prior-phase tripwire symbol/string count, including Phase 17's own Detection Accuracy Analytics functions and Phase 19's own normalize_word_token, is unchanged - whisper_full_with_state stable at 96 for a 14th consecutive rebuild. fuzzy_match_book and canonicalize_book (the public API surface the new/changed code calls into) are directly confirmed present; correct_scripture_context is present at count 2, consistent with this project's established pattern for Tauri command functions. resolve_fuzzy_book/levenshtein/max_allowed_distance were inlined by the release-mode optimizer, as expected for small functions with a single call site or pure small helpers - consistent with this project's own established precedent (ordinal_value in Phase 19, classify_overload/chunk_duration_ms in Phase 3.8.7.3).",
        "frontendTextSearchLimitation": "The frontend bundle is not searchable as plain strings inside cip-desktop.exe the same way Rust symbols are - the new 'fuzzy_book' union member and the manual-entry hint/placeholder text were not independently string-searched, consistent with every prior phase's methodology.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events, zero new migrations, zero schema changes.",
            "ReferenceKind::FuzzyBook never mutates ScriptureContext - confirmed by a dedicated regression test.",
            "correct_scripture_context's new behavior is strictly more permissive than before - no existing valid input becomes invalid.",
            "Every other domain contract crate (core/sermon, core/music, core/presentation) is entirely untouched."
        ],
        "honestLimitation": "Fuzzy matching only covers single-word book names - the numbered-book gap is real and explicitly deferred, not silently dropped. The similarity threshold/distance budget is documented reasoning, not empirically calibrated against real mishearing data - no labeled real-service audio-to-transcript corpus exists in this repository to calibrate against yet.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gates are a real operator speaking a plausibly mis-transcribable book name (e.g. Whisper rendering 'Romans' as 'Roman') during a live service and confirming it now surfaces as a suggestion, and a real operator using the corrected Correct Active Context field with a full book name or abbreviation instead of the internal database code - see physicalHardwareStatement item 29."
    },
    "vadTriggeredFlushProof": {
        "summary": "Direct operator instruction: 'Keep going, start Phase 21 with overlapping Whisper windows + VAD' - the audit's own top-ranked item, explicitly deferred out of Phase 19 and Phase 20 pending a safely-scoped design.",
        "problem": "WhisperSpeechEngine ran one inference pass every time its buffer reached a fixed ~3s sample-count cap (CHUNK_SAMPLES), with no relationship to what was actually being said - a window boundary could land mid-word purely because a sample counter crossed a threshold.",
        "designChoice": "The operator's own phrasing named two techniques together; they were evaluated separately. VAD-triggered flush was implemented: a window now flushes as soon as at least MIN_VAD_FLUSH_SAMPLES (1.5s) has buffered and the trailing TRAILING_SILENCE_SAMPLES (0.4s) is genuinely quiet, so most windows now end where the speaker actually paused - directly addressing the mid-word-cut problem with no new risk. Audio-overlapping windows (decoding a shared audio tail in two windows for extra context) was deliberately NOT implemented: it requires reconciling text produced from the same audio decoded twice, which real streaming ASR systems solve via token-timestamp-based stitching that needs real, timing-sensitive audio this container cannot produce or validate against - getting it wrong risks duplicated words in the persisted is_final:true transcript, a strictly worse outcome than the mid-word cuts it would fix.",
        "newCode": {
            "whisperRs": "ai/speech/src/whisper.rs: new MIN_VAD_FLUSH_SAMPLES/TRAILING_SILENCE_SAMPLES consts, new pure has_trailing_pause/should_flush_early functions, a new last_feed_was_vad_early_flush field, feed_audio/flush updated to set it - run_inference itself is untouched.",
            "speechEngineRs": "core/ai/src/speech_engine.rs: new SpeechEngine::last_feed_was_vad_early_flush() trait method, default false so ScriptedSpeechEngine/NullSpeechEngine need no changes.",
            "diagnosticsPlumbing": "apps/desktop/src-tauri/src/state.rs (new SpeechDiagnostics::vad_early_flushes counter) and commands.rs (increment logic + SpeechRuntimeDiagnostics field + mapping), apps/desktop/src/config/appConfig.ts (vadEarlyFlushes) and PilotDiagnosticsPanel.tsx (new conditional render line) - mirrors the Phase 5.3/14 diagnostic-counter pattern exactly."
        },
        "newTests": {
            "backendTestCount": 8,
            "testingBoundaryNote": "has_trailing_pause/should_flush_early are pure and fully unit-tested: a buffer shorter than the trailing window never triggers a false pause, a buffer ending in real audio never triggers, a buffer ending in a genuine quiet stretch does trigger, silence earlier in the buffer that isn't at the very end is ignored, the minimum-duration guard prevents an almost-immediate pause from fragmenting the window, and an empty buffer never triggers. Fully verifiable without a real model file, exactly like every other pure classification function in this file (is_silence, is_non_speech_placeholder).",
            "command": "cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test --workspace (default and --features whisper) && npm run typecheck && npm run lint && npm run test -- --run && npm run build",
            "result": "fmt clean, clippy clean (both feature configs), cargo test clean in both feature configs (cip-ai-speech 27/27 with --features whisper, up from 19 - the 8 new tests; cip-desktop unaffected at 359/359 in both feature configs). Frontend: typecheck 0 errors, lint the same 5 pre-existing warnings (unchanged), test 303/303 (unchanged - purely additive diagnostic field), build clean."
        },
        "regressionResult": "Full workspace cargo fmt/clippy/test clean in both feature configs; full frontend typecheck/lint/test/build clean. Zero regressions in any other crate.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms every prior-phase tripwire symbol/string count, including Phase 17's own Detection Accuracy Analytics functions and Phase 20's own fuzzy_match_book/canonicalize_book, is unchanged - whisper_full_with_state stable at 96 for a 15th consecutive rebuild. last_feed_was_vad_early_flush (the new trait method's own name) is directly confirmed present. should_flush_early/has_trailing_pause were inlined by the release-mode optimizer, as expected for small pure functions each called from exactly one site - consistent with this project's own established precedent (fuzzy_match_book's internal helpers in Phase 20, ordinal_value in Phase 19).",
        "frontendTextSearchLimitation": "The frontend bundle is not searchable as plain strings inside cip-desktop.exe the same way Rust symbols are - the new vadEarlyFlushes field and its rendering were not independently string-searched, consistent with every prior phase's methodology.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events, zero new migrations, zero schema changes.",
            "The fixed CHUNK_SAMPLES (3s) hard cap is unchanged and checked first - VAD-triggered flush is strictly additive (an earlier opportunity to flush), never a removal of the existing latency bound.",
            "run_inference itself is untouched - only the decision of when to call it changed, keeping the already-tested inference/timestamp/silence-gate logic at zero risk.",
            "apps/desktop/src-tauri/src/segmentation.rs's TranscriptSegmenter accumulates raw windows purely by watching each one's own start_ms/end_ms against a fixed time target - it has no assumption about how many raw windows make up a logical segment or how long any one is, so variable-length windows compose with it exactly as fixed-length windows always did.",
            "Every other domain contract crate (core/bible, core/sermon, core/music, core/presentation) is entirely untouched."
        ],
        "honestLimitation": "Audio-overlapping windows are not implemented - a word spoken with genuinely no pause anywhere near the hard 3s cap can still be cut, exactly as before this phase. The 1.5s/0.4s thresholds are documented reasoning, not empirically calibrated against real speech-timing data - no labeled real-service audio exists in this repository to calibrate against yet, the same honest limitation every other threshold in this codebase carries.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gates are a real operator running a live service and confirming the vadEarlyFlushes diagnostic counter rises during normal speech with natural pauses, and that transcript quality at sentence boundaries subjectively improves compared to the pre-Phase-21 fixed-3s-window behavior - see physicalHardwareStatement item 30."
    },
    "modelTierDiagnosticsProof": {
        "summary": "Operator feedback, with screenshots: 'The interface is shallow, UI/UX needs improvement... Bible detection is not working well, 1. It's not accurate 2. It appears late 3. The rate at which it appears is 10%... I don't think CIP understand the English of a Nigerian Pastor.' This proof covers the detection-accuracy half of that feedback; the UI half is tracked separately as Phase 23.",
        "rootCause": "core/config.rs's WHISPER_MODEL_FILENAME and docs/live-speech.md/docs/first-use.md's own prior wording pointed every operator at ggml-tiny.en.bin, whisper.cpp's smallest, least accurate model (39M parameters, ~75MB). Bible/sermon detection can only match text Whisper actually produced correctly, so a model this small mistranscribing what was said - especially on an accent it was trained on the least of - is a real, sufficient explanation for all three named symptoms, independent of anything in the detection/fuzzy-matching logic itself (Phase 20's own fuzzy book-name matching is untouched by this phase).",
        "secondIssueFound": "commands::install_whisper_model (Phase 3.8.7.1's 'Select Existing Model File' picker) always copies the operator's chosen file to the same fixed destination filename (state.config.whisper_model_path) by design, so create_speech_engine can always find the active model at one known path. Side effect: WhisperModelDiagnostic's path field was permanently uninformative about which real model tier was actually installed - a large, high-quality model and the smallest possible one were indistinguishable by path alone in System Diagnostics.",
        "verifiedBeforeScoping": [
            "cip_ai_speech::SUPPORTED_LANGUAGES already offers English, Yoruba, and Hausa as real, whisper.cpp-verified language selections (Phase 12), each a real trained language in whisper.cpp's vocabulary, not a guess.",
            "Igbo's absence is a verified, real model limitation, not an oversight: whisper.cpp's vocabulary has no Igbo entry at all (re-confirmed this phase via ai/speech/src/lib.rs's own doc comment and its igbo_is_never_offered_as_a_supported_language test).",
            "WhisperSpeechEngine::is_multilingual() (Phase 12) already detects, from the loaded model itself, whether it has language tokens at all, and the operator UI already warns when a non-English language is selected against an English-only model.",
            "docs/live-speech.md's 'Language support' section had never been updated after Phase 12 shipped - it still said Yoruba/Igbo/Hausa/Pidgin were entirely unimplemented, which was stale and actively misleading. Fixed as part of this phase's doc pass.",
            "CIP holds exactly one active WhisperSpeechEngine/WhisperContext for the process's life (AppState.speech_engine, built once in create_speech_engine). There is no existing mechanism to run a fast low-latency model and a separate high-quality model concurrently."
        ],
        "scopeDecision": "The operator's own stated model strategy (a fast detector tier plus a separate high-quality transcript tier, running concurrently) would require a second WhisperContext, an output-arbitration policy, and real microphone audio to validate the reconciliation against - none of which exists in this container. This is explicitly deferred, not silently omitted. What this phase does instead: make it honestly possible to tell what model is installed despite the fixed-filename constraint; stop steering every new deployment toward the worst model whisper.cpp ships, in every doc that told operators to do so; give the single active model architecture an honest, evidence-based upgrade path (base.en/small.en minimum, large-v3-turbo for quality); and fix the stale Phase 12 language-support documentation.",
        "newCode": {
            "commandsRs": "apps/desktop/src-tauri/src/commands.rs: new pure classify_model_size_tier(size_bytes: u64) -> &'static str (heuristic tiny/base/small/medium-or-turbo/large classification using whisper.cpp's own documented unquantized ggml file sizes as thresholds); WhisperModelDiagnostic::Present gained a size_tier_hint: String field, computed by diagnose_whisper_model and returned by both get_pilot_diagnostics and install_whisper_model.",
            "libRs": "apps/desktop/src-tauri/src/lib.rs: create_speech_engine now logs a log::warn! (never a hard block) when the loaded model classifies as tiny-class, naming the concrete upgrade path.",
            "frontend": "apps/desktop/src/config/appConfig.ts (sizeTierHint mirror) and apps/desktop/src/components/workspace/PilotDiagnosticsPanel.tsx (renders it in both the diagnostics summary line and the post-install confirmation message)."
        },
        "newTests": {
            "backendTestCount": 6,
            "testingBoundaryNote": "classify_model_size_tier and the extended diagnose_whisper_model are pure, filesystem-metadata-based, and fully unit-tested without a real model file - one test per size bucket plus a boundary test guarding an off-by-one in the threshold constants. What remains unverified in this container: whether installing a genuinely larger model measurably improves real-world detection accuracy/latency/rate against a real Nigerian-accented service (no real microphone audio or model weights available here - see modelPackagingStatement), and whether the byte-range heuristic correctly identifies every real quantized model variant (a known, documented limitation of a size-only heuristic, not a bug).",
            "command": "cargo fmt --check && cargo clippy --all-targets -- -D warnings && cargo test --workspace (default and --features whisper) && npm run typecheck && npm run lint && npm run test -- --run && npm run build",
            "result": "fmt clean, clippy clean (both feature configs), cargo test clean in both feature configs (cip-ai-speech 27/27 unchanged; cip-desktop 365/365, up from 359 - the 6 new tests). Frontend: typecheck 0 errors, lint the same 5 pre-existing warnings (unchanged), test 303/303 (unchanged - purely additive diagnostic field), build clean."
        },
        "regressionResult": "Full workspace cargo fmt/clippy/test clean in both feature configs; full frontend typecheck/lint/test/build clean. Zero regressions in any other crate.",
        "directBinaryProof": "x86_64-w64-mingw32-strings against cip-desktop.exe extracted from this exact installer confirms 20 of 21 prior-phase tripwire symbol/string counts unchanged, including whisper_full_with_state stable at 96 for a 16th consecutive rebuild. One documented, test-verified benign deviation: retry_paraphrase_or_semantic_with_fuller_context dropped from 2 to 1 distinct mangled symbols because the thin apps/desktop/src-tauri/src/pipeline.rs wrapper (unrelated to this phase's changes) was inlined into its two call sites by the release-mode optimizer this build - its own cip_core_service::bible_intelligence symbol remains fully present, and cargo test's unchanged 365/365 pass count (exercising this exact code path) confirms the behavior itself is intact. classify_model_size_tier was inlined by the optimizer, as expected for a small pure function - consistent with this project's own established precedent (should_flush_early/has_trailing_pause in Phase 21, fuzzy_match_book's internal helpers in Phase 20). diagnose_whisper_model remains directly present.",
        "frontendTextSearchLimitation": "The frontend bundle is not searchable as plain strings inside cip-desktop.exe the same way Rust symbols are - the new sizeTierHint field and its rendering were not independently string-searched, consistent with every prior phase's methodology.",
        "architecturalSafetyNotes": [
            "Zero new Tauri commands, zero new events, zero new migrations, zero schema changes.",
            "classify_model_size_tier and the extended diagnose_whisper_model are pure and have no effect on run_inference, is_multilingual, VAD-triggered flush (Phase 21), or any core/bible detection/matching logic - they only change what diagnostics say, never what the engine does.",
            "The new create_speech_engine startup log line is log::warn!, not a blocking check - a tiny-class model still loads and runs exactly as before this phase, unchanged behavior, only a new line in the log.",
            "Every other domain contract crate (core/bible, core/sermon, core/music, core/presentation) is entirely untouched."
        ],
        "honestLimitation": "True concurrent dual-tier (fast detector + high-quality transcript) inference is not implemented - this is exactly what the operator asked for and is real, substantial future work. classify_model_size_tier is a heuristic, not a certainty, by design. No real Nigerian-accented audio was available to test the recommendation against in this container - the improvement claim rests on whisper.cpp's own published model comparisons, not a measurement taken here. Nigerian Pidgin remains unsupported with no realistic path to support through CIP-side configuration.",
        "environmentCStatus": "NOT YET VERIFIED for this exact artifact - the decisive pending gate is a real operator installing a base.en/small.en (or large-v3-turbo) model via the System Diagnostics picker and confirming detection accuracy/rate improves during a real service - see physicalHardwareStatement item 31."
    },
    "queueStripRealSlideProof": {
        "phase": "24.1",
        "summary": "The Live/Preview stage's queue strip (shipped in Phase 24) now shows each prepared item's real RenderedSlide (word-wrapped body text + translation footer), not a raw-text approximation of PresentationContent.",
        "newTauriCommand": "get_prepared_item_slide(item_id) -> RenderedSlide",
        "howItWorks": "Looks up the already-persisted PresentationItem by id (persistence::get_presentation_item - the same lookup get_presentation_item already exposes) and calls cip_presentation_renderer::render_content on its content - the exact same pure rendering function display_presentation and get_presentation_display_state already call. Never mutates status; works for an item in any status, since it is a render, not a lifecycle transition.",
        "frontendWiring": "apps/desktop/src/lib/commands.ts gained getPreparedItemSlide(itemId). LiveChurchBrain.tsx and ServiceReplay.tsx each gained a queueSlides: Record<string, RenderedSlide> state populated by a useEffect that fetches a slide for any preparedItems entry not already cached (a failed fetch falls back to the prior raw-text approximation, never a broken card). LivePreviewStage.tsx's queue-item rendering now prefers the real slide's heading/body/footer.",
        "realBrowserVerification": "Headless Chromium with a mocked Tauri bridge (this session's established local verification technique - see docs/phase-23-audit.md/docs/phase-24-audit.md) extended with a get_prepared_item_slide mock keyed by item id. Confirmed on both Live Service and Service Replay that the queue-strip cards render the mocked RenderedSlide's real wrapped body text and translation footer, not the single-line raw-text snippet Phase 24 shipped.",
        "regressionScope": "PresentationCard, the real display window (PresentationDisplay.tsx), and every other presentation command/event are untouched. Zero new events, zero new migrations, zero schema changes.",
        "environmentCStatus": "Not yet performed - see knownLimitations."
    },
    "interimTranscriptProof": {
        "phase": "24.2",
        "summary": "WhisperSpeechEngine now runs one real, bounded interim decode per window, giving the operator's Live Transcript panel provisional text while a longer sentence is still being spoken, replaced by the settled segment once the window closes.",
        "howItWorks": "Once a window has buffered at least 1.5s (the same MIN_VAD_FLUSH_SAMPLES threshold Phase 21's VAD-triggered flush already used) without either flush condition firing, feed_audio calls the new try_interim_decode, which runs the exact same decode_pass (the real whisper.cpp full() call) the final decode uses, on just the audio buffered so far. At most one interim attempt per window (window_interim_decoded), so worst-case extra cost is exactly one additional decode pass, never unbounded. The interim segment shares its window's id with the eventual final segment, per TranscriptSegment::id's own documented contract.",
        "whyNoRealAudioValidationWasNeededUnlikeOverlappingWindows": "Unlike audio-overlapping windows (still explicitly not implemented - see docs/phase-21-audit.md), interim decoding never decodes the same audio twice in a way that needs reconciling: it decodes a strict, non-overlapping prefix of one window's audio, and that prefix's decode is always fully superseded (never merged/stitched) by the final decode of the complete window.",
        "frontendWiring": "LiveChurchBrain.tsx gained interimTranscript state, set by onTranscriptUpdated on a non-final segment (previously simply discarded) and cleared once that window's final segment arrives or the service ends. Rendered as a dimmed, provisional trailing line in the Live Transcript panel. ServiceReplay.tsx was not touched - it never runs the real WhisperSpeechEngine.",
        "realBrowserVerification": "Headless Chromium using Tauri's own official @tauri-apps/api/mocks module (mockIPC/mockWindows with shouldMockEvents:true) rather than this session's earlier hand-rolled event stub - confirmed an interim TRANSCRIPT_UPDATED event renders the dimmed provisional line, and the same window's later final event (same id) removes it and adds the real, timestamped, confidence-scored entry to the permanent transcript list.",
        "regressionScope": "Zero new Tauri commands, zero new events. run_inference's final-decode behavior (window boundaries, VAD gating, silence handling, non-speech-placeholder filtering, confidence scoring, language detection) is bit-for-bit unchanged - decode_pass is the same logic, only relocated so try_interim_decode can reuse it.",
        "environmentCStatus": "Not yet performed - see knownLimitations."
    },
    "dualTierWhisperProof": {
        "phase": "24.3 / 24.3.1 / 24.3.2",
        "summary": "CIP can now run a second, independently-loaded Whisper model (typically larger/slower than the fast tier) that re-decodes each fast-tier window's raw audio on its own thread, producing a new, linked transcript segment - never an in-place edit of the original.",
        "howItWorks": "WhisperSpeechEngine now retains the raw i16 audio of its most recently finalized window (last_final_window_audio), exposed via the new SpeechEngine::take_last_final_window_audio. commands::handle_audio_chunk takes that audio for every genuine final window and, if a quality engine is configured and ready, try_sends a QualityJob to a bounded channel. spawn_quality_worker (its own dedicated thread) calls the quality engine's transcribe_once (the same decode_pass real inference the fast tier uses, run in isolation on the given audio, with no buffering/window state of its own) and, on a real non-empty result, builds a new final TranscriptSegment (fresh id, same timing/speaker as the original) and routes it through finalize_bible_only/finalize_and_route_segment - the exact same pipeline every other final segment uses, including re-running Bible detection per the operator's own explicit choice.",
        "whyNoInPlaceEdit": "pipeline.rs's own documented principle - 'the transcript-and-detection record is never edited after the fact' - already applies to ScriptureDetection; Phase 24.3 extends the same discipline to transcript text. The corrected text lands as a new segment, linked to the original via a new transcript_corrections table (migration 0019), so what the operator actually saw live is never silently rewritten.",
        "backpressureDesign": "The quality channel is small and bounded (capacity 4) with try_send (never blocking send) - a quality worker still catching up on a backlog (expected, since it is by design a slower model) simply drops the newest job rather than ever stalling the fast tier's own live pipeline or audio capture. speech_quality_diagnostics.jobsDroppedBacklog counts this honestly.",
        "optInDesign": "The quality channel/worker are only ever created when state.speech_quality_ready is true (a real model successfully loaded from AppConfig.whisper_quality_model_path, installable via System Diagnostics' new 'Select Quality-Tier Model File...' button or CIP_WHISPER_QUALITY_MODEL_PATH). An operator who never installs a second model file gets zero new threads, zero new channel overhead, and zero change to the fast tier's own behavior.",
        "frontendWiring": "LiveChurchBrain.tsx tracks corrected-segment ids via the new TRANSCRIPT_CORRECTED event and renders a small 'corrected below' badge on the original transcript line - the correction itself already appears as its own new entry via the existing TRANSCRIPT_UPDATED handling, unchanged. PilotDiagnosticsPanel.tsx gained a quality-tier section mirroring the fast tier's own layout (install button + live diagnostics).",
        "realBrowserVerification": "Not performed this phase - judged not applicable the way Phase 24.2's frontend-rendering change was; this phase's real risk surface (two concurrently loaded Whisper models, a second thread, real audio hand-off) is exactly the class of behavior a headless-Chromium/mocked-Tauri-event harness cannot exercise. See docs/phase-24-3-audit.md.",
        "regressionScope": "Zero changes to any existing Tauri command's signature/behavior, zero changes to any existing event's payload shape, zero changes to any existing migration - two new commands, one new event, one new purely-additive migration.",
        "environmentCStatus": "Not yet performed - see knownLimitations and docs/phase-24-3-audit.md's own Known Limitations section for the full honest accounting.",
        "languageConditioningFix": "Phase 24.3.1: transcribe_once gained a language_hint parameter, populated in commands::handle_audio_chunk from the fast tier's own already-detected TranscriptSegment.language for the exact window whose audio is being handed to the quality tier (via the new QualityJob.language_hint field). WhisperSpeechEngine uses language_hint.unwrap_or(&self.requested_language) for that one decode call only - the engine's own separately-configured default (self.requested_language) is never mutated. Closes the gap where the two tiers could silently condition on different languages for the same audio, most visibly under \"auto\" language detection.",
        "backlogVisibilityFix": "Phase 24.3.2: SpeechQualityDiagnostics gained consecutive_jobs_dropped - reset to 0 by spawn_quality_worker the instant it dequeues and processes any job (a real correction, silence/placeholder, or even a transcribe_once error all count as evidence the worker is alive and keeping up), incremented by handle_audio_chunk every time try_send fails because the bounded quality channel is full. commands::classify_quality_backlog derives a QualityBacklogState (normal at 0, busy at 1, falling_behind at 2, overloaded at 3+ consecutive drops) - a streak rather than a duration, since quality jobs arrive as discrete, infrequent events (at most one per fast-tier final window) rather than a continuous stream the fast tier's own ms-based OverloadState/classify_overload measures. Purely additive diagnostics - never read by any decision-making code; the quality worker's own drop-newest-on-full-channel backpressure behavior is unchanged. See docs/phase-24-3-2-audit.md."
    }
}
