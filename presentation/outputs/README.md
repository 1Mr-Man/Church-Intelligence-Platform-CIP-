# Presentation outputs

Reserved for rendered output artifacts (e.g. exported slides, output-window
snapshots) produced by `presentation/renderer` at runtime. Nothing is
written here yet; contents of this directory besides this file are
git-ignored.
