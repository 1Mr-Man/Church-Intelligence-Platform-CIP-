[package]
name = "cip-integrations-vmix"
description = "vMix integration - a real vMix HTTP API client that pushes the current presentation text into an operator-configured title field."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
thiserror = { workspace = true }
ureq = { workspace = true }
