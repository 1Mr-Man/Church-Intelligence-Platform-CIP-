//! This crate has no library code of its own - it exists to host
//! cross-crate integration tests (`tests/foundation_wiring.rs`) that prove
//! the Phase 1 domains actually compose, not just compile in isolation.
